{
  "name": "team-tools-visual-audit",
  "private": true,
  "version": "1.0.0",
  "description": "Static-render visual audit: production partials + fixture-backed google.script.run in headless Chromium. Manual/on-demand — see README.md.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "shoot": "node build.mjs && node shoot.mjs"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
