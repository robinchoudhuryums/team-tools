---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: Batch C of the operator testing-notes round (2026-09-10) —
  N4 | Spanish Inbox: a manager-only "Auto-assign N unclaimed" button (least-loaded pick; a trigger may follow)
  N6 | Dept Requests: the card subject carries the patient name & TRX (stored at send), and every card gains Expand ⇄ Collapse over the source note through a scoped read
  N8 | Manage → Admin → Config: a "QA reviewers" editor for QA_MEMBERS (the answer to "where are QA agents added?"); a manager can now be ASSIGNED a recording
Files modified: web-app/Code.js, web-app/metrics/script_metrics.html, web-app/metrics/script_deptrequests.html, web-app/cn/script_callnotes.html, web-app/Tests.js, test/client/run.js, test/client/dom/runDom.js, test/visual/mock.js, test/visual/shoot.mjs, CLAUDE.md

CHANGES:
N4 | web-app/Code.js | `spanishAutoAssignPick_(unclaimed, members, load)` — PURE, Node-pinned: each unclaimed request (oldest first) goes to the member with the fewest live claims, alphabetical tie-break so two runs over one state pick the same member; existing claims are RESPECTED as load and never reassigned; no members → nothing. `spanishAutoAssignCore_(emp, days)` is the reusable body (a scheduled trigger can call it unchanged): members from `getSpanishInboxMembers_()` (refused by name when empty), pending through the EXISTING `getSpanishInboxPending` (one scope rule, one voicemail fold — voicemails are included, they are worked the same way), then ONE ScriptLock inside which the load + the still-unclaimed set are RE-DERIVED from the live claim map (a claim that landed between the read and the lock is respected, not overwritten), the pick runs, and ONE batched `setValues` writes the claim rows (the exact row shape `spanishClaimsFold_` reads: `[stamp, threadId, 'claim', by, self, nowMs]`). Counts-only audit row `SpanishInboxAutoAssign` (`assigned=N; members=M`) after the lock. `autoAssignSpanishThreads(days)` is the endpoint — MANAGER-gated (`{success:false, error:'Manager access required.'}`, writer shape), NOT the `canSeeSpanishInbox_` tier: a distribution decision is a manager's, like Assign.
N4 | web-app/metrics/script_metrics.html | `spanishAutoAssignBtnHtml_()` renders for a manager only, from STATE (`spanishUnclaimedCount_()` — null while the list has not loaded), disabled WITH THE REASON in its title in the three inert states (loading / no members configured / everything already claimed — INV-187: the count it shows is the count it will act on); it rides beside the filter strip in a flex row (`.sp-toolbar-row`, wraps on a phone) inside `spanishHeadHtml_`, and `spanishRenderList_` ends with `spanishRefreshAutoAssign_()` (an outerHTML swap of the whole button) so the count follows the list. `spanishAutoAssign_(btn)`: `uiConfirm` naming the count and the members → ONE RPC carrying `SPANISH_STATE.days` → every returned claim applied to the pending payload → ONE `spanishRenderList_()` (cards' claim pills AND the button) → toast; nav-guarded on `currentView`.
N6 | web-app/Code.js | `DR.PATIENT_TRX:13` + `'PatientTrx'` trailing on `DR_HEADERS` (self-heals via the existing `getOrCreateDeptRequestsSheet_` short-header branch); `DR_PATIENT_TRX_MAX = 120`. `emailFromCallNote`'s append writes `''` for ResolvedVia (the resolver's cell) and the capped `note.patientAndTrx` LAST; its "PHI-free" comment rewritten — the row now carries the second half of the constructed subject BY OPERATOR DECISION (the store stays inside the Workspace), note CONTENT still never enters it, and `deptRequestsOverdueOpen_` (the daily SLA digest) + every audit row stay LABEL-ONLY (pinned). `getDeptRequests` items carry `patientTrx` (additive; a legacy row reads ''). `drCanAct_(emp, row)` is THE ownership rule — sender / manager / member of ANY component dept via `drSplitDepts_` + `empDepartments_` — EXTRACTED from `resolveDeptRequest` (which now delegates, same error text) so the new read and the old write cannot disagree. `getDeptRequestDetail(requestId)`: employee gate with the bare `{error:'Not authorized.'}` read shape; a blank id, an unknown id AND a scope refusal all return the SAME `'Request not found.'` (existence never leaks); no lock, no audit row; base `{requestId, label, patientTrx, byName, toDept}` + `note` WHITELIST-built from the SENDER's own Sheet (`lookupEmployeeById_` → `getCallNotesSheet_` → `findCallNoteRow_` → `callNoteRowToObject_`: callback / caller / relationship / patientAndTrx / issue / transferredTo / resolution / dateLocal — never the raw row or subformData); `note: null` + a NAMED `reason` for a pre-NoteId row, an unenrolled/departed sender, an unreadable Sheet, or a deleted note (INV-187).
N6 | web-app/metrics/script_deptrequests.html | the subject reads `<label> · <patient & TRX>` (`.dr-subj-trx`, escaped; the label alone on a legacy row); every card renders a real `Expand`/`Collapse` button (`.sp-more.dr-expand`, `aria-expanded` + `aria-controls` — INV-173/174) in the actions row (which now always renders) and a `.dr-detail` panel rendered FROM STATE (`DR_STATE.bodies` / `expanded` — the A5 Spanish shape), so an in-place resolve repaint keeps an open card open and a second Expand costs no RPC. `drExpand_` mirrors `spanishExpand_` (nav-guarded; a button repainted away mid-fetch falls back to `drRepaintRequest_`); `drDetailHtml_` escapes every field and renders `note:null`'s reason rather than an empty panel. CSS: `.dr-detail[hidden]{display:none}` (the [hidden] companion), a `110px minmax(0,1fr)` label grid with a ≤480px stack (A2), `overflow-wrap:anywhere` on values.
N8 | web-app/Code.js | `saveQaMembers(emails)` — the `saveSpanishInboxMembers` shape exactly: admin-gated (INV-136, count 49 → 50), array check, cap 30, email-shape validation naming the offender, lowercase + trim + dedupe, writes Script Property `QA_MEMBERS`, `AdminConfigChange` audit; an EMPTY list is valid (= managers only). `getAdminConfig.qaMembers` ships the sorted list. `qaCanReviewEmail_(email)` = a `QA_MEMBERS` entry OR a roster row marked manager (found through `empRosterEmail_`, INV-183; a failed roster read falls back to the members list) — mirrors `canSeeQa_`; `qaAssignRecording`'s target check routes through it with a message naming the Admin card, so a manager can now be assigned a recording by another manager (the old `QA_MEMBERS`-only check refused the very people `canSeeQa_` admits).
N8 | web-app/cn/script_callnotes.html | a "QA reviewers" card after the QA scorecard-criteria card — the Spanish-members card's markup (`.cn-spm-chips`, ids `cn-admin-qam-*`), `cnRenderQaMemberChips_` whose empty state SAYS what no reviewers means (every manager still reviews), Add validates + dedupes client-side, Enter adds, an EMPTY save danger-confirms ("Clear all QA reviewers?"), `saveQaMembers(out)`.
N8 | web-app/Tests.js, CLAUDE.md | omnibus cases `saveQaMembers` (ADMIN tier — `ADMIN_GATED`) and `autoAssignSpanishThreads` (MANAGER tier); INV-136 count 49 → 50 at both sites + the backticked `saveQaMembers` entry (F7/F9/GATE-TIER all derive from these).
N6 | web-app/Tests.js | `test_deptReq_detailScoped` (registered after `deptReq_incomingAndMemberResolve`): a 14-col fixture row with a non-roster sender; the India rep as a receiving-dept member reads the base (`patientTrx` rides back) with `note:null` + a named reason; the PH rep is refused with `'Request not found.'`; a manager reads it; an unknown id reads exactly like a scope refusal. Editor suite 308 → 309.
pins | test/client/run.js | C-N4 (the pick driven — round-robin / existing load / balancing / empty cases; gate-before-core; pending reuse; NO voicemail filter; lock + live re-derivation inside it + batched setValues + counts-only audit after release; the client button/refresh/confirm/one-render; the fixture routing through the VERBATIM pick inside the DO-NOT-EDIT region; the omnibus case on the manager tier), C-N6 (store/enum/header/cap/append order; `drCanAct_` driven; both callers delegate with no inline membership; the detail contract — gate shape, the ONE not-found on scope refusal, no lock/audit, the standard readers, whitelist note, four named reasons; the list item; the digest + audit trail label-only; the client subject/expand/escape/CSS/breakpoint; fixture-as-a-function + legacy row + scenario; the editor test present + registered), C-N8 (`saveQaMembers` driven in a vm — gate, non-array, bad email, canonical write, audit, empty clears, cap; `qaCanReviewEmail_` driven incl. a failed roster read; the assign routing; `getAdminConfig`; the card + handlers; fixture; omnibus tier). REWRITTEN IN PLACE (the honest bookkeeping): N3-DR (a) for the new trailing slot, and the cycle-17 batch-3 fixture-shape pin — its mock-WIDE ban on `patientTrx:` was scoped to the coaching rows, because the Dept Requests item's server field IS spelled `patientTrx`. Pure 781 → 784.
pins | test/client/dom/runDom.js | C-N4 DOM (a non-manager member sees no button; the manager's button counts the UNCLAIMED (2 of 3); a cancelled confirm makes NO RPC; confirm → ONE RPC carrying the window → no Claim button remains → the button reads 0, disabled, and says why — `uiConfirm` reassigned through the vm bridge, the handler called directly per the jsdom onclick trap), C-N6 DOM (subject escaped — a `<b>` in the patient text never becomes markup; a legacy row's label alone; Expand fetches ONCE through the scoped read; the issue text's `<img>` renders as TEXT; Collapse hides with no RPC; a repaint keeps it open from state; `note:null` renders the reason). DOM 110 → 112.
fixtures | test/visual/mock.js, test/visual/shoot.mjs | verbatim `spanishAutoAssignPick_` in the DO-NOT-EDIT region (the F4 mirror pin derives it); `autoAssignSpanishThreads` fixture runs the verbatim pick over the pending fixture and updates its claims; `getDeptRequestDetail` is a FUNCTION of the id (F14; r6 is the legacy row → note:null + reason); DR rows carry `patientTrx`; `getAdminConfig.qaMembers`; scenario `deptreq-expanded-light-wide` (post hook `drExpand_(…)`). Visual matrix 101 → 102.

TEST RESULTS: pure harness 784 passed / 0 failed (781 → 784); DOM harness 112 / 0 (110 → 112); visual matrix re-shot for the six touched scenarios (spanish-light-wide, spanish-light-mobile, deptreq-light-wide, deptreq-expanded-light-wide, admin-config-light-wide, admin-config-light-mobile) — all machine-clean (no missing fixture, 0 overflow, 0 errors) and eyeballed: the Auto-assign button sits beside the filter strip and reads "2 unclaimed" (t1 + the voicemail t7 — the claimed t2/t5 are not counted) and wraps under the strip at 390px; the DR cards read "Close Order · S. Alvarez · TRX 48230" with the expanded panel's label column and the legacy r6 row rendering its label alone; the QA reviewers card renders between the criteria card and the AI settings with the fixture's chip. Bite-checks: 13 mutations / 13 bites (16 harness runs — B10/B11/B13 bit in BOTH harnesses; B3 bit C-N4 AND the F4 mirror pin; B9 bit C-N8 AND F7 AND GATE-TIER), every one against a COMMITTED tree, restored via `git checkout`. Editor suite (Test Command manual — not executed here): 308 → 309 expected. Regression Scenarios walked (subsystems overlapping the modified files): S74 Dept Requests — PASS on the harness + camera halves (subject with patient & TRX, Expand ⇄ Collapse, in-place resolve keeping an open card open); its editor half (`test_deptReq_detailScoped`) NOT RUN (editor-only); S80 Spanish share chart — PASS (unchanged, on camera beside the new button); S90 QA — PASS for the editor card (on camera) + the assign refusal message (pinned); the assign UI itself NOT re-shot; S57 compliance audit panel — PASS by pin (the new `SpanishInboxAutoAssign` row is counts-only; no DR audit row carries the patient); S97 Admin System — NOT APPLICABLE (untouched); S1/S2 — NOT RUN (Apps Script editor only).
REGRESSION RISKS: (1) `getDeptRequests` keeps its `dept_req_v1` result-cache key (Batch B's posture for `resolvedVia`): for ≤90s after deploy a cached payload lacks `patientTrx` and the subject renders the label alone — the client guards on the field. (2) `getDeptRequestDetail` reads the DeptRequests tab in FULL (`getDataRange`, the `resolveDeptRequest` posture — an old request must still expand) rather than the bounded list read; fine at this volume, noted. (3) The N3-DR pin's "trailing slot" assertion was rewritten for the new column — a deliberate contract change, not a weakening. (4) `qaCanReviewEmail_` walks the (5-min-cached) roster per assign — cheap. (5) A failed Expand shows a toast and restores the button rather than rendering an in-panel error card (the A5 precedent; INV-175's "never render as empty" holds — the panel stays hidden, nothing reads as an empty note).
INVARIANTS AT RISK: INV-138 / the Dept Requests KDD — their "PHI-free store" claim is now FALSE by operator decision (PatientTrx is stored; the SLA email + AuditLog stay label-only) and needs the amendment below; INV-31's Spanish endpoint list — `autoAssignSpanishThreads` is deliberately on the MANAGER tier, not the seven-endpoint `canSeeSpanishInbox_` tier (document it); INV-196's `qaAssignRecording` assignee check now admits roster managers via `qaCanReviewEmail_` (document it); INV-136 already updated in-batch (50). INV-01 (the core is locked), INV-02 (manager gate first), INV-32 (counts-only / label-only audit rows), INV-173/174 (real buttons + aria state), INV-183 (roster inclusion through `empRosterEmail_`), INV-185 (verbatim pick + derived fixtures), INV-187 (named reasons, disabled-with-reason) — all held and pinned.
NET SCORE: 1 − 0 = +1 (N6's collapsed card was unidentifiable to the desk working it — an operator-observed interface defect on the running app, counted per the R18 rule. N4 (auto-assign), N6's Expand and N8's editor are CAPABILITIES (3); N8's assign-check widening is DEFENSIVE (1) — a real refusal, but not one the operator hit.)

OPERATOR ACTIONS / DEPLOY:
- None blocking. No Script Property, trigger, migration, OAuth scope or CONFIG value is added: the `DeptRequests` tab's 14th column (`PatientTrx`) SELF-HEALS on first touch (legacy rows read blank and render the label alone); `QA_MEMBERS` becomes editable in-app (direct property editing still works). | BLOCKS DEPLOY: N
Deploy: Server + Client (Metrics views) + Client (Call Notes views): `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy (open windows get the reload prompt within ~20 min). Test Suite: same push; post-deploy `runAllTests()` — expect 309 (the new `deptReq_detailScoped`; the omnibus grew IN PLACE).

FOLLOW-ON ITEMS:
- web-app/Code.js — the N4 SCHEDULED trigger the operator said "might follow": an INV-44 `assertManagerCaller_` handler calling `spanishAutoAssignCore_` (reusable by construction), added to BOTH `installAutomationTriggers` TARGETS arrays with a heartbeat — the trigger-wiring/gate-type tripwires will auto-cover it. Not built: the operator asked for the button first.
- web-app/Code.js (getDeptRequestDetail) — a full-sheet scan per Expand (the resolve posture). If the tab ever grows past the bounded list window, a NoteId/RequestId column scan + one-row fetch (the `findCallNoteRow_` shape) is the cheaper read.
- web-app/metrics/script_deptrequests.html — the Expand panel shows the note as it is NOW (the sender's live Sheet), not as it was at send; an edited note reads differently from the email that went out. Acceptable (the email is the record), stated here so nobody reports it as drift.
- web-app/Code.js (getDeptRequests) — the `dept_req_v1` cache key was kept for the additive `patientTrx` (≤90s stale after deploy). A bump would cost one pin update; not worth a key change for a field the client guards.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md — INV-138 + the Dept Requests Key Design Decision: the store is NO LONGER PHI-free (`PatientTrx`, operator decision 2026-09-10 — the constructed subject's second half; the SLA digest and every audit row stay label-only; `DR_HEADERS` is 14 wide; `drCanAct_` is the ONE ownership rule shared by `resolveDeptRequest` + `getDeptRequestDetail`; the scoped detail read's not-found-on-refusal contract; the client subject + Expand ⇄ Collapse from state).
- CLAUDE.md — INV-31: `autoAssignSpanishThreads` is MANAGER-gated (deliberately outside the seven-endpoint `canSeeSpanishInbox_` tier); the Spanish claims block gains `spanishAutoAssignPick_` (pure) + `spanishAutoAssignCore_` (reusable for a trigger) + the `SpanishInboxAutoAssign` counts-only audit action; the Spanish Inbox KDD gains the button (manager-only, beside the filter strip, disabled-with-reason, one RPC + one render; voicemails included).
- CLAUDE.md — INV-196 + the QA operator-state entry + S90: `QA_MEMBERS` is now edited in-app (Manage → Admin → Config → QA reviewers, `saveQaMembers`, admin-gated); `qaAssignRecording`'s target check is `qaCanReviewEmail_` (member OR roster manager — a manager can be assigned); the "set QA_MEMBERS" setup step gains the in-app path.
- CLAUDE.md — Regression Scenarios: S74 gains the subject + Expand/Collapse + scoped-detail steps (incl. the not-found-on-refusal check and the legacy-row label-alone case); S90 gains the QA reviewers card + assign-a-manager steps; a Spanish auto-assign scenario (S80's neighbour or a new S105: manager-only button, unclaimed count, confirm, one render, disabled states; a member sees no button).
- CLAUDE.md — counts: pure 784, DOM 112, visual matrix 102 (`deptreq-expanded-light-wide`), editor 309; the Test Suite paragraph's running-total prose (C-N4/C-N6/C-N8 + the two DOM tests + the N3-DR/batch-3 in-place rewrites and WHY the batch-3 casing ban had to be scoped).
- CLAUDE.md — Operator State Checklist: a "Batch C adds NO operator state" entry (no properties/triggers/migrations; the 14th DeptRequests column self-heals; `QA_MEMBERS` editable in-app; behaviours to expect: the Auto-assign button, the patient & TRX on Dept Requests cards + Expand, the QA reviewers card; post-deploy `runAllTests()` expect 309).
- CLAUDE.md — INV-136's note that the count has "drifted five times" — this batch updated both sites in the same commit; no further drift to record.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
