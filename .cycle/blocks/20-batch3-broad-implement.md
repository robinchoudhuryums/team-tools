---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F-07 — `mPctClass_` (Metrics table) and `dashPctTone_` (Clock KPI card) disagreed on a NULL amber band: 0 pt on the table, the hand-carried 5 pt on the card. F-08 — the server shipped `standardSource` with every Metrics payload and nothing rendered it, while the badge tooltip still said `res.threshold || 85`. F-09 — the holiday calendar fell back to the federal list on an absent / empty / unreadable `Company Holidays` tab and nothing said which calendar was live (the fallback itself: DEFERRED, per the plan). F-35 — the manager badge judged calendar-yesterday (silent every Sunday and Monday; the morning after a holiday judged an empty CDR day). F-32 — `cdrAnswerPct_` returned 0 when answered + missed was zero, so a window of rung legs with a third disposition read as "every call missed". F-38 — six sites of "US holiday / Federal observance" copy on the time-off and manager surfaces.
Files modified: web-app/script_core.html, web-app/metrics/script_metrics.html, web-app/tc/script_clock.html, web-app/40_metrics.js, web-app/10_core.js, web-app/cn/script_callnotes.html, web-app/tc/script_manager.html, web-app/tc/script_timeoff.html, web-app/Tests.js, test/client/run.js, test/client/server-split-manifest.json, test/visual/mock.js, CLAUDE.md (generated block), .cycle/STATE.md

Estimate: M (~6 h) — recorded 2026-09-17 in STATE.md BEFORE the first edit
Actual: ~1.5 h

CHANGES:
F-07 | web-app/script_core.html, web-app/metrics/script_metrics.html, web-app/tc/script_clock.html | new `mtAnswerBand_(band)` beside `mtPctTone_` is the ONE null-band rule for % Answered: a published band is the amber width; no band (null / absent / non-numeric) is 0 — below the target is red on BOTH surfaces. `mPctClass_` and `clkDashTone_`'s answered branch delegate to it; the Transfer % branch still passes no band and keeps `DASH_TONE_SLACK_PP`. The stale "mid band floor stays 50" pin message is replaced by the rule it now asserts.
F-08 | web-app/metrics/script_metrics.html, web-app/10_core.js, web-app/cn/script_callnotes.html | new pure `mStandardSourceHtml_(thr, source)` renders the standard's provenance in the hero delta row of My Stats AND Team Metrics ("from the Dashboard Standards tab"; muted "no Dashboard Standards row … — no target, tone or badge" for no-row / empty / no-tab; warn for `unavailable`; silent on an older payload). The badge tooltip drops `|| 85` and names the Dashboard Standards target (a badge exists only with a published one). Server: new `cdrStandardProbe_()` rides the CDR Storage Health row as `standard: {dept, target, band, source, error}` when the store is reachable; client `cnStandardFindings_` raises ONE CDR-area finding — ok for `sheet` / `global` (target + band, or "no amber band — below target is red"), warn for the three missing shapes (naming the three silences + the 1h cache), warn with the reader's error for `unavailable`; the inventory row's detail states the same facts.
F-09 | web-app/10_core.js, web-app/cn/script_callnotes.html | new `cdrHolidayProbe_()` rides the same row as `holidays: {source, ranges, thisYear, year, error}` (a `sheet` source with no ranges is reported as `empty` — the shape `getCompanyHolidays_` actually falls back on); `cnHolidayFindings_` — ok when the tab lists dates this year; warn when the tab has ranges but none this year (the yearly-maintenance reminder); warn for empty / no-tab ("the computed US-federal list is in use instead — Columbus and Veterans Day count as closures…"); warn with the error for `unavailable`. The CDR row's role now names both tabs. The federal fallback is UNCHANGED.
F-35 | web-app/40_metrics.js | `getMetricsAmbient` judges `prevWorkdayIso_(todayMgr)` (weekends AND company holidays stepped over, manager tz kept); the hand-rolled UTC step-back and the weekend early-return are gone; `!yIso` → no badge. Tooltip copy reads "Previous workday (date)".
F-32 | web-app/40_metrics.js, web-app/metrics/script_metrics.html, web-app/Tests.js | `cdrAnswerPct_` returns null when answered + missed is 0 (`0/3` is still a real 0). Consumers checked: both heroes route through new `mPctValueHtml_` (dash for null, never "null%"); the team table's % Ans cell renders a muted dash for null; `mTrendAvg_`, `mBestWorstDays_`, `mBuildHeroSparkSvg_`, `metricsTeamAvgSeries_`, `metricsBuildKpiSeries_`, the Clock card, the CN Stats tab and the CSV export already skipped or dashed null; the ambient badge already guarded. The editor smoke `test_cdrAnswerPct_isTheDashboardFormula` and H2-1 now expect null.
F-38 | web-app/tc/script_manager.html, web-app/tc/script_timeoff.html | "(US holiday)" ×2, "US holiday" ×2, the "US Holiday" legend and the "— US holiday / Federal observance; offices may be closed." conflict card read "Company holiday" / "From the Company Holidays calendar; offices may be closed."; the one comment at the chip site updated. The "weekends and US holidays excluded" strings in Dept Requests / Metrics / Coaching are NOT in F-38's scope (they have their own pin) — noted below.
Pins | test/client/run.js | seven new: F-07 (behavioural grid — the two vocabularies agree on nine (value, target, band) triples; the null-band and Transfer % branches; both delegations structural); F-08 client (every source string, the warn/muted split, both heroes, the tooltip's `threshold || \d` ban, the fixtures); F-08/F-09 server (both probes driven through sheet / no-row / empty / unavailable / throw; the reachable-only gate); F-08/F-09 client (`cnHealthFindings_` driven through every source for both findings; an older payload raises neither; the detail row); F-35 (behavioural — `getMetricsAmbient` in a vm: Monday asks for Friday, a Friday holiday steps to Thursday, midweek asks yesterday; no weekday check survives); F-32 (null from the formula, the team aggregate, both heroes, the cell, the averages, the editor smoke); F-38 (no "US holiday" / "Federal observance" in the two tc partials; the six sites by shape). H2-1 and H2-4 updated to the new rule/shape.
Tooling | test/client/server-split-manifest.json, test/visual/mock.js, CLAUDE.md | manifest regenerated for `cdrStandardProbe_` + `cdrHolidayProbe_` (1286 declarations; three revisions: `cdrAnswerPct_`, `getMetricsAmbient`, `getStorageHealth`); the three Metrics fixtures carry `standardSource: 'sheet'` and the Storage Health fixture's CDR row carries both probe shapes (INV-185); counts block pure 871 → 878.

TEST RESULTS: passed. Pure harness 878/878 (was 871 — seven new pins), DOM harness 126/126 (unchanged), `lint-server` clean (16 files), `split-manifest --check` current, `counts --check` green. Editor suite NOT RUN HERE (editor-only); registrations unchanged at 337 (one smoke assertion changed: `cdrAnswerPct_(0, 0)` → null). Visual matrix: `metrics` (7 scenarios) and `admin-system` (7) shot — 0 missing fixtures, 0 overflow; the Team hero shows "target 85% · from the Dashboard Standards tab" and the CDR Report row gained its detail chevron with the passing-check count up by two (the two new ok facts).
Bite-checks — TEN written, TEN bite (committed file mutated, the named pin went red, file restored): B1 `mtAnswerBand_` null → 5 again → F-07 · B2 the Clock card bypasses the rule → F-07 · B3 the `unavailable` message says "not set" → F-08 · B4 the probes run on `configured` instead of `reachable` → F-08/F-09 server · B5 a sheet source with no ranges stops reading as `empty` → F-08/F-09 server · B6 a listed tab with no dates this year reads ok → F-08/F-09 client · B7 the badge judges today instead of the previous workday → F-35 · B8 `cdrAnswerPct_` returns 0 again → F-32 · B9 the hero prints the raw null → F-32 · B10 the conflict card says "US holiday" again → F-38.
Regression Scenarios (Test Command is `manual` — no editor or browser here):
- S1 / S2 | Test Suite | NOT RUN HERE — editor-only; no registration change. One assertion changed (null, not 0).
- S41 Metrics — My Stats | Server, Client (Metrics) | NOT RUN HERE — browser. On camera (fixture): the hero delta row gains "· from the Dashboard Standards tab" after the target chip; layout holds at wide and mobile (0 overflow).
- S42 Metrics — Team Metrics presets | Server, Client (Metrics) | NOT RUN HERE — browser. Same source line on the team hero; the % Ans cell of a rep with rung legs but no answered/missed now shows a muted dash instead of a red 0%.
- S43 Metrics — CDR unavailable | Server, Client (Metrics) | NOT RUN HERE — browser. Unchanged: the CDR-unavailable path returns before any hero renders; `getStorageHealth` runs neither probe when the CDR store is unreachable (the row's own fail finding covers it).
- S46 Time / PTO page | Client (Time Clock) | NOT RUN HERE — browser. The legend reads "Company holiday"; a holiday conflict card reads "— Company holiday · From the Company Holidays calendar; offices may be closed."
- S72 Coverage planner · S98 Manage surface | Server, Client (Time Clock) | NOT RUN HERE — browser. The approval-queue chip and the team-calendar day title read "(Company holiday)". No logic changed.
- S97 Admin → System findings-first | Client (Call Notes) | PASS on camera (fixture) + pinned: the all-clear fixture still reaches "Nothing needs attention" (both new findings are ok facts on a published standard + a listed calendar); the two warn shapes are pinned, not shot. The CDR Report row's chevron opens "Dashboard Standards: sheet · target 92% · amber band 2 pt · dept CSR" and "Company Holidays: sheet · 9 in 2026 (9 on the tab)".
- S110 Company holidays | Server, Client (Metrics) | NOT RUN HERE — browser + the live tab. Step 3 now ALSO covers the manager sidebar badge (previous workday, dated); step 5 (row parked → federal fallback) now shows a "Company Holidays tab is empty" warning on Admin → System's CDR card within the hour — walk it with the batch.
- S111 One answer rate, one standard | Server, Client (Metrics, Time Clock) | NOT RUN HERE — browser + the live tab. Step 4's Dashboard card and the Team table now agree with a 2-pt band AND with no band (both red one point under); step 5 (rows parked) now renders the muted "no Dashboard Standards row for this department — no target, tone or badge" line on both heroes and a warn finding on Admin → System. Expected text needs the source line added (docs).

REGRESSION RISKS:
- `cdrAnswerPct_` now returns null; every server consumer stores it as-is (`agents[k].pctAnswered`, daily, per-rep-day, `teamTotals`, the team benchmark, `getShiftStats`' CDR overlay) and every client consumer either dashes or skips null (verified per site above). A FUTURE consumer that does arithmetic on `pctAnswered` without a null guard gets NaN — the g55 class; the F-32 pin lists the consumers it checked. The editor smoke asserts null.
- The Clock card's % Answered tone tightens: with a published standard that has NO band, a value one point under the target is now red where it was amber. That is the finding (the table already said red); a standard WITH a band is unchanged. Transfer % is untouched.
- The ambient badge can now fire on a Monday (judging Friday) and on the morning after a holiday (judging the workday before) — two mornings that were silent. Cache key unchanged (`metrics_ambient_v2:<threshold>`), TTL `CDR_CACHE_TTL`; the cached `date` says which day it judged.
- `getStorageHealth` makes up to two more reads on the CDR workbook (both memoized + 1h-cached readers; a throw is caught into `source:'unavailable'`). `getDeployReadiness` calls it with `scanEmbeds:false, checkDrive:false` and now also gets the two fields — additive, unread there.
- The two new findings use area `cdr`; if the automation payload failed but storage loaded, the CDR overview card already reads "Unavailable" (degraded) and the findings still list — the same posture as the existing storage-derived findings.
- `mStandardSourceHtml_` renders nothing for a payload without `standardSource` (≤5-min stale cache, older server) — the pre-batch look.

INVARIANTS AT RISK: none violated. INV-02 (no gate changed; `getStorageHealth` stays admin-only, `getMetricsAmbient` manager-only), INV-85 (no cached shape needs a key bump — `pctAnswered: null` is a value change within the same shape; `getMetricsAmbient`'s cached object gained nothing), INV-185 (every fixture updated with the server's shape, pinned), INV-186 (both new findings read ok on a normal deployment — the all-clear fixture still reaches "Nothing needs attention"), INV-175/187 (this batch is the class again: two silent fallbacks and one hand-carried number now speak), INV-209/212 untouched. g123's fallback rule is unchanged (deferred); g128's "not found vs could not look up" split is applied to both new findings.

NET SCORE: 4 − 0 = 4
  a) Would this have fired in production this month? F-07 YES for the seed install (CSR 92 / 2 publishes a band, so the two surfaces AGREE today — but the first standard published without an Amber Band, or the `*` row without one, splits them; counted because the disagreement is a wrong tint on a live card whenever it occurs; if the operator reads this as "not this month", score it defensive). F-08 YES (the tooltip said 85 on every badge since H2 shipped; the source line is the capability half). F-09 NO-as-observed — the tab exists on the seed install; the finding is the missing SURFACE (defensive). F-35 YES (every Monday morning the badge was silent; every Monday the dashboard side had Friday's rate). F-32 YES for the class (a rung-only DQE day reads 0% — whether one landed this month is unverified; counted because the 0 is wrong on every occurrence). F-38 NO (copy; defensive/interface — the calendar is already the company one).
  b) New failure mode? NO for all six. The one behaviour a reader should know: a null band on the Clock card is now red one point under, not amber.

OPERATOR ACTIONS / DEPLOY:
- `clasp push -f` + cut a New version. | BLOCKS DEPLOY: Y
- `runSmokeTests` on prod — registrations still 337; read the count off the run. | BLOCKS DEPLOY: N
- Manage → Admin → System after the deploy: read the CDR Report row's chevron (Dashboard Standards + Company Holidays sources) and confirm the two new ok facts are in the passing list; if either warns, that IS the finding — fix the tab, not the app. | BLOCKS DEPLOY: N
- The first Monday after the deploy: the manager badge (if Friday was under target) should be dated Friday. | BLOCKS DEPLOY: N
- S111 step 5 / S110 step 5 walks now have a visible outcome on Admin → System and on both heroes. | BLOCKS DEPLOY: N
Deploy: Server — `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. Client (Metrics, Time Clock, Call Notes views) — same push, same New-version step.

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- `web-app/Tests.js:1897` `test_teamBenchmark_subtractsPublishedExcludes` expects `85.7` for the with-manager benchmark, but `cdrAnswerPct_` has rounded to a WHOLE percent since H2 (run.js H2-2 expects 86). That editor test is RED as written and has never run against the deployed code (the integration tier has never run — cycle 19 step 8). Out of Batch 3's scope; a one-line fix for `/test-sync`.
- "weekends and US holidays excluded" copy at `metrics/script_deptrequests.html:166,175`, `metrics/script_metrics.html:2050`, `train/script_coaching.html:527` (+ the Spanish auto-assign toggle description in `00_config.js:1266`) still says "US"; the pin at `run.js:16930` asserts that string. Same class as F-38, not in its six sites.
- `40_metrics.js:1927` (`getShiftStats`-side rep rows) still hand-carries `pctAnswered: cdr ? cdr.pctAnswered : 0` for a rep with NO CDR row — a 0 that means "absent" (the F-32 class one level up). Not touched here; the CN Stats tab dashes null but receives 0.
- The two warn states of each new finding (no standard / federal fallback) are pinned but not on camera; the Visual Audit Stage's "a new block ships THREE scenarios" rule would want a `?fixture=` variant of `admin-system` carrying `source: 'no-row'` / `'no-tab'`.
- The Storage Health standard/holiday probes read through the 1h caches; the fix copy says so. A `getStorageHealth({fresh:true})` bypass would make the operator's "I just added the row" check immediate.

DOCUMENTATION UPDATES NEEDED:
- `.cycle/config.md` S111 expected (step 5: the muted source line on both heroes + the Admin → System warn finding) and S110 expected (step 3 also the badge; step 5 the "tab is empty" warning); INV-209/H2 entries: `mtAnswerBand_` is the one null-band rule; a new invariant for "a published standard's SOURCE is rendered wherever its verdict is" if the library wants it.
- `docs/gotchas.md` g123 (holiday source now surfaced on Admin → System; fallback unchanged) and g124 (the null-band rule; the badge judges the previous workday; `cdrAnswerPct_` null); CLAUDE.md index lines for both.
- `docs/operator-state.md` the `Dashboard Standards` and `Company Holidays` entries: Storage Health shows each tab's source; the two findings and what they mean.
- `docs/modules.md` Metrics (source line, null rate dash, Monday badge) and Manage → Admin (the two CDR findings).
- `docs/operator-log.md` a Batch 3 entry; `docs/test-harness-log.md` the seven pins + the vm-driven `getMetricsAmbient`.
- The running-totals block is regenerated (pure 878).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
