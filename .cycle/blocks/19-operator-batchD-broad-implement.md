---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: Batch D of the operator testing-notes round (2026-09-10) —
  N10 | Team Right Now: a warn chip "active · not clocked in" (or "active · clocked out") for a teammate who is USING THE APP — a real gesture within ~30 minutes — while their punch state says they are not working; the count is named in the card summary
Files modified: web-app/Code.js, web-app/script_core.html, web-app/tc/script_clock.html, web-app/styles.html, web-app/Tests.js, test/client/run.js, test/client/dom/runDom.js, test/visual/mock.js, CLAUDE.md

CHANGES:
N10 | web-app/Code.js | `recordPresence()` — rep-gated (`{success:false}` for a non-employee, the recordViewEnter shape), NO lock, NO sheet write, NO audit row: ONE CacheService put under `PRESENCE_CACHE_PREFIX + emp.id` ('presence_v1:') with `PRESENCE_TTL_SEC` (1800 — the ~30-minute window the chip's tooltip names); never throws. `presenceMap_(empIds)` — ONE `getAll` over the roster's keys → `{id: true}`; a blank value is not a hit; ANY failure returns `{}` (no flags — a missed chip is the safe failure, INV-187/190). `teammateActiveNotIn_(isSelf, present, status)` — PURE, Node-pinned: self is never flagged; a present rep is flagged only in a NON-working state (`not_in` / `clocked_out`; lunch and clocked-in never). `getTeammateStatus` folds the map into ONE additive boolean `activeNotIn` on each row — the row literal carries exactly `name / status / isSelf / activeNotIn`, never the stamp's time (INV-24 amendment) — keeps the C8 order (auth before the flag read), and sorts a flagged rep ahead of the unflagged within its status. **DESIGN DEVIATION from the STATE.md-recorded shape, deliberate:** the stamp is NOT written inside the polls reps already hit (`getEmployeeState`'s 3-min reconcile, the 60s Call Notes ambient poll, `recordViewEnter`) — a pinned pop-out left open overnight keeps polling, and a poll-written stamp would flag its owner "active but not clocked in" at 7am before they arrived, the false positive that teaches reps to ignore the chip. A gesture is the one signal a left-open window cannot fake; the non-stamping of all three polls is pinned.
N10 | web-app/script_core.html | the presence beacon: `presenceOnGesture_` fires `recordPresence` on a real user GESTURE (pointerdown / keydown, bound `capture + passive` by the idempotent `presenceBind_`, called at shell boot beside `remindersStart_` — a shell capability so the pinned pop-out counts, INV-190), throttled to ONE send per `PRESENCE_MIN_GAP_MS` (10 min) per window, with the stamp landing BEFORE the send so a slow or failed RPC never double-fires; fire-and-forget (both handlers empty); NO second interval — an idle or left-open window costs nothing. View-as previews stamp under the admin's own id (they ARE the one interacting).
N10 | web-app/tc/script_clock.html | `clkActiveNotInChipHtml_(t)` renders ONLY on a strict `activeNotIn === true` (an older server ships no field → nothing), the label per status, and a tooltip stating the v1 limit out loud ("Shift hours and time off are not checked — they may be off shift or on PTO."), escaped; `renderTeammateCard` mounts it on the status line, counts strict-true rows into a NBSP-joined summary tail (" · N active but not in" — the count can never split from its phrase when the summary wraps), and the card head's two spans carry the new `emp-team-title` / `emp-team-summary` classes.
N10 | web-app/styles.html | `.emp-active-chip` — warn-toned via the deep alias (`--warning-deep` on `--warn-soft`), a wrapping BLOCK on its own line (`display:block; width:fit-content; max-width:100%; white-space:normal`); and two head rules at (0,3,1): the title keeps one line (`flex:0 0 auto; nowrap`), the summary is the flexible, right-aligned, wrapping half. Both are MEASURED fixes (see TEST RESULTS).
N10 | web-app/Tests.js | `test_getTeammateStatus_shapeRestricted` — ALLOWED_KEYS widened by exactly the one key + `typeof activeNotIn === 'boolean'` asserted; NEW `test_presence_stampAndFlag` (registered): a non-employee writes no stamp, an employee's stamp lands under the prefixed key, the pure rule's five cases, the PH viewer sees the India row flagged iff its status is non-working, self never flagged, no timestamp key on the row; the key is removed in `finally`. Editor count 309 → 310.
N10 | test/client/run.js | the D-N10 pin: (a) the pure rule driven (7 cases); (b) `presenceMap_` driven — hits map, a blank value is not a hit, a thrown read yields `{}`; the prefix + TTL constants; (c) `recordPresence` rep-gated + cache-only + no lock/sheet/audit; (d) the row literal's EXACT four keys (read per LINE — the literal uses ES shorthand, which the colon-only key extractor is blind to: its first write read two keys out of four), the flag from the rule, ONE getAll, no timestamp field, C8 kept, the flagged-first sort; (e) the three polls + `getTodayPunches_` do NOT stamp; (f) the beacon — gap constant, throttle, stamp-BEFORE-send, empty failure handler, capture+passive on both events, idempotent bind, bound beside the reminders, no setInterval; (g) the chip's strict-true guard, both labels, the tooltip's stated limit, escaping, the card wiring, the CSS class defined with the warn tokens, the chip as a wrapping block (never nowrap), the two head rules, the NBSP tail; (h) fixture rows carry the server's exact keys (INV-185), Leo flagged, the `recordPresence` fixture; (i) Tests.js widened + the presence case registered.
N10 | test/client/dom/runDom.js | D-N10 DOM: `renderTeammateCard` driven into a live DOM — exactly the two flagged rows carry a chip, the label per status, the tooltip's limit, a hostile name inert, the summary counts them (NBSP-normalized), an unflagged row and a FIELD-LESS (older-server) row render no chip, no flagged rep → no suffix; then the beacon: boot alone stamps nothing, the first gesture sends ONE `recordPresence`, gestures inside the gap are swallowed, past the gap the next sends again.
N10 | test/visual/mock.js | every teammate row carries `activeNotIn` (Leo Kim `true` — the chip is on camera in the four Clock scenarios); `recordPresence: {success:true}` so the beacon is never a missing fixture.
N10 | CLAUDE.md | INV-24 AMENDED in-batch (the stated shape changed) — the one boolean, the stamp/beacon/rule contract, the non-stamping polls, the v1 limit, the pins; S14 extended with the two-account walk + the chip's expected states.

TEST RESULTS: pure harness 785 passed / 0 failed (784 → 785); DOM harness 113 / 0 (112 → 113); visual matrix — the four Clock scenarios re-shot (clock-light-wide, clock-dark-wide, clock-light-mobile, clock-light-compact), all machine-clean (no missing fixture, 0 overflow, 0 errors) and the card eyeballed via wide/dark/mobile crops. **Two defects the first cut shipped, both found by MEASURING, neither visible in a PNG:** (1) report.json flagged +34px page overflow on clock-light-mobile — an element walk (skipping overflow-x scrollers) found both `.dash-foot` cards at 412px on the 390px phone, and re-rendering with the chip hidden gave 390/390: the inline `white-space: nowrap` pill propagated min-content through the shell's ≤540px `1fr` `.emp-grid`; the chip became a wrapping block. (2) the first wide crop showed the head as "Team / Right / Now" on three lines with the summary drawn over it — the longer summary squeezed the title under the shared `.card-label > span:first-child { flex: 1; min-width: 0 }` rule; a (0,2,0) fix LOST to that (0,2,1) rule (measured: title 10px wide), the (0,3,1) rules hold (title 108px one line, summary two lines beside it). Bite-checks: 11 mutations / 11 bites, every one against a COMMITTED tree and restored via `git checkout` — self flagged; presenceMap_ rethrows; a timestamp on the row; getEmployeeState stamping; stamp-after-send; the chip on every row (bit BOTH harnesses); the fixture unflagged; the gap zeroed (bit BOTH); chip nowrap again; the title rule dropped; the NBSP tail split (the DOM test deliberately normalizes NBSP, so the pure pin holds that property). Editor suite (Test Command manual — not executed here): 309 → 310 expected. Regression Scenarios walked (subsystems overlapping the modified files): S14 (extended) — PASS on the harness + camera halves (chip per status, strict-true only, self never, hostile name inert, one send per gesture gap, no timestamp on the row); its live two-account walk NOT RUN (needs the deployed app); S3 / S39 / S101 Clock — PASS on camera (rail, fold and Needs-you untouched; the card sits below the fold); S25 compact — PASS (compact scenario clean; the beacon binds in the pop-out too); S73 phone width — PASS by measurement (390/390 after the fix); S76 reminders — NOT APPLICABLE (the beacon shares only the boot site; the tick is untouched); S1/S2 — NOT RUN (Apps Script editor only).
REGRESSION RISKS: (1) The deviation above — one NEW tiny RPC (`recordPresence`, ≤1 per 10 min per ACTIVE window) instead of zero; the alternative (stamping from polls) was wrong, not merely costlier. (2) `getTeammateStatus` gains ONE `getAll` per call at the Clock SWR cadence — negligible beside its existing full Timesheet read. (3) Deploy skew is safe both ways: an older client ignores the field; an older server ships none and the strict-true guard renders no chip. (4) The chip is truthful but NOT schedule-aware — a rep on PTO or before their shift who opens the app is flagged; stated on the tooltip, gating is a follow-on. (5) The card summary may now take two right-aligned lines when the tail appears; the title never wraps. (6) A stamp outlives the gesture by up to ~30 min (TTL), so a rep who clocks out and closes the app can read "active · clocked out" for that long — the tooltip's "within the last 30 minutes" is the exact claim. (7) `test_getTeammateStatus_shapeRestricted` widened by exactly the one key.
INVARIANTS AT RISK: INV-24 — AMENDED in-batch (the boolean only; the timestamp never rides; pinned). INV-190 (a shell capability with NO second interval — gesture-driven, bound beside `remindersStart_`; the no-setInterval scan is pinned), INV-187 (a failed cache read yields NO flags and the tooltip states the limit — the chip's absence is never a verdict), INV-183 (roster inclusion unchanged — `empRosterEmail_`), C8 (auth before the flag read — pinned), INV-185 (fixture rows carry the server's exact keys — pinned), INV-173/174 (the chip is a status span, not a control — no aria owed; the `title` is elaboration on visible text). INV-01/08 are deliberately NOT engaged by the stamp (no lock, no audit — a volatile signal is not a record, stated in the code). All held.
NET SCORE: 0 − 0 = 0 (N10 is a CAPABILITY — the operator asked for a signal that did not exist; nothing fired in production. The two layout defects were introduced AND fixed inside the batch — defensive by the cycle-19pre rule, and the reason both harnesses' machine checks exist.)

OPERATOR ACTIONS / DEPLOY:
- None — no Script Properties, triggers, migrations, CONFIG values or OAuth scopes; one auto-managed CacheService key family (`presence_v1:<empId>`, TTL ~30 min, self-expiring). | BLOCKS DEPLOY: N
Deploy: Server + Client (shell, Time Clock views): `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: **New version** → Deploy (the ONE deploy the whole post-#176 backlog owes). Test Suite: same push; run `runAllTests()` post-deploy — expect **310**.

FOLLOW-ON ITEMS:
- Schedule/PTO gating of the chip (v1 flags ANY present non-working rep and says so): resolve each rep's shift (`empShiftSchedule_`) + `empIsOffToday_` inside `getTeammateStatus`, or ship the window to the client — an OPERATOR decision on which states should stay un-flagged (pre-shift? PTO? after a clock-out?).
- The N4 scheduled auto-assign trigger (Batch C's logged follow-on) — unchanged.
- `getTeammateStatus` still reads the FULL live Timesheet per call (pre-existing; the Clock page's heaviest read) — the presence `getAll` adds nothing to that, noted only.
- A `clock-teammate-*` scenario is not needed (the chip rides the four Clock scenarios), but a compact crop of the card would make a future chip regression visible without a walk — optional.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md INV-24 + S14: DONE in-batch.
- CLAUDE.md Time Clock KDD (the "Clock view: hero + shift-strip + ledger" / Dashboard entries mention "Today's Punches and teammate status render below" — note the presence chip + the summary tail + the head classes) and the Common Gotcha family: a new entry for the two MEASURED layout lessons (a nowrap inline pill inside a `1fr` grid propagates min-content — measure, don't eyeball; a shared `> span:first-child` rule at (0,2,1) beats a plain class fix at (0,2,0)).
- CLAUDE.md Operator State Checklist: a "Batch D (2026-09-10) adds NO operator state" entry (the CacheService key family; behaviour to expect — the chip, one gesture RPC per 10 min per window, the v1 limit; post-deploy `runAllTests()` expects 310).
- CLAUDE.md Cycle Workflow Config running totals: pure 784 → 785, DOM 112 → 113, editor 309 → 310 (visual matrix unchanged at 102); the D-N10 pin + DOM test described.
- CLAUDE.md S101 (Time Clock surface): the Team Right Now card's chip is on camera in the four Clock scenarios.
- README (if it describes Team Right Now): the chip.
- STATE.md: DONE with this block.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
