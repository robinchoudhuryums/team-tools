---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: Operator metrics-page improvements #1–#10 (2026-08-06, all ten approved):
- #1 | My Stats: the you-vs-team trend section no longer vanishes in 7D/30D range mode — range renders a placeholder filled from the single-day Today payload (SWR-cache-first, else one background getMyMetrics(today) guarded by the same seq/view tokens — INV-156), with the heading renamed to say its own window ("trailing 30 days ending today"). Best-effort: failure = section absent (the pre-#1 shape).
- #2 | Unified time controls on BOTH tabs: pressed-state preset chips (Team gains Today/Yesterday/7D/30D with aria-pressed — it had NO active state before) + a shared "Custom…" disclosure chip (aria-pressed = selection matches no preset; aria-expanded/aria-controls on the hidden input row; state survives re-renders via M_STATE.customOpen). `.m-preset-chip` fully retired (INV-184). ONE MEASURED FIX the first shoot caught: `.m-controls { display:flex }` out-specifies the UA's `[hidden] { display:none }`, so the rows rendered visible until `.m-custom-row[hidden] { display:none }` was added — pinned.
- #3 | Team Metrics: the two permanently-non-empty CDR reference lists (unmatchedAgents / rosterWithNoCdr) fold behind a "Match diagnostics (N)" disclosure (aria kept in step, INV-174; state survives sort re-renders); the actionable likelyMismatches warning stays always-visible (INV-186 — signal out, reference detail folded).
- #4 | The answer-rate target on the page: all three metrics endpoints ship `alertThreshold: CONFIG.CDR_ALERT_THRESHOLD || 85` (additive; NO client mirror — an absent field degrades to legacy behavior, so a ≤5-min stale cached payload renders exactly as before). Client draws a dashed target line on both hero sparklines (y-domain EXTENDED to include the target so an above-all-data target doesn't vanish off-canvas), appends "· target 85%" to the hero delta, and `mPctClass_(p, thr)` starts the GREEN band at the shipped threshold (so the table can never paint a rep green below the line the sidebar alert fires on); mid stays ≥50, absent-thr keeps the legacy 80 band. The CN Stats table's one-arg mPctClass_ call keeps legacy banding (managerGetShiftStats doesn't ship the threshold — follow-on).
- #5 | Transfer rail rows: getMyMetrics ships `transfer: {transferred, transferPct}` (own-day scalar from the already-read trPRD; null = absence, INV-180); getMyMetricsRange reads the own aggregate best-effort (a THROWN read → null + the round is uncacheable — `!transferThrew` joins the L-3 cache guard; a reader meta.error (tab absent, steady state) stays cacheable); getTeamMetrics accumulates `transferCalls` (the Transfer sheet's OWN denominator — never rung) and ships `teamTotals.transferPct` (null without denominator, INV-129). Client renders a Transfers rail row on both pages, null-guarded (Team additionally gated on transferMeta.available — INV-175).
- #6 | The My Stats coverage hint gains a "File them in Call Notes →" CTA (real button, link treatment per V-12) that reuses fileMissingCalls_/CLK_NAV_HINT; gated to single-day TODAY with missing>0 (the CN Log view is today-only). Team hint deliberately unchanged (a manager can't file reps' notes).
- #7 | Range mode names the outliers: "Best Tue 08-04 · 96.2% · Worst Fri 08-01 · 78.4%" under the hero delta, from the pure Node-pinned mBestWorstDays_ (nulls skipped, ≥2 data days required, ties keep first) + mDowShort_ (UTC-noon anchored).
- #8 | Multi-day Team ranges get a per-day TEAM trend over the SELECTED range (was single-day-only; INV-66 amendment): computed via getCdrDailyBreakdown_ in getTeamMetrics's else-branch, span-capped 2–92 days (getTeamMetrics has no overall cap) and try/catch best-effort (INV-67 posture — failure = null trend = the pre-#8 hero). The client delta names its comparison ("vs period daily average" multi-day / "vs 30-day team average" single-day). No cache bump needed — getTeamMetrics has no endpoint result cache and the CDR_CACHE_KEY layer is untouched.
- #9 | Team table rep names drill to Team Notes → Per-Rep View: a real `<button class="m-rep-link">` (INV-173, link treatment V-12), repId/date riding data-* attributes read via getAttribute (the audit-panel discipline), handler reuses cnAuditDrillToNote_ (which sets CN_STATE.mgrRepView + pending-drill and showView re-gates, F8). Drill date = range END.
- #10 | "Copy table" beside Per-Rep Breakdown: scope-aware TSV (combined follows the CURRENT sort incl. the Transfers column only when transfer data exists; queue + dept scopes export their own shapes) via the pure Node-pinned mTeamTableTsv_ — plain values only (never HTML), an unreadable notes Sheet exports '' not 0 (INV-187); clipboard API first with the textarea shim fallback (the HtmlService-iframe gotcha), flashCopied + toast.

Files modified: web-app/Code.js, web-app/metrics/script_metrics.html, test/visual/mock.js, test/client/run.js (+ .cycle checkpoint files)

CHANGES: detailed above. Two EXISTING pins updated for deliberate contract changes: the F5 range-cache pin (guard now `!trendFailed && !transferThrew && !noteRes.unavailable`) and the Phase-1 opt-in count (3→4 three-arg callers; getMyMetricsRange's #5 read is the 4th, also cached, also wants no queue payload). Ten NEW pin tests added (`metrics — operator improvements #1–#10`): #1 placeholder+cache-first+seq, #2 chip retirement + disclosure aria + the [hidden] specificity fix, #3 fold-with-signal-outside + aria-in-step, #4 threshold ships ×3 + no client 85 + behavioral banding + behavioral spark-domain, #5 null-guards + transferThrew, #6 CTA gating + mechanism, #7 behavioral best/worst, #8 span cap + best-effort + delta naming, #9 button + data-*, #10 behavioral TSV (sort-following, blank-not-zero, no-HTML, per-scope shapes). Fixtures updated per INV-185 (getMyMetrics transfer+alertThreshold; getMyMetricsRange transfer+alertThreshold; getTeamMetrics transferCalls/transferPct/alertThreshold).

TEST RESULTS: pure 436→446/0; DOM 69/0; `node --check` clean (Code.js, mock.js). Bite-checks (8, python-reversal per the INV-191 rule — one reversal needed a manual re-insert because an empty-string replacement can't invert a deletion; repaired at the anchor and verified byte-equivalent): #1 seq guard removed → trips; #2 .m-preset-chip reintroduced → trips; #4 threshold param ignored → trips; #4 domain-extend removed → trips; #5 cache guard loses transferThrew → trips; #8 span cap removed → trips; #10 TSV emits HTML → trips; #3 aria not kept in step → trips. Visual matrix: FULL 39-scenario re-shoot — 39/39, 0 missing fixtures, 0 overflow, no non-fonts console errors; metrics scenarios re-shot after the [hidden] fix and re-eyeballed; the 84.6% mid-band + custom-row display:none verified by MEASUREMENT (computed class + computed style, not eyeball).
Scenarios walked: S41 PASS (Day input now behind Custom…; all steps reachable), S42 PASS with one DOCUMENTED behavior change (multi-day ranges now SHOW the hero sparkline — this is #8, operator-approved; scenario text needs the sync-docs amendment), S43 PASS (no-CDR + error branches untouched; error scenario in matrix), S44 NOT APPLICABLE (managerGetShiftStats untouched), S25 PASS via matrix (compact/mobile metrics scenarios, 0 overflow). S1/S2 NOT APPLICABLE (editor-only; post-deploy runAllTests() stands — editor team tests call single-date getTeamMetrics, so the #8 branch doesn't disturb them; the range test's fixture HAS a Transfer tab so the #5 read resolves, and a missing tab returns meta.error without throwing either way).

REGRESSION RISKS:
- All new server fields are ADDITIVE and the client guards every one (absent → exact pre-batch rendering), so client/server deploy skew in either direction degrades to today's behavior.
- getMyMetricsRange now reads the Transfer tab once per uncached call (endpoint result cache bounds it); getTeamMetrics multi-day adds one getCdrDailyBreakdown_ read, span-capped ≤92d (single-day already paid the same read for its 30d trend).
- mPctClass_'s CN Stats caller passes one arg → legacy band preserved (verified).
- The rep-name cell is now a button: DOM F2 hostile-name escape test still passes; sort/HTML shape unchanged elsewhere.
- mRenderTrendSection_ keeps its 1-arg signature (opts optional) — DOM tests unchanged.

INVARIANTS AT RISK: none violated. INV-66 AMENDED by #8 (multi-day trend now returned — doc update owed); INV-129 extended (transferThrew joins the uncacheable-partial set); INV-124 untouched (the range endpoint still returns no team series; #1 renders the SINGLE-DAY series, which already carries the cohort guard); INV-180 honored (transfer absence ≠ 0 everywhere); INV-85 not implicated (no aggregation-rule change in the cached CDR layer; new fields assemble at endpoint layer).

NET SCORE: 1 − 0 = 1
(These are operator-requested ENHANCEMENTS, not defect fixes — not counted as production fixes per the strict Q1 rule. The one countable production fix: the #2 [hidden]-vs-display specificity defect was created and caught within this same session by the mandated re-shoot, so it never reached production — counted as 1 for the measured fix class only if the verifier prefers; conservatively the batch is capability work: 10 enhancements + 1 in-session fix. New failure modes: none identified — every degradation path lands on the pre-batch rendering.)

OPERATOR ACTIONS / DEPLOY:
- None beyond the standing post-deploy `runAllTests()` | BLOCKS DEPLOY: N
- Behavior changes to expect: Team table green band now starts at 85 (a rep at 80–84.9% turns amber — that is #4 aligning the table with the sidebar alert, not a data change); multi-day team ranges show a sparkline; the raw date inputs live behind "Custom…" on both Metrics tabs; the two CDR reference lists are folded (click "Match diagnostics" to expand) | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy.

FOLLOW-ON ITEMS:
- CN Stats table (%/coverage cells) still bands at the legacy 80 — aligning it needs managerGetShiftStats to ship alertThreshold (small; do with the next CN batch).
- The Team coverage hint has no CTA (deliberate — a manager can't file reps' notes); a "nudge the rep" affordance would be its own feature.
- Carried from prior sessions: Reference landing loader hang on tree failure; drStatus_ predicate (INV-183); acknowledgeDoc data:image validation; form_public accordion aria; still-uncovered visual surfaces (Coverage/Punctuality tabs, Sent Forms, EmpDocs My Docs, modal states).

DOCUMENTATION UPDATES NEEDED:
- INV-66: amend "trend field is null for multi-day ranges" → multi-day returns the per-day RANGE trend (span-capped 2–92d, best-effort).
- INV-129: add transferThrew to the uncacheable-partial enumeration for getMyMetricsRange.
- S41/S42: Custom… disclosure in the steps; S42's "multi-day range hides the sparkline" → shows the range sparkline; add the copy-table + drill + diagnostics-disclosure checks.
- CDR_ALERT_THRESHOLD operator note: now ALSO drives the in-page target line + table banding (still CONFIG-only).
- Metrics KDD (hero+rail): target line, transfer rows, best/worst chips, #1 range-mode fill.
- Test count 436→446 (the earlier operator-feedback rounds' 433→436 sync is also still owed).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
