---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F-20 — `sendDailyMissedPunchAlerts` and `runDailyExportCheck` write no audit row and had no heartbeat (a dead trigger was invisible; for the export check that is a silently missing payroll export), `DIGEST_STALE_HOURS` had no key for `sendAutomationHealthDigest`, and a `computeAutomationHealth_` throw inside the digest reached nobody. F-19 — a multi-month catch-up credit wrote ONE ledger row keyed `2026-06,2026-07`, and the reconcile pass reported that key as unreconcilable every day for months once one member left the 3-month window. F-46 — the accrual job's early returns never rewrote `PTO_ACCRUAL_RECONCILE` nor cleared its error, so a stale shortfall kept alarming after PTO tracking was switched off. F-21 — five trigger-gate tests append `_TEST_MGR_EMAIL` to `MANAGER_EMAILS` and a killed run left it; cleanup stripped ADMIN_EMAILS only. F-22 — the DeptRequests tests deleted their probe rows by POSITION on a live tab and neither DeptRequests nor ClientErrors had a cleanup backstop. F-49 — `getTeamCalendar` read the time-off tab by name (a fresh deployment rendered no calendar until the first request).
Files modified: web-app/10_core.js, web-app/20_timeclock.js, web-app/cn/script_callnotes.html, web-app/Tests.js, test/client/run.js, test/client/server-split-manifest.json, test/visual/mock.js, CLAUDE.md (generated block), .cycle/STATE.md

Estimate: M (~7 h) — recorded 2026-09-18 in STATE.md BEFORE the first edit
Actual: ~2 h

CHANGES:
F-20 | web-app/10_core.js, web-app/20_timeclock.js, web-app/cn/script_callnotes.html, test/visual/mock.js | DESIGN NOTE — the plan said "two JOB_CHECKS rows"; a JOB_CHECKS row needs an audit action to compare against (the seams-18 F4 coupling pin), and neither job writes one. The mechanism built for row-less jobs is the digest HEARTBEAT, so that is what they use: `DIGEST_STALE_HOURS` gains `missedPunch` / `exportCheck` / `automationHealth` (26h); `sendDailyMissedPunchAlerts` stamps `missedPunch` once the read succeeded and BEFORE its no-work early return (a quiet morning is a live trigger), clears `MissedPunchAlerts` and stamps it on failure; `runDailyExportCheck` stamps `exportCheck` at the end of a clean run, clears / stamps `DailyExportCheck`; `sendAutomationHealthDigest` stamps `AutomationHealthDigest` when the report throws (and in its outer catch), and heartbeats `automationHealth` + clears only once a report was computed — a dead computation reads stale AND failed. `automationProblems_` now carries a stamped failure under any key the JOB_CHECKS table does not know (once; tabled keys stay with the table). Client: both `CN_DIGEST_LABELS_` and the detail panel's `DIGEST_LABELS` label the three keys with their cadence; `cnHealthFindings_` reads the stamp's real field (`e.message` — it read `e.error`, so EVERY stamped failure rendered "unknown error" on the Admin card; found while wiring F-20, fixed as part of it). Both Automation Health fixtures carry the three heartbeats.
F-19 | web-app/20_timeclock.js | `planPtoAccrualRun_` Pass 3 keeps each month's slice (`p.byMonth`) and earns their SUM through new pure `accrualEarnedByMonth_` (each month rounded exactly as its ledger row records it — `days(5 h) + days(5 h)` = 0.04 where the once-rounded `days(10 h)` = 0.05 — so the preview promises what the rows add up to). The credit loop iterates new pure `accrualMonthRows_(p)` — one plan-shaped entry per month — through the UNCHANGED `adjustLeaveBalance_` / `accrualCreditNote_` / `accrualZeroNote_`, so every `PtoAccrualCredit` row's `months=` is a single month and the ledger's keys are per-month; a rep with no Timesheet rows gets one zero row per owed month, each naming the reason; a rep gated away mid-run still skips their stamp. `planAccrualReconcile_`'s `skipped` line names a legacy multi-month key as "a multi-month row written before per-month ledger rows, 2026-09-18 — it leaves the ledger read once the window moves past its write date" (the only remaining source of that daily line; none is expected in prod — the accrual shipped 2026-08-19 and its first credits were single-month).
F-46 | web-app/20_timeclock.js | new `accrualNothingToDo_(window, reason)` rewrites `PTO_ACCRUAL_RECONCILE` to an empty pass (`reason: 'PTO tracking disabled'` with window `[]`, or `'no accruing reps'` with the window it would have reconciled) and clears `PtoAccrualCredit`; both early returns call it (the tracking-off one before the lock).
F-21 | web-app/Tests.js | `cleanupTestData` strips every `@example.invalid` address from `MANAGER_EMAILS` through `_testAdminEmailsSplit_` (the one predicate), keeps a real list verbatim, deletes an all-test list; logged like the ADMIN strip.
F-22 | web-app/Tests.js | the three DeptRequests tests (`test_deptReq_resendDedupLookup`, the Incoming/resolve test, the detail test) and `test_recordClientError_authBoundsAndAppend` tidy BY KEY through `_cleanupRowsByPrefix` (`TEST_DR_` on `DR.REQ_ID`; `TEST_` on the ClientErrors EmpId column) — `deleteRows(before + 1, after - before)` is gone; `cleanupTestData` backstops both tabs by key through `getSheetByName` on the DeptRequests store (`getDeptRequestsSS_()`, so a `DEPT_REQUESTS_SS_ID` deployment is swept too) and the ADP sheet's ClientErrors tab, never provisioning either.
F-49 | web-app/20_timeclock.js | `getTeamCalendar` reads the time-off tab through `getOrCreateTimeOffSheet_()`.
Pins | test/client/run.js | F-20 (the three windows; heartbeat placement relative to the early return / the period gates / `if (!report) return`; both digest stamps; the digest DRIVEN in a vm through a throwing and a clean report; `automationProblems_` driven with an untabled and a tabled stamp — each reported once; `cnHealthFindings_` shows the stamped message; both label tables; both fixtures), F-19 (`accrualEarnedByMonth_` sum vs the once-rounded total, the no-slices zero and the bad-rate null; `accrualMonthRows_` split with emp/plan carried and per-month uncounted days; the credit and zero notes round-trip to single-month keys; the loop, the planner and the legacy `why` structurally), F-46 (`creditMonthlyPtoAccruals` DRIVEN through both early returns — stamp reason + window, error cleared, lock released), F-21/F-22 (the strip form is the only MANAGER_EMAILS write; both sweeps by key on the right store; no provisioning; no positional delete survives; every probe row is `TEST_DR_`-keyed), F-49 (structural + a fresh-deployment run appended to the calendar's behavioural pin: the by-name tab is null, the provisioner serves an empty tab, the calendar renders). Five existing pins repointed (the credit's mutator regex, the planner's earned line, the SA-1 stale-window regex, the calendar vm context, the F5 coupling now reads both label tables).
Tooling | test/client/server-split-manifest.json, CLAUDE.md | manifest regenerated for `accrualEarnedByMonth_`, `accrualMonthRows_`, `accrualNothingToDo_` (1289 declarations); counts block pure 878 → 883.

TEST RESULTS: passed. Pure harness 883/883 (was 878 — five new pins), DOM harness 126/126 (unchanged), `lint-server` clean (16 files), `split-manifest --check` current, `counts --check` green. Editor suite NOT RUN HERE (editor-only); registrations unchanged at 337 — four tests changed their tidy-up, none its assertions.
Bite-checks — TWELVE mutations, TWELVE bite (committed file mutated, the named pin went red, file restored): B1 the missed-punch heartbeat moved AFTER the no-work early return → F-20 · B2 the digest heartbeats a dead report → F-20 · B3 the untabled-stamp sweep inverted → F-20 · B4 the client reads `e.error` again → F-20 · B5 the earned total rounded once again → F-19 · B6 one row for the whole month-set again → F-19 · B7 the tracking-off path skips the stamp → F-46 · B8 the no-reps path skips the stamp → F-46 · B9 the MANAGER_EMAILS strip deletes a real list → F-21/F-22 · B10 cleanup provisions the DeptRequests tab → F-21/F-22 · B11+B12 `getTeamCalendar` reads by name again → the calendar behavioural pin AND the F-49 structural pin (a first, un-anchored mutation landed on a DIFFERENT function with the same two-line shape and did not bite — the anchored one did; noted under g116).
Regression Scenarios (Test Command is `manual` — no editor or browser here):
- S1 / S2 | Test Suite | NOT RUN HERE — editor-only; no registration change. The suite's cleanup now also strips MANAGER_EMAILS residue and sweeps DeptRequests + ClientErrors; a next full run on the dev instance should log those lines (or nothing, when clean).
- S9 Automated daily missed-punch alert | Server | NOT RUN HERE — trigger. Additive: the job now heartbeats `missedPunch` and stamps `MissedPunchAlerts` on failure; the employee/manager emails are unchanged.
- S8 ADP-format export | Server | NOT APPLICABLE — the manual range export is untouched; only the daily CHECK gained a heartbeat.
- S97 Admin → System findings-first | Client (Call Notes) | PASS by fixture (pinned): the all-clear fixture still reaches "Nothing needs attention" with three more heartbeats listed fresh; a stamped failure now shows its MESSAGE in the finding detail.
- S106 Diagnostics retention | Server, Client | NOT APPLICABLE — the purge is untouched; the ClientErrors tab only gained a test-row sweep.
- S107 PTO accrual dry run | Server | NOT RUN HERE — editor. The preview's `wouldCreditDays` is now the per-month SUM; for a single owed month it is identical (the common case since 2026-08-19). Walk it once with a rep whose column R is two months behind: the preview and the job must agree, and TWO `PtoAccrualCredit` rows land, one per month.
- S108 PTO accrual reconciliation | Server | NOT RUN HERE — editor. Unchanged for single-month keys (the editor test's 2026-08 replay is single-month); a legacy multi-month key's skip line now names itself.
- S91 Team punches calendar · S98 Manage surface | Server, Client (Time Clock) | NOT RUN HERE — browser. Unchanged on a deployment that has the tab; a fresh one now renders (pinned).
- S15 Per-employee PTO toggle | Server, Client | NOT APPLICABLE — the per-row gate is untouched.

REGRESSION RISKS:
- F-19 changes a NUMBER: a multi-month catch-up credit is now the sum of per-month roundings, which can differ from the once-rounded total by up to 0.01 day per extra month. Single-month credits (every credit since the accrual shipped) are bit-identical. The preview shares the sum, so it cannot promise a different amount than the job lands.
- F-19 writes more AuditLog rows on a catch-up (one per month instead of one per set) — `readAccrualLedger_`'s 5000-row tail is unaffected at this volume.
- F-46 stamps `PTO_ACCRUAL_RECONCILE` on EVERY run that returns early, including a manual editor call with tracking off — the stamp is auto-managed and says why (`reason`); `automationProblems_` raises nothing from an empty pass.
- F-20's sweep in `automationProblems_` reports EVERY untabled stamp; a stamp left behind by a job that no longer exists would nag until the property is cleared (the documented remedy for `AUTOMATION_LAST_ERRORS`). Today there is no such key.
- F-22's cleanup reads the whole DeptRequests tab (`getDataRange`) once per full run — the same cost every other sweep in `cleanupTestData` already pays; it carries `PatientTrx`, which is only read, never returned.
- The client label tables must keep every `DIGEST_STALE_HOURS` key — the F5 coupling pin now reads BOTH tables (it silently read only the detail panel's before).

INVARIANTS AT RISK: none violated. INV-01 (the credit's writes stay under the same lock; `accrualNothingToDo_` writes a Script Property, not a sheet), INV-27 (every per-month credit still goes through `adjustLeaveBalance_`), INV-85 (no cached shape changed), INV-179 (the reported digest set is still derived from `DIGEST_STALE_HOURS`), INV-183 (`_testAdminEmailsSplit_` is the one predicate on both properties now), INV-186 (an all-clear deployment raises nothing new: heartbeats stamp on every clean run, including quiet mornings), INV-201 (the reconcile stamp's degrade shrink is unchanged), INV-210 (the ledger fail-closed rule — per-month rows are read by the same reader; a legacy set is still skipped, never valued). INV-21 (`TEST_` is the cleanup key) is now HELD on two more tabs.

NET SCORE: 3 − 0 = 3
  a) Would this have fired in production this month? F-20 YES (the three jobs run daily; a stale `AUTOMATION_LAST_ERRORS` entry from any job rendered "unknown error" on the Admin card every time it was read — that half is certain; the liveness half fires on the first dead trigger). F-19 NO-as-observed (no multi-month catch-up row exists yet — defensive, but the daily-noise class is real the first time a re-enabled rep is credited). F-46 YES for the class (a stamped shortfall persists across a tracking-off toggle; whether the operator toggled it this month is unverified — counted because the stale stamp is wrong on every read after the toggle). F-21 YES (the suite has been killed by the 6-minute limit mid-run before — that is why Part A/B exist — and each such kill left a test manager in the gate). F-22 NO-as-observed (needs a real request during the seconds a test holds its probe rows — defensive). F-49 NO (every existing deployment has the tab — defensive; fires only on a fresh instance, which the dev-instance stand-up will be).
  b) New failure mode? NO for all six. The one behaviour a reader should know: a catch-up credit's total is a per-month sum.

OPERATOR ACTIONS / DEPLOY:
- `clasp push -f` + cut a New version. | BLOCKS DEPLOY: Y
- `runSmokeTests` on prod — registrations still 337; read the count off the run. | BLOCKS DEPLOY: N
- The morning after the deploy: Manage → Admin → System → Automation detail lists three new heartbeats (missed-punch 8am, export check 12pm, failure digest 9am); until each job has run once they read "no heartbeat recorded yet", NOT stale. | BLOCKS DEPLOY: N
- If `AUTOMATION_LAST_ERRORS` holds a stale entry, its message is now visible on the card — clear the property after fixing. | BLOCKS DEPLOY: N
- S107 with a two-months-behind rep, once, to see the per-month rows land. | BLOCKS DEPLOY: N
Deploy: Server — `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. Client (Call Notes views — Admin System) — same push, same New-version step. Test Suite — rides the same push.

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- The scan's F-20 wording asked for JOB_CHECKS rows; heartbeats were used because neither job writes an audit row and the coupling pin forbids a row without one. If the operator wants these two on the "Automation last seen" audit table as well, each needs a counts-only audit action (2 AuditLog rows/day).
- `scripts/bite.sh` accepted a mutation that changed a DIFFERENT function than the pin covers and reported NO BITE — a true statement about the wrong mutation. The helper cannot know which function a regex hit; a `--fn <name>` option that refuses a mutation outside that function's span would close the class (g116).
- A legacy multi-month ledger key still produces a daily `skipped` line until it ages out of the ledger read; if one ever exists, a one-off note rewrite (split the row's months with their hours) is the operator remedy — there is no tooling for it.
- `sendAutomationHealthDigest`'s own failure is stamped and shown on the Admin card, but by construction cannot be EMAILED (it is the emailer). The 10-minute manager badge (`getAutomationHealthBadge`) reads `automationProblems_` and so now carries it.

DOCUMENTATION UPDATES NEEDED:
- `docs/operator-state.md`: the `AUTOMATION_DIGEST_LAST_RUNS` entry (three new keys + cadences), the `AUTOMATION_LAST_ERRORS` line in CLAUDE.md's storage map (three new stamp keys; the message now renders), `PTO_ACCRUAL_RECONCILE` (`reason` field; rewritten on every path), the Daily automation triggers entry (heartbeats for the two daily jobs).
- `docs/design-decisions.md`: the accrual-ledger decision (per-month rows; the total is the per-month sum) and the Automation Health panel entry (untabled stamps reach the digest).
- `docs/gotchas.md` / CLAUDE.md index: g08/g24 family — a test that appends to a gate property or a live tab owes the cleanup backstop (TEST_ is the key on properties too); g116 — an un-anchored bite mutation can hit the wrong function.
- `.cycle/config.md`: INV-210 amended (per-month keys), a new invariant for "every daily trigger handler has a liveness signal — an audit row or a heartbeat — and stamps its own failure", S9 expected (heartbeat), S107 expected (per-month rows).
- `docs/modules.md` Time Clock (accrual per-month rows) and `docs/operator-log.md` (a Batch 4 entry); `docs/test-harness-log.md` (the five pins + the mis-anchored bite lesson).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
