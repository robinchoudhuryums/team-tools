# 2026-08-31 — Team punches calendar (operator: no manager view of team punch times for a given date)

---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- TCAL-1 | The manager cut the app lacked: every rep's punch times for an ARBITRARY date in one place. Live Status is this view pinned to TODAY (last punch only); Day Edit is one rep at a time and edit-framed; the Time/PTO calendar is self-service only. Operator's own framing: "no easy way to see punch times for all of my team members on a given date in one place."
- TCAL-2 | Placement DELIBERATED rather than assumed. The operator proposed a role-scoped toggle on the rep-facing Time/PTO page; argued against on two grounds and the operator agreed: (a) the registry reorg deliberately moved every team-facing surface OUT of rep tools into the Manage module, where the `managerOnly` tab machinery gates whole tabs instead of embedding role logic inside a rep page (more gate surface = more gate mistakes); (b) the Day Edit modal — the natural click-through target — already lives in `tc/script_manager.html`. Shape then chosen by the operator via AskUserQuestion: calendar + full-width table below (over a day-popover, which is a tight fit for a whole-team table).

Files modified: web-app/Code.js, web-app/tc/script_manager.html, web-app/Tests.js, test/client/run.js, test/visual/mock.js, CLAUDE.md, .cycle/STATE.md, .cycle/blocks/

CHANGES:
TCAL-1 | Code.js | `getTeamCalendar(month)` — manager-gated READ (bare `{error}`, the GATE-SHAPE rule), month-shape validated. ONE Timesheet + ONE TimeOffRequests read per month (the client's calendar AND day-table render from a single round trip, matching `buildCalendarForEmployee_`'s one-shot month model). Per-day per-rep derivation MIRRORS `buildTimesheetForEmployee_` exactly — last punch per type wins (the `findExistingPunch_`/`managerSaveDay` last-row-wins convention, so the table can never disagree with what a pencil click will edit), `calcHours_` null → INCOMPLETE never 0 (INV-176/187), ClockIn-without-ClockOut is in-progress on the REP's own today and incomplete on a past day. Roster inclusion via `empRosterEmail_` (INV-183); a garbage COMMENTS value is not a punch (the `getNextActions_` lesson); TO status trimmed+lowercased at the ONE read (the DR/TO.STATUS family); `punchCount` reports collapsed extras so multi-lunch/duplicate days are visible. Live-tab-only by design (the calendar/Punctuality posture) with `archiveNote` when the month predates the live tab's oldest row (INV-187). December also consults year+1 holidays (a Saturday Jan 1 observes on Dec 31).
TCAL-2 | tc/script_manager.html | The `#mgr-team-cal` card between Live Status and Pending Time Off, loaded with the other lazy cards. Month calendar in the Time/PTO `.cal-*` vocabulary (reps-punched corner badge, `+N` off-count, PTO dots, holiday star, warn tint when any rep's day is incomplete; selection is a distinct `--info` inset ring so today + selected read as two facts). Full-width day table through the shared `mtRenderTable_` (V-11): ClockIn / LunchOut / LunchIn / ClockOut / Hours, ADJ chips, warn-tinted incomplete rows, `+N` collapsed-extras marker, and ABSENT reps merged from `mgrData.liveStatus` as muted "no punches" rows — past/current weekdays only, deduped by id AND name, never for a rep already listed off. Pencil per row opens Day Edit prefilled to that rep + date (rendered only inside the adjust window; `openDayEditModal` gained an optional 3rd `prefillDate` honored only inside the picker's own [min, today] bounds — the `openAdjustModal` precedent, and the To-picker min + initial punch load follow the prefilled date). Per-month SWR: key-exact cache write BEFORE the seq check (INV-156), clean rounds only (INV-129), painted refresh failure keeps last-good via warn toast while a cold failure renders `errorStateHtml_` (C17-5). Dates derive from the manager's roster tz (`isoDateTz(empTz())`, the F6 discipline); future-month nav refused; cells are `role="button" tabindex="0"` with `aria-pressed` selection state (INV-173/174) and delegated listeners, never an inline `onclick` on a div.
PINS | test/client/run.js 688→690, Tests.js, test/visual/mock.js | (a) Behavioural: the REAL `getTeamCalendar` driven in a vm with the REAL EMP/ADP/TO enums + `PUNCH_LABELS_` + `empRosterEmail_`/`normalizeType_`/`calcHours_`/`timeToMins_` (only the environment stubbed) — gate + read shape, month validation, last-per-type wins, ADJ flagging, garbage-type exclusion, a CORRUPT TIME CELL reading incomplete-not-0, a padded `" Approved "` overlay counting while Denied/offboarded never do, offboarded rows out of rows AND rosterCount, archiveNote on a pre-live-tab month. (b) Client wiring: loader placement, the cache-write-before-seq ordering, clean-round gate, the C17-5 split, cell a11y, escaping, manager-tz derivation, future-nav refusal, the bounds-checked prefill, the honest no-punches merge, `mtRenderTable_` usage, and the fixture's rep-row keys DERIVED from the server's own `repRows.push` literal (INV-185) plus the fixture being a FUNCTION of its month argument (the F14 rule). (c) Tests.js omnibus gained the `getTeamCalendar` gate case IN PLACE — the F9 derived net fired on the new gated endpoint before the case existed, which is the net working.

TEST RESULTS: pure 690 passed / 0 failed; DOM 82 passed / 0 failed; visual matrix 54 scenarios re-shot (the card renders inside the existing manager scenarios via the new fixture). 6 mutations / 6 bites, commit-before-bite: (A) garbage-type skip dropped; (B) null hours coerced to 0; (C) `empRosterEmail_` guard dropped; (D) clean-round cache gate dropped; (E) cache write moved after the seq check; (F) fixture `punchCount` key dropped. TWO pins were strengthened when their first bite exposed them as weaker than the property: (B) was UNOBSERVABLE until a corrupt-time fixture row exercised the `calcHours_`-null path (mutate against the property, not its neighbourhood), and (F) only bit once the key was removed from EVERY fixture row rather than one. Regression Scenarios walked: S7 (manager edit-day diff-and-apply — the prefill is additive; an un-prefilled `openDayEditModal` call still defaults to today, asserted) PASS; S10 (cross-timezone live status — untouched) PASS; S46/S79 (Time/PTO + pay statement — untouched, the rep page gained nothing) NOT APPLICABLE.

REGRESSION RISKS: (1) `openDayEditModal` gained a parameter — the two existing call sites pass two args and keep today's behaviour (asserted). (2) The card adds one endpoint call per Manage Time enter; it is lazy like the other Manage cards and month-cached per session, so a re-enter is free. (3) The absent-rep merge reads `mgrData.liveStatus`, so on a cold Manage Time load where the dashboard failed, the merge simply yields no extra rows (punched reps still render) — degradation is quiet but never wrong.
INVARIANTS AT RISK: None violated — INV-02 (gate before any read), INV-129/156 (cache discipline), INV-173/174 (control semantics), INV-176/187 (null-not-zero, archiveNote), INV-183 (roster predicate), INV-185 (derived fixture shape), V-11 (shared table) all followed and asserted.

NET SCORE: 1 production capability (a manager surface that did not exist) − 0 new failure modes = +1. Strictly additive: no existing endpoint, view, or gate changed behaviour.

OPERATOR ACTIONS / DEPLOY:
- None new — no properties, triggers, or migrations | BLOCKS DEPLOY: N
- Post-deploy `runAllTests()` — still expects **296** (the omnibus case grew IN PLACE) | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy (ships with the whole undeployed PR set).

FOLLOW-ON ITEMS:
- Reading THROUGH `TimesheetArchive` was deliberately not built (the calendar/Punctuality posture); an archived-away month says so and points at the export. If archiving is enabled and managers browse old months often, that is the next increment.
- A per-day CSV/copy of the team table would be cheap on top of the same payload if the operator wants it for payroll conversations.

DOCUMENTATION UPDATES NEEDED: None — applied in this session (CLAUDE.md: the manager-gate list gains `getTeamCalendar`, a new Team-punches-calendar Key Design Decision, the Day Edit KDD's prefill paragraph, a 2026-08-31 operator entry, narrative 688→690; STATE.md NEWEST #10 + the test-count line).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
