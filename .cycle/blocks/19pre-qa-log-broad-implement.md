---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- QA Log | A fourth QA tab, **Log** — the QA agent's "Notes" equivalent for daily progress tracking: one entry per recording audited (= the latest scorecard per (recording, reviewer), a READ over `QaScorecards` — no second store), managers see every reviewer's log, a non-manager is scoped to self.
- Typed rubric | Scorecard criteria gain `type` (scale 1–5 default / Yes-No check / dropdown choice with 2–12 options), defined in the Admin editor's new Type column; non-scale answers are stored as NON-NUMERIC strings so every type-blind fold (stats, coverage, calibration, the verbatim mock copies) is unchanged.
- Manual fallback | A recording-less audit (`qaCreateManualRecording`) mints a synthetic `manual-<uuid>` QaRecordings row so coverage/stats/log treat it uniformly; the Drive boundary refuses it by construction and the client skips the player.
Files modified: web-app/Code.js, web-app/qa/script_qa.html, web-app/script_core.html, web-app/cn/script_callnotes.html, web-app/Tests.js, test/visual/mock.js, test/visual/shoot.mjs, test/client/run.js, test/client/dom/runDom.js, CLAUDE.md, .cycle/STATE.md

CHANGES:
QA Log | Code.js | `getQaLog({reviewer, from, to})` (canSeeQa_, bare `{error}` read gate before the store; span ≤92, default trailing 30 in the manager tz; a non-manager forced to self, a manager `'*'` = all; bounded reads of recordings/scorecards/comments; payload capped with the pre-slice `total`; `pending` = the caller's open assignments, `agentOptions` from the roster via `empRosterEmail_`, `reviewers` for the picker; `notConfigured` when `QA_SS_ID` is unset) + the pure `qaLogEntries_` (latest cards → entries, inclusive day window, unindexed recordings KEPT with blank fields, `avg` null-never-0, per-(recording, reviewer) active comment counts, newest-first) + `qaYmdMs_`.
Typed rubric | Code.js | `QA_CRITERION_TYPES` + the choice-option constants; `qaCriteriaSanitize_` keeps the canonical shape (`type` only when not scale, `options` only for choice; a choice with <2 usable options is DROPPED, never demoted); `qaCritType_`, `qaChoiceOptionsSanitize_`, `qaOptionIsNumeric_`, `qaRatingNormalize_` (refuses by name); `saveQaScorecardCriteria` validates type/options strictly (unknown type, bare-number option, 2–12 options, duplicate option); `qaSaveScorecard` routes every rating through the normalizer.
Manual fallback | Code.js | `QA_MANUAL_ID_PREFIX`, `qaIsManualId_`, `qaCreateManualRecording` (QA-gated, locked, label required + capped, full-width in_review row assigned to the caller, id-only `QaManualRecording` audit); `manual` flag on `getQaQueue` items + `getMyQaReviews` rows.
Client | qa/script_qa.html | the Log section (`enterQaLogView`, `qaLogLoad_` seq+view guarded with `errorStateHtml_` on both failure shapes, the shared `mtDateRange_` control with 7d/30d/QTR, reviewer select, refetch-free search, derived summary strip, day groups, cards through the ONE chip builder, `qaLogOpen_` parking `QA_OPEN_HINT`, the named "Log an audit" dialog via `ensureOverlay` with the manual entry LAST); typed scorecard form (Yes/No pair with aria-pressed, a named `<select>`, the unselect rule extended to the blank option); `qaRatingChipsHtml_` (yes/no tones, choice untoned); the detail skips `qaLoadAudio_` on a manual recording; My Reviews pills a manual review instead of Play; Stats columns filtered to scale criteria; CSS incl. the 720px stack.
Client | script_core.html | `qaLog` registry entry under the same `managerOnly + also:'canSeeQa'` gate.
Admin | cn/script_callnotes.html | the criteria editor's Type select + options field (shown only for choice); save collects `{key, label, type, options[]}`.
Tests | Tests.js | the QA gate case gained `getQaLog` + `qaCreateManualRecording` IN PLACE (17 endpoints).
Fixtures | test/visual/mock.js, shoot.mjs | typed criteria in the QA fixtures; `getQaLog` fixture (4 entries incl. a manual + an unindexed one) + its empty twin; `manual: false` on the queue/My Reviews rows; five `qa-log-*` scenarios (wide, mobile, empty, error, dialog).
Pins | test/client/run.js, dom/runDom.js | QA-24 (types + normalizer + strict save behavioural, `qaCardStats_` type-blind, the editor), QA-25 (`qaLogEntries_` behavioural), QA-26 (endpoint contracts, id-only audit, no manual branch in the audio boundary), QA-27 (client wiring + scenarios + fixture keys DERIVED from the push literal and the base block), QA-LOG-DOM (typed form + Log list + hint consumption + string answers in the save); QA-7/9/10/14/15/23 repointed for the changed contracts.

TEST RESULTS: pure 762/0, DOM 107/0; 11 mutations / 11 bites (one first bite was a no-op anchor — re-aimed at the real line, then bit). The vm-realm `deepStrictEqual` prototype trap fired three times on first write (two pure, one jsdom) — each corrected to a JSON/join compare. Visual: `qa-log-*` ×5, `qa-detail-light-wide`, `admin-config-light-wide` — 0 overflow, no missing fixtures, eyeballed (the dialog's h3/field spacing was widened after the first shot). Regression Scenarios walked: S90 PASS (four tabs now), S100 PASS (unchanged surfaces), S103 NEW (the Log), S51/S54 NOT APPLICABLE (Admin card markup grew a column; save contract asserted by QA-15/QA-24). Editor suite unchanged at 308 (the gate case grew in place).
REGRESSION RISKS: `qaSaveScorecard` now REFUSES a fractional or off-type rating it used to parseInt (a `'4.5'` used to save as 4) — deliberate, named. An existing criteria property without types reads unchanged. A choice criterion whose options get edited leaves older cards carrying the old option text (rendered verbatim, untoned) — the documented rename-orphans posture. The Log lists across ALL periods; coverage stays period-scoped (different questions).
INVARIANTS AT RISK: INV-196 (amended (k)); INV-169 (cap + total honoured); INV-183 (roster walk through the predicate); INV-32 (id-only audits — label never reaches the AuditLog); INV-01 (manual mint locked); none violated.
NET SCORE: 3 capabilities (Log, typed rubric, manual fallback) − 0 new failure modes = 0 production fixes (feature work, the 18pre convention); 0 defensive.

OPERATOR ACTIONS / DEPLOY:
- None to set up — no properties, triggers, migrations or scopes. Define the rubric types in Manage → Admin → Config → QA scorecard criteria when ready. | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → New version → Deploy (ships with the whole #176+ backlog); `runAllTests()` after — still **308**.

FOLLOW-ON ITEMS:
- The Log is scoped to (reviewer, range); a per-AGENT log (every audit of one agent across reviewers) is one filter away and was not asked for.
- Criteria types are not versioned — changing a criterion from scale to check leaves older numeric answers rendering as numbers under a Yes/No label; an edit-time warning (like the rename one) would name it.
- `.cycle/STATE.md` had a stale 2026-09-01 duplicate tail (every heading repeated) — removed this round; the editing rule already warns about exactly this.
- Standing: delete `SPLIT_REPAIR_2026_09_03` + the two wrappers once the operator confirms the manual punches; the Metrics 30-day calendar-day trends; a dashboard-cache warm trigger gated on the startup numbers.

DOCUMENTATION UPDATES NEEDED:
- Done in this round: CLAUDE.md (QA module bullet, INV-196 (k), S90 four tabs, S103, the 2026-09-04 operator entry, the count chain), .cycle/STATE.md.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
