---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: Batch S of the post-cycle-19 next-steps plan (`.cycle/blocks/19-next-steps-plan.md`) — S1 sharded editor suite (smoke + integration A/B, Part A/B entry points, runAllTests still one execution), S2 derived "Expected: N registrations" summary line, S3 `_suiteEnvCheck_()` logging every deployment setting the suite depends on, S4 three derived run.js pins, S5 the "smoke on prod, full on dev nightly" runbook sentence. Plus ONE defect the S4 pin found while being written: the runWeeklyDigests dispatcher gate test (#239) carried the SAME name as the sendCallNotesWeeklyDigests gate test and shadowed it — renamed.
Files modified: web-app/Tests.js, web-app/Code.js (one comment in runNightlySelfTest), test/client/run.js, CLAUDE.md, docs/deployment.md, README.md, .cycle/STATE.md
Estimate: M (~4 h) — recorded in STATE.md's `Estimates:` line BEFORE the first edit (the Batch P rule)
Actual: ~1.3 h

CHANGES:
S1 | web-app/Tests.js | `_runAllTests` is now three register functions — `_registerSmokeTests_` (118), `_registerIntegrationA_` (91: Time Clock punches / adjustments / PTO / adjust queue / managerSaveDay + perRepSchedule), `_registerIntegrationB_` (106: Call Notes, forms, metrics/CDR, KB, training + docs + coaching, intake, trigger gates, audit rows) — moved by a script that bucketed every registration by kind and position (order within each shard unchanged; the banners travelled with their groups). `runAllTests` runs all three in ONE execution; `runAllTestsPartA` (smoke + A) / `runAllTestsPartB` (B) go through `_runSuitePart_` (prod guard, own setup + cleanup in finally). Every test at a shard boundary was checked to clear its own state (`_clearTestState` / `_clearTestCallNotes`), so a Part stands alone. `runNightlySelfTest` keeps calling `runAllTests()` (the parts run sequentially in that one execution; one comment added in Code.js — the existing BCN/self-test pin regex is untouched).
S2 | web-app/Tests.js | `_TEST_COUNTING` counting mode in `_smokeTest`/`_integrationTest` (record the name, return before `_test`); `_expectedTestCount_(parts)` walks the requested shards and returns `{total, smoke, A, B, duplicates[]}`; `_printSummary(expected)` prints `Expected: N registrations (S smoke · A integration-A · B integration-B)`, a `⚠ Recorded X of N` line on a short run, and `⚠ Duplicate registration name(s)` by name. `runSingleTest` passes nothing and prints no Expected line. No entry point carries a typed-in count.
S3 | web-app/Tests.js | `_suiteEnvCheck_()` — first statement of `setupTestEnvironment` (try/caught, before the prod guard so the log explains a refusal): INSTANCE_LABEL / INSTANCE_IS_PROD + the verdict from the app's own `isDevInstance_`/`isProdInstance_`; ADMIN_EMAILS classified through `_testAdminEmailsSplit_` (unset = admin==manager / N real + narrowed note); MANAGER_EMAILS count + whether the test manager is listed; the four fixture properties; the three save/restore properties (CN_FEATURE_FLAGS, TIMESHEET_ARCHIVE_DAYS, WHATSNEW_KB_ID); FORMS/KB/HR_DOCS/QA store ids; the three TEST roster rows (present / onboarded / fixture tz). Reads only; never throws.
S4 | test/client/run.js | Three pins above the summary line, all DERIVED from Tests.js: (1) shards kind-pure + disjoint + union == every registration in the file + every registered function defined EXACTLY once + `_runAllTests`/Part A/Part B/`_runSuitePart_` shapes; (2) the counter driven behaviourally in a vm with every `test_*` stubbed to THROW (counting touches none), per-shard sums, a duplicate name reported, the Expected / short-run / duplicate lines printed, no Expected line without an expectation; (3) `_suiteEnvCheck_` names every `getProperty('…')` literal the rest of the file reads (+ the two instance markers), uses the app's predicates, never writes or throws, and runs first in setup. 8/8 mutations bite-checked against the COMMITTED tree (duplicate name re-added; a registration outside the shards; counting mode dropped; the short-run warning removed; a property dropped from the check; the check moved after the guard; the check writing a property; Part B registering half A).
FIX | web-app/Tests.js | `test_triggerGate_weeklyDigests_nonManagerThrows` was defined TWICE since #239 (line 4388 for `sendCallNotesWeeklyDigests`, line 4510 for the `runWeeklyDigests` dispatcher); the later declaration shadowed the earlier, both registrations ran the dispatcher body, and the digest's own gate went unverified while the count read 315. The dispatcher test is now `test_triggerGate_runWeeklyDigests_nonManagerThrows` (registration `triggerGate_runWeeklyDigests_nonManagerThrows`); 315 registrations, all distinct.
S5 | CLAUDE.md (Development section + Deploy Command Test Suite line + the INV-44 dispatcher-test names), docs/deployment.md, README.md | "smoke on prod, full on dev nightly; `runAllTests` on prod is the exception, not the routine" + the Part A/B split + the derived Expected line + the env block.

TEST RESULTS: `node --check` Code.js + Tests.js OK; pure harness 799 passed / 0 failed (796 + 3); DOM harness 113 / 0 (untouched partials, run for `npm test` parity); 8/8 bites. Test Command is `manual` — Regression Scenarios overlapping the modified subsystems: S1 (smoke suite) NOT APPLICABLE — editor-only; the S2 pin's vm drive of the REAL registrars + counter stands in for the count/duplicate half. S2 (full suite) NOT APPLICABLE — editor-only (and the runbook now says dev, not prod); the split's safety rests on every boundary test clearing its own state (verified by reading the nine candidates) and on the union pin. S30 (trigger gates reject non-managers) NOT APPLICABLE — editor-only; the renamed test restores the `sendCallNotesWeeklyDigests` case that had silently stopped running. Expected editor count after deploy: read it off the run's `Expected:` line (315 today).
REGRESSION RISKS: (a) A Part-B test that depended on rows a Part-A test wrote would fail when Part B runs alone — none found (every boundary candidate clears its own state; the same property already held for `runSingleTest`), and `runAllTests`/`runNightlySelfTest` are unchanged in effect. (b) The smoke registrations moved BEFORE the integration ones in the full run (they were interleaved) — they are pure and already ran in that order under `runSmokeTests`. (c) `_expectedTestCount_` runs the registrars in counting mode AFTER cleanup; it references every `test_*` identifier as a value, which the pin proves exist. (d) The restored `sendCallNotesWeeklyDigests` gate test has not executed since #239 — it passed before, and the gate is unchanged; a red on the next run there is real information, not this batch. (e) Docs: three run.js pins read CLAUDE.md (F7 INV-136 paragraph, VIS-COVER marker) — neither region was touched.
INVARIANTS AT RISK: INV-44 (the dispatcher gate test rename — the INV text updated; the derived TARGETS/gate-type nets do not reference test names); INV-21 (setup/cleanup symmetry — `_suiteEnvCheck_` is read-only and the two existing setup/cleanup pins stay green); INV-179 (honoured — every S4 assertion derives its set from the file); INV-188 (every scan comment-stripped). None violated.
NET SCORE: 1 − 0 = 1 (the shadowed gate test is a real test-integrity defect that fired on every run since #239 — counted as the one production fix; S1–S5 are operability/structural by design).

OPERATOR ACTIONS / DEPLOY:
- `clasp push -f` (Tests.js runs from the editor; no New version is needed for the suite itself, though the pending New version carrying #236–#240 still stands) | BLOCKS DEPLOY: N
- Batch 0f (the DEV instance + `installAutomationTriggers()` there) is what makes "full on dev nightly" TRUE rather than documented — S1's code needs nothing from it, but the runbook sentence does | BLOCKS DEPLOY: N
- On the next editor run read the `── Suite environment ──` block at the top of the log and the `Expected: N registrations` line at the bottom; `runAllTestsPartA` / `PartB` are there for a run that must be split | BLOCKS DEPLOY: N
Deploy: Test Suite — `cd web-app && clasp push -f` (editor-run; no New version required for Tests.js). Server — the one-line comment in Code.js rides the same push; no behaviour change.

FOLLOW-ON ITEMS:
- `runNightlySelfTest`'s doc comment still says "the FULL 281-test suite" — a hand-carried count; Batch C's derivation is the fix, not another hand edit.
- The CLAUDE.md "expect **315**" prose across the operator entries is untouched by design (Batch C rewrites every such sentence to point at the run's `Expected:` line — S2 is what makes that rewrite honest).
- The A/B split is by SECTION, not by measured runtime; if Part B (the fixture-heavy half) runs materially longer than Part A on dev, rebalance by moving a banner group — the pins hold either way.
- Still open from Batch P: the Admin → System findings read `e.error` where `stampAutomationError_` writes `message`, so a stamped automation failure renders "unknown error".
(Batch Q — the Script Property size guard — is NEXT in the plan and the only latent defect in the set.)

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md Key Design Decisions: a short entry for the sharded suite (three registrars, Part A/B, the derived Expected line, the env block, the S4 derivation) and the naming rule it enforces (a test function is defined exactly once).
- CLAUDE.md Common Gotchas: the shadowed-test-function class — a second `function test_X()` silently wins, both registrations run it, and the count stays right; the S1/S4 pin is the net.
- Regression Scenarios S1/S2: add the `Expected:` line + the env block to the Expected text; an S2 variant for Part A + Part B.
- Test Command narrative: the pure-harness total (799) — Batch C derives it; do not hand-carry.
- INV-44: updated in this batch (dispatcher test name); nothing further.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
