---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: (operator testing notes 2026-09-10, Batch B of A–D)
  - N2 | "Confirm Dept Requests avg/median exclude weekends, holidays and off-hours." CONFIRMED in code (BIZ-2 already pinned the item's `elapsedMin` as the business figure) — but the per-department fold was INLINE in `getDeptRequests`, so nothing could DRIVE it; it is the pure `drDeptStats_` now, driven over durations the real `bizMinutesLocal_` produced.
  - N3 | A request marked resolved by hand — the in-app "Mark resolved" button (Dept Requests) or the manual mark-resolve (Spanish Inbox) — counted as a TIMED reply on both surfaces, dragging avg/median and the SLA figures. It is a manual CLEAR now: still a resolved COUNT, out of every duration series, and the exclusion is REPORTED (INV-187) rather than absorbed.
Files modified: web-app/Code.js, web-app/Tests.js, web-app/metrics/script_deptrequests.html, web-app/metrics/script_metrics.html, test/client/run.js, test/visual/mock.js

CHANGES:
N2 | web-app/Code.js, test/client/run.js | The deptStats fold moved out of `getDeptRequests` into the pure `drDeptStats_(items, slaCfg)` (fed the SAME `all` items the endpoint ships, `result.deptStats = drDeptStats_(all, slaCfg)`; the inline `durations.push` is gone). Pin N2 builds a vm with `drSplitDepts_` + `getDeptRequestSla_` + the fold and drives it over `bizMinutesLocal_(Fri 16:00 → Mon 09:00) = 120` (wall clock 3900): Billing avg 90 / median 120 over the two email resolves, never the wall figure; a multi-dept send counted under EACH department; a department whose only resolve was manual reads null, never 0; plus the source wiring (fold fed from `all`, item `elapsedMin` = business figure or null).
N3-DR | web-app/Code.js, web-app/Tests.js, web-app/metrics/script_deptrequests.html, test/visual/mock.js, test/client/run.js | Store: a TRAILING `ResolvedVia` column (`DR.RESOLVED_VIA:12`, `DR_HEADERS` +1, `getOrCreateDeptRequestsSheet_` self-heals a short header — the INV-126/135 pattern), read ONLY through `drResolvedVia_(row)` (trimmed + lowercased; only `email`/`app` survive; blank/unknown = `''` "source not recorded" — the drStatus_/INV-183 one-reader shape, zero bracketed reads outside it). Writer: `markDeptRequestResolved_(token, byEmail, via)` validates the path against `DR_RESOLVED_VIA_VALUES`, writes the cell, and names it in the (PHI-free) audit note; `serveResolvePage_` passes `'email'` (the receiver's link — the ONLY timed reply), `resolveDeptRequest` passes `'app'`. Item: `resolvedVia` ships beside `timed = !isResolved || resolvedVia === 'email'`; a non-email resolve nulls BOTH `elapsedMin` and `elapsedWallMin` (no unit leaks a duration), so every consumer — the fold, the client median, the card, the SLA bands — excludes it by the existing null guard. deptStats rows gain `manualResolved` / `untrackedResolved` (legacy blank) / `timed` (the sample size). Client: the resolved card reads "marked in app" (or plain "resolved" for a legacy row) and shows a duration ONLY for an email resolve; the KPI median filters `resolvedVia !== 'app'` explicitly (the optimistic patch leaves the OPEN minutes-since-created figure on the row, which must not enter the median) and its sub-line names the excluded count; `drApplyResolved_` stamps `resolvedVia = 'app'` (exactly what the next payload says — `resolvedBy`/`elapsedMin` still untouched, the in-place pin holds); the manager table gains a "Not timed" column (`drStatsNotTimedCell_`: "5 · 3 in app · 2 legacy"; an older server with no counts renders an em dash, never 0) and a one-line note saying Avg/Median count email-link resolves only. Fixture: an `'app'` row with null minutes, `'email'` on the timed row, the three counts on deptStats (INV-185). Tests.js M-11 (`test_deptRequest_resolveLinkIdempotent`) appends a 13-column row, passes `'email'` on every call and asserts the ResolvedVia cell through the reader.
N3-SP | web-app/Code.js, web-app/metrics/script_metrics.html, test/visual/mock.js, test/client/run.js | `getSpanishInboxStats`: a manual mark-resolve increments `resolvedCount` AND a new `manualCount`, and reaches NEITHER duration series (`if (wasManual) { manualCount++; } else { …both pushes… }`); `manualCount` ships on the payload; the stats cache key is bumped `spanish_inbox_v1:` → `v2:` (a cached v1 payload would still carry manual resolves in its median for the TTL — INV-85). `getSpanishInboxResolved`: a manual thread ships `resolveMinutes: null` + `resolveWallMinutes: null` (the stamp is when someone pressed the button, not when the requester was answered — a duration would be a substitute, INV-187). Client: the strip note reads "· 11 timed · 1 marked manually, not timed"; the resolved card's chip reads "not timed" with an explanatory title rather than an em dash that could pass for a missing figure. The resolver-share chart is UNTOUCHED by design — it counts resolvers, not durations — and N3-SP pins that it attributes manual resolves and never reads a duration, so nobody "fixes" it to drop them. Fixture: the manual thread carries null minutes; the stats carry `businessCount: 11, manualCount: 1`. BIZ-2 (b) was REWRITTEN in place for the changed literals (`wasManual ? null : …` on both units) — the honest bookkeeping when a contract changes under a test.

TEST RESULTS: passed.
  - Pure harness `node test/client/run.js`: 781 passed, 0 failed (778 baseline; +N2, +N3-DR, +N3-SP; BIZ-2 rewritten in place). HARNESS CORRECTION: the three new pins first reported into a summary that still read 778 — the `pass/fail` console line sat ABOVE `process.exit` but BELOW nothing else, so a block appended above the exit (the documented rule) landed AFTER the summary: it ran and set the exit code, but never counted. The summary line now prints LAST, and the appended-test rule is "above the SUMMARY line". Batch A's A9 pin was affected the same way (it counted only because its section ran before the summary).
  - DOM harness `node test/client/dom/runDom.js`: 110 passed, 0 failed (unchanged).
  - `node --check` clean on Code.js and Tests.js.
  - Visual: `deptreq-light-wide`, `spanish-light-wide`, `spanish-light-mobile` re-shot — 0 overflowPx, 0 missing fixtures, no non-font console errors; all three eyeballed (DR: the Median cell reads "to resolve · 1 marked in app, not timed", the app-resolved card reads "✓ marked in app" beside the email-resolved "✓ 1h 24m", the manager table's Not-timed column reads "5 · 3 in app · 2 legacy" with the note beneath; Spanish: the strip note ends "11 timed · 1 marked manually, not timed", the manual resolved card reads "✓ not timed"; mobile stacks cleanly).
  - EVERY change bite-checked against the COMMITTED tree: 8 mutations, 8 bites (item `elapsedMin` un-nulled → N2; the fold reading `elapsedWallMin` → N2; the Spanish manual guard removed so manual resolves push durations → N3-SP; resolved-list `resolveMinutes` un-nulled → BIZ-2 + N3-SP; `resolveDeptRequest` passing `'email'` → N3-DR; the client median filter dropped → N3-DR; `drApplyResolved_` not stamping the via → N3-DR; the Spanish card showing a duration for a manual thread → N3-SP). One expectation was wrong about which pin bites: the un-nulled item literal is N2's assertion, not N3-DR's — the mutation was caught either way.
  - THREE first-write faults in the new pins, all mine and all corrected before they went green: hand arithmetic (Fri 16:00 → Mon 09:00 is 65h = 3900 min, not 4140 — "arithmetic in an assertion deserves the same check as arithmetic in code", again); the one-reader count read the reader's own null-guard double-read as a second site (the pin now asserts ZERO reads OUTSIDE the reader, the INV-167 shape); and the share fold was sliced out of Code.js when it lives in the metrics partial.
  - Regression Scenarios (Test Command is `manual`) — walked below.

REGRESSION SCENARIOS (Subsystems touched: Server, Client (Metrics views), Test Suite):
  Every scenario targets the DEPLOYED Apps Script project, so none is executable here. PASS where a harness or the matrix covers the exact step; NOT APPLICABLE where only the live project can answer.
  - S74 (Dept Requests end to end) | PASS for the client contract on camera + pins (the in-app resolve now reads "marked in app" and its elapsed figure does NOT freeze as a resolution time — it is null; the median skips it; the manager table reports it); NOT APPLICABLE for the live email-link click, the re-send dedupe and the SLA digest (live). NOTE a scenario step is now STALE by design: "re-opening confirms the elapsed figure FROZE at the resolution time" was true of an in-app resolve; it now reads "marked in app" with no figure — S74 owes a rewrite (listed under docs).
  - S93 (business hours) | PASS for the Spanish strip note and the DR fold (N2 drives the exact Friday→Monday case the scenario names through the real core); NOT APPLICABLE for the live digest.
  - S80 (Spanish resolution-share chart) | PASS — untouched by construction and pinned so (counts resolvers; never reads a duration); the manual attribution "· 1 manual" still renders on camera.
  - S57 (compliance audit panel) | NOT APPLICABLE — the `DeptRequestResolved` audit row gained a `via=` suffix in its PHI-free notes; the panel renders the notes field verbatim, nothing parses it.
  - S1 / S2 (editor suites) | NOT APPLICABLE — editor-only. Test doubles scanned FIRST: `test_deptRequest_resolveLinkIdempotent` appended a 12-column row and called the writer with two args (it still worked — a missing `via` reads as untracked — but it encoded the old contract); updated to the 13-column row + `'email'` + a ResolvedVia assertion as part of the fix. No other test reads `deptStats`, `resolveMinutes` or the DR item shape. Post-deploy `runAllTests()` stays 308.

REGRESSION RISKS:
  - Every EXISTING resolved Dept Requests row has a blank `ResolvedVia` and now reads UNTIMED (operator decision: "exclude legacy rows"), so on deploy every department's Avg/Median reads "—" with "N legacy" in the Not-timed column until new email-link resolves accumulate. Deliberate and reported, but it is the one number that visibly changes on the morning of the deploy.
  - `getDeptRequests` has a per-caller 90s result cache (`dept_req_v1:` + generation salt); a payload cached in the last 90s before the deploy lacks `resolvedVia` and carries durations for app-resolved rows until it expires or the next resolve bumps the salt. Accepted (a 90-second window); the client renders a missing `resolvedVia` as plain "resolved".
  - The Spanish stats cache WAS bumped (v2) because it is 5-minute and org-wide.
  - `markDeptRequestResolved_`'s third arg is optional by shape (`String(via || '')`) — an unknown caller passing nothing writes a blank cell, which reads as legacy/untimed, never as timed. Fail direction is the safe one.
  - The optimistic patch stamps `'app'` on the row; a later authoritative payload says the same, so no flicker between "marked in app" and a duration.
INVARIANTS AT RISK: None — INV-183 (one reader, pinned at zero outside reads); INV-187 (both exclusions reported as counts, null never a substitute); INV-185 (fixtures mirror the server literals and are pinned); INV-131 (`drFindOpenRequest_` untouched — dedupe reads status only); INV-138 (deptStats shape is additive; the SLA digest reads `deptRequestsOverdueOpen_`, which is unchanged); INV-85 (Spanish key bumped); INV-32 (the audit `via=` suffix is PHI-free).
NET SCORE: 2 − 0 = +2 (N3 on both surfaces was OPERATOR-OBSERVED this month; N2 was a verification that changed no behaviour — scored 0, its value is the pin. Zero new failure modes — the two deliberate behaviours above are stated in the UI, not silent.)

OPERATOR ACTIONS / DEPLOY:
- None to set up. | BLOCKS DEPLOY: N
- Expect on the morning of the deploy: Dept Requests Avg/Median read "—" per department (every historical resolve is "legacy" — resolved before the source was recorded) and fill in as receivers click the email link; Spanish Avg/Median may DROP (manual resolves left the sample) and the note says how many. | BLOCKS DEPLOY: N
Deploy: Server + Client (Metrics views): `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy (rides the ONE deploy owed since PR #230). Test Suite: same push; `runAllTests()` post-deploy — still 308 (M-11 grew IN PLACE).

FOLLOW-ON ITEMS:
- test/client/run.js — CLAUDE.md's harness-hazard note says "append new tests ABOVE the exit"; the true rule is "above the SUMMARY line", which now prints last. Update the note (listed under docs).
- web-app/Code.js (getDeptRequests) — the 90s result cache is not version-keyed; a shape change like this one rides out its TTL. A `_v2` bump would be cheap if a future change is not tolerant of a 90-second-old shape.
- Batches C (N4 auto-assign button, N6 DR `PatientTrx` + expand via a scoped `getDeptRequestDetail`, N8 QA reviewers editor) and D (N10 "active but not clocked in" on Team right now) are planned and unstarted.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md INV-138 (c) + the "Inter-department request tracking" KDD: the trailing `ResolvedVia` column (`email`/`app`, header self-heals, `drResolvedVia_` the one reader), the writer's `via` arg and both callers, only email-link resolves timed (app + legacy-blank untimed and REPORTED via `manualResolved`/`untrackedResolved`/`timed`), the pure `drDeptStats_` fold, the manager table's Not-timed column.
- CLAUDE.md INV-31 (the `resolveSpanishThread` manual mark-resolve paragraph): counted as resolved, `manualCount` shipped, out of BOTH duration series, `resolveMinutes`/`resolveWallMinutes` null on the resolved card; cache key `spanish_inbox_v2`.
- CLAUDE.md business-hours KDD "Where it shows": manual/app resolves are excluded from the sample on both surfaces and named beside the timed count.
- CLAUDE.md storage map: the DeptRequests tab's columns (ResolvedVia) — the tab is listed under the ADP store's fallback; add the column to the tracking KDD rather than the table.
- CLAUDE.md Operator State: a Batch A+B entry — NO new operator state; the two behaviour changes to expect post-deploy (the DR Avg/Median reset to "—" until email-link resolves accumulate; the Spanish figures dropping as manual resolves leave the sample).
- CLAUDE.md Test Command section: pure 778 → 781 (+N2/N3-DR/N3-SP; BIZ-2 rewritten in place), the summary-line placement fix, and the harness-hazard note ("above the exit" → "above the summary line").
- Regression scenarios: S74 (the in-app resolve reads "marked in app" and carries no figure; the Not-timed column; the legacy-row expectation), S80 (untouched by N3, pinned), S93 (the manual exclusion note on both surfaces).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
