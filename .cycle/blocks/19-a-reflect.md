---CYCLE SUMMARY BLOCK---
Scope: broad (cycle 19 — the 2026-09-09 /broad-scan's four implementation batches F1–F8 + D1, the operator testing-notes round of 2026-09-10 (Batches A–D, notes 1–10), and the 2026-09-11 follow-ons round RT/BP/SA/TW/DR; the between-cycles operator work before the scan was closed separately as 19pre) | Cycle: 19 / 2026-09-11
Production fixes: 8 — severity: 2 Medium (N3 on both surfaces — a manual Spanish mark-resolve and an in-app Dept Requests resolve entered the medians the SLA targets are chosen against), 6 Low (F4 the Needs-you list stale for up to 120s after completing an item; N1 the stored `major` enum leaking as "Major" into the training-overdue digest + manager brief; N5 the Spanish "Show full request" expand that could not collapse and lost its state on every re-render; N7 the quiz editor re-rendering the whole modal on every option add/remove; N9 the Team Notes "Coach on this" button shipped with no stylesheet rule; N6 a Dept Requests card unidentifiable to the desk working it)
New capabilities/features: 7 — N4 manager auto-assign, N6 Expand + stored PatientTrx, N8 the QA reviewers editor, N10 the presence chip, RT the diagnostics retention tier, BP the `reportBreakPairingChanges()` twin, SA the scheduled auto-assign trigger
Defensive/structural: 16 — F1 (greedy `breakPairs_` cursors; needs a stray stamp the live path cannot produce), F6 (the C17-9 index precondition, unreachable today), F2 (`notConfigured` vs `unavailable`; this deployment has the HR store set), F3 (`MAIL_BCC_ALL` surfaced — whether it is set is UNKNOWN from here), F5 (`qaOptionIsNumeric_` width), F7 (the PAP purple's one home), F8 (the intake-feedback third state), N2 (the `drDeptStats_` extraction — a pin, no behaviour change), N8's assign-check widening, D-N10's two layout defects introduced-and-fixed inside the batch, the sheet-doctor `unpaired` widening, TW-A, TW-B + the three stale canvas fallbacks, DR's bounded read, the shared `tsPunchDaysWithArchive_` reader
New failure modes: 0 — severity: none. Examined and rejected as failure modes, all DECLARED in their blocks and in the UI: Batch B reads every LEGACY resolved Dept Request as untimed on the morning of the deploy (operator decision, reported in the "Not timed" column); F2 narrows the Needs-you warning on a store-unset deployment (Storage Health owns that fact); SA is PTO-blind exactly as the button it mirrors; RT is destructive by design and defaults OFF on both windows.
Net score: 8 − 0 = 8
Invariant candidates (library max is INV-199 after the 19pre proposals were finally written on 2026-09-11; these start at INV-200):
  INV-200 | A colour that a `<canvas>` reads through `getPropertyValue('--t') || '#hex'` is a second source of truth, and the fallback must EQUAL the token's Console-light value; any hex literal equal to a declared token value is banned outside that shape and the named INV-166 freezes, and every other chromatic literal rides a two-sided per-file ratchet with its reason recorded — a literal replaced by a token fails until the baseline is LOWERED. Writing the scan found all three canvas fallbacks stale (`--accent` falling back to `--accent-2`'s value). | Client (shell) + Test Suite | Verify: TW-B (derived token set, the canvas regex incl. the `(… \|\| '').trim()` shape, the FROZEN list exactly-once-inside-its-selector, the RATCHET map; 6 mutations bite-checked)
  INV-201 | A validator guarding a parse must accept EXACTLY what the parse accepts — never a narrower regex beside a wider `Number()` (`"4."`, `"1e0"`, `"0x5"` passed a decimal regex and would have folded a dropdown option into the scale averages). Any function whose NAME says it decides numeric-ness decides via `isFinite(Number(…))` and carries no digit regex. | Server + Test Suite | Verify: TW-A (derived over Code.js + the scanned partials, ≥1 match required) + QA-24
  INV-202 | Only a resolution recorded by the party being MEASURED is timed. A manual mark-resolve (Spanish) or an in-app resolve (Dept Requests) is counted as resolved but excluded from every duration series, and the exclusion is NAMED beside the timed count ("N timed · M marked manually, not timed"; the "Not timed" column) — a resolve stamp records when someone pressed a button, not when the requester was answered, so a duration built from it is a substitute (INV-187). The resolver-share chart still ATTRIBUTES manual resolves: it counts people, not durations. | Server + Client (Metrics views) | Verify: N3-SP (both series, the v2 cache key, the share-chart attribution pin), N3-DR (`drResolvedVia_` one reader, timed/untimed item contract on both sides of the wire), N2 (`drDeptStats_` driven over a Fri-16:00 → Mon-09:00 pair)
  INV-203 | A SCHEDULED twin of a manual action calls the manual action's one core unchanged, sits behind a server-scope flag that defaults OFF, heartbeats BEFORE the flag check (INV-151), acts only inside `businessMinutesBetween_`'s window, and stamps a refused run into `AUTOMATION_LAST_ERRORS` — so the button and the trigger cannot pick differently and a dead trigger is visible while the feature is off. | Server | Verify: SA-1 (behavioural drive with stubs, `assertBefore` orderings, the derived heartbeat set) + `test_triggerGate_spanishAutoAssign_nonManagerThrows` + the derived TARGETS/gate-type nets
  INV-204 | A diagnostics tab's purge defaults its window to 0, deletes CONTIGUOUS bottom-up runs under a per-run budget (never row-by-row inside the ONE project lock), never deletes a row whose timestamp it cannot parse, keeps the spare-row guard against Sheets' delete-all refusal, and has its liveness checked ONLY while a window is set (INV-186) — the same shape every retention tier in the app now follows. An older client's config save can never reset a window it does not carry (`hasOwnProperty`-gated writes). | Server + Client (Call Notes views) | Verify: RT-1 (6 mutations bite-checked) + `test_triggerGate_diagnosticsPurge_nonManagerThrows` + S106
  (Folded as AMENDMENTS rather than new entries, already in the library: greedy independent-cursor pairing → INV-176; `notConfigured` ≠ `unavailable` → INV-129/INV-186; the presence beacon → INV-24; the PatientTrx store decision + `drCanAct_` → INV-138; the manager-tier `autoAssignSpanishThreads` → INV-31 (iii); the doctor's `unpaired` finding → INV-155/INV-159.)
Most structurally significant change: the break-pairing rule. F1 made `breakPairs_` greedy over two independent cursors — the payroll arithmetic every hours builder, the pay statement, the team calendar and the accrual read through — and the follow-ons round then made the shape it survives VISIBLE (the doctor's `unpaired` finding) and AUDITABLE (`reportBreakPairingChanges()` off ONE shared archive-aware reader, with the pre-F1 loop kept verbatim so the old figure is derived, never re-implemented). A rule that silently paid every break on a damaged day is now a rule with a detector and a history.
Should-have-been-deferred: TW-B's ratchet half. The token-equality ban and the canvas-fallback rule earned their place (three stale fallbacks found by measurement), but the per-file chromatic ratchet with hand-reasoned baselines (`styles.html` 8, `tc/script_clock.html` 23, `metrics` 4) is a maintenance obligation on every colour edit in exchange for guarding literals that fire on no real page; the clock ribbon's rgba pairs it now enumerates are a token-pair follow-on it could have waited for.
---END CYCLE SUMMARY BLOCK---

Honest impact summary
- What changed for a user right now: on the running app the operator tested on 2026-09-10 — a Spanish request card that expands AND collapses and stays open across a claim; a quiz editor that keeps the typed title when an option is added; a styled Coach button; digests and the brief that say "Moderate"; medians on both request trackers that no longer include button-press timestamps; a Dept Requests card that names its patient and expands to the sender's note; an "active · not clocked in" chip on Team Right Now; a manager auto-assign button. After the next New version: a Needs-you list that drops a finished item at once, a Retention panel with two diagnostics windows, and a sheet doctor that names a two-break day's stray stamp instead of offering to delete a real one.
- What changed for the next developer in this subsystem: the pairing rule has a positional twin kept verbatim for the report and a shared reader both reports use; every retention tier now has the same shape (window default 0, contiguous bounded deletes, fail-safe on an unparseable stamp, heartbeat checked only while set); a scheduled twin of a manual action has a documented posture (INV-203); two derived scans (TW-A/TW-B) turn the F5 and F7 classes into CI failures with the fix named in the message; and `drCanAct_` is the one Dept Requests ownership rule the scoped read and the write share.
- What became safer under scale / concurrent load: `getDeptRequestDetail` reads one column plus one row per Expand instead of the whole tab; the diagnostics purge and the bulk approve both bound their time inside the ONE project lock (2000-row budget; a stale index discarded rather than reasoned about); the presence beacon is one tiny RPC per real gesture per 10 minutes, never a poll.
- Effort spent on dead code / zero-caller paths / future-proofing: F6's precondition is unreachable today (guarded by a distant dup check); TW-B's ratchet; the sheet doctor's `unpaired` finding on a shape `reportMultiBreakDays()` found NO instance of on 2026-09-01 (`reportBreakPairingChanges()` has not yet been run, so the population is unverified); SA and RT are default-OFF capabilities nobody has asked to enable yet.

Scoring notes (the correction record the verifier should read first)
- The SELF-REPORTS' nets sum to 1 + 0 + 4 + 2 + 1 + 0 + 0 = 8, and this reflection AGREES with them — the first cycle since 16 where the blocks' nets and the strict re-derivation match. The reason is that every block scored itself under the 19pre rule (operator-observed on the running app = YES; introduced-and-fixed inside the batch = defensive; capabilities not counted), so there was nothing to demote.
- One CLAIM in the follow-ons block is corrected here: it says the round "rides the SAME deploy every round since PR #176 has been waiting on". The operator's 2026-09-10 testing notes describe design-handoff code from 2026-09-02 (the Coach button, the "Moderate" label, the Spanish expand), so the RUNNING app carries at least the cycle-19 audit batches (PR #235); what is outstanding is a New version carrying the operator round (PR #236) and this branch, not a backlog to #176. The F4 YES rests on that: the Needs-you block was running when reps used it.
- category_d_ratio is computed as defensive ÷ (fixes + defensive) = 16 ÷ 24 = 0.67 — the same derivation the cycle-17/18 rows used.
- Axis-B lowest: Test Coverage Quality, FIFTH cycle running. This cycle's instances: F1 survived the round that wrote it because the A1 pin exercised only extra OUTS (the in-direction cases are pinned now); the pure harness's summary line printed BEFORE three pins ran, so they were uncounted (Batch B moved it last); the jsdom outside-only trap bit a THIRD time; the D-N10 key extractor read two keys out of four on an ES-shorthand literal; a bite-script restore check assumed a "\n" replacement was unique and silently failed, and the recovery was the very `git checkout` the commit-first rule exists to make safe; a `const` inside `vm.runInContext` is not a context property. Every one was caught by bite-checking against a committed tree — the discipline holds, the harness keeps teaching the same lesson.
- Estimates: NOT recorded at estimate time for any of the seven batches — the FIFTH consecutive occurrence (cycles 13/16/18/19pre/19). The blocks carry mutation and bite counts but no S/M/L; nothing is appended to estimates.csv rather than inventing hours. The fix is the one the 19pre row named and nobody applied: estimate every batch, requested or not, in its plan message before the first edit.
- Deploy: ONE operator action outstanding — `clasp push -f` + New version carrying PR #236 + this branch, then `runAllTests()` expecting 312, `installAutomationTriggers()` for triggers #20/#21, and `reportBreakPairingChanges()` once.
