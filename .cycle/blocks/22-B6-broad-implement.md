---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- A1 — automation liveness read only the last 4,000 AuditLog rows: the monthly accrual row scrolled out within days (daily false "has not run this month" on the dot + digest) and a dead daily job went silent once its last row scrolled out; `auditScanComplete` was computed and never read
- A2 — the Admin System tab derived its own findings subset and omitted job staleness, open punches and accrual shortfalls, so it read "Nothing needs attention" under a red health dot
- C2 — the weekly training/review digest read a queue `{error}` as an empty queue, sent nothing and stamped a HEALTHY heartbeat (INV-109's failure mode)
- A3 — a configured-but-unopenable `DEPT_REQUESTS_SS_ID` silently fell back to the ADP (payroll) sheet for reads AND writes
- S7 — `offboardEmployee` cleared only the roster email, so an offboarded manager stayed in `MANAGER_EMAILS` / `ADMIN_EMAILS` (daily brief with PHI, every `assertManagerCaller_` gate), and the drift detector, keyed on the roster email, could not see it
- A4 — a failed tag-taxonomy load rendered "No tags in use yet" ({error}) or a blank pane (transport failure)
- A5 — deploy readiness blamed a failed automation READ on missing triggers and an unreachable CDR store
- A6 — a failed ClientErrors read reported "No client-side errors"
- A8 — `stampAutomationError_` / `clearAutomationError_` did an unlocked read-modify-write of one shared map
Files modified: web-app/00_config.js, web-app/10_core.js, web-app/20_timeclock.js, web-app/30_callnotes.js, web-app/50_deptrequests.js, web-app/cn/script_callnotes.html, test/client/run.js, test/client/server-split-manifest.json, test/visual/mock.js, test/visual/shoot.mjs, CLAUDE.md (running totals only), .cycle/STATE.md
Estimate: L (~12 h) — A1 M 3h · A2 M 3h · C2 S 1h · A3 S 1h · S7 S 1.5h · A4 S 1h · A5 S 0.5h · A6 S 0.5h · A8 S 0.5h
Actual: ~3.5 h

CHANGES:
A1 | 00_config.js, 10_core.js, cn/script_callnotes.html, mock.js | A per-job RUN LEDGER: `writeAuditLog_` stamps `AUTOMATION_RUN_<action>` = {ts, notes} (one Script Property per job — no shared read-modify-write, no lock) on every AUTOMATION_AUDIT_ACTIONS row. `computeAutomationHealth_` folds it into the tail scan via the pure `automationLastRunMerge_` (the NEWER run wins; an unparseable stamp never displaces a parsed row) and ships `auditWindow: {complete, startMgr, rows}` — the truncated scan now names its OLDEST row. `automationJobProblems_` (via `auditWindowCoversMonth_`) claims "has not run this month" for a job with no run on record ONLY when the window read reaches back past the 1st; a mid-month window is UNKNOWN and the panel row says "cannot confirm" (`cnRunNeverText_`) instead of "no audit row in the scan window". A run on record from an earlier month is evidence either way.
A2 | 10_core.js, cn/script_callnotes.html, mock.js | `automationProblems_(report, {items:true})` returns {kind, key, text} (the default still returns the strings the digest and badge read — the same list); `automationJobProblems_` likewise. `getAutomationHealth` attaches `report.problems`. `cnHealthFindings_` renders every item whose kind it has no branch for (`CN_SYS_CLIENT_KINDS_`) as a warn finding with a per-kind fix (`CN_SYS_PROBLEM_KINDS_`: job, openPunch, accrual; anything new gets a generic line). The populated mock fixture carries one open-punch item so the matrix photographs the new path, and a truncated audit window.
C2 | 30_callnotes.js | `sendCallNotesWeeklyDigests` sends the queue it could read, then on any queue `{error}` stamps `CallNotesWeeklyDigests` (read by F-20's untabled-key path onto the dot + digest) and RETURNS without the heartbeat, so the weekly row goes stale too; a clean run clears the stamp; the outer catch stamps as well.
A3 | 50_deptrequests.js | `getDeptRequestsSS_` throws a named error ("DEPT_REQUESTS_SS_ID is set but the spreadsheet could not be opened (…) — nothing was read from or written to the ADP sheet instead") when the property is set and `openById` fails; the ADP fallback applies only while it is UNSET. Every caller was already best-effort or `{error}`-returning; Storage Health's probe now reports the outage instead of the fallback.
S7 | 00_config.js, 20_timeclock.js, 10_core.js, cn/script_callnotes.html | `offboardEmployee` calls `offboardFromGateLists_(repEmail)` inside its lock after the roster clear: removes the address from MANAGER_EMAILS and ADMIN_EMAILS (pure `gateListWithout_`, case-insensitive) EXCEPT when it is a list's last entry (an empty ADMIN_EMAILS makes every manager an admin; an empty MANAGER_EMAILS stops every trigger) — that refusal is returned as `keptIn` with the reason, audited, and named in the offboard toast. The address is recorded in `OFFBOARDED_EMAILS` (capped 100). `managerSourceDrift_` gains a third arg and keys on the LIST: a listed address the app recorded as offboarded and not back on the roster is drift (still false-positive-free — only offboardEmployee writes the record; a non-roster service address is never flagged). The managerSource detector checks ADMIN_EMAILS the same way.
A4 | cn/script_callnotes.html, shoot.mjs | The taxonomy failure handler lands `{error}`; `cnRenderAdminAugmentHtml_` renders "The tag taxonomy could not be loaded: … — this is not an empty list" for null or `{error}`, and "No tags in use yet" only for a real empty payload. New scenario `admin-tags-fail-light-mobile`.
A5 | 10_core.js | `getDeployReadiness` turns a throw or null into `{readFailed}`; `deployReadinessItems_` renders both the triggers and CDR rows as "Could not check — the automation health read failed (…). This says nothing about …" for `{error}`, `{readFailed}` or no report.
A6 | 10_core.js, cn/script_callnotes.html | `clientErrorsSummary_`'s catch sets `error` (count stays 0); the finding becomes a warn "Client errors could not be read" and the detail panel says "this is NOT an all-clear".
A8 | 10_core.js | `withAutomationErrorLock_` serialises both helpers on the USER lock (tryLock 3s, fail-open): every caller is a trigger handler running as the one installer, often inside a catch that still holds the SCRIPT lock, which a nested acquire+release would drop early.

TEST RESULTS: passed — pure harness 988/988 (was 977; +11 Batch 6 pins), DOM 156/156, `npm run lint:server` clean, `node scripts/counts.mjs --check` agrees (pure 988, visual 116), split manifest regenerated (F2c green). Bite-checked 14 mutations, all BITE: A1 window guard removed · A1 ledger stamp removed · A1 merge keeps the older run · A2 generic render removed · A2 client claims a kind with no branch · C2 error check removed · A3 ADP fallback restored · S7 wouldEmpty ignored · S7 detector list-keying removed · S7 endpoint not wired · A4 error branch removed · A5 blame restored · A6 error not set · A8 script lock. Visual matrix (admin-light-wide, admin-tags-light-mobile, admin-tags-fail-light-mobile, admin-system-light-mobile/-light-wide/-dark-wide/-allclear-light-wide/-error-light-wide): 0px overflow, no missing fixture, only the pre-existing proxy font-certificate error; the open-punch finding renders under "Needs attention" and the failed Tags pane names itself.
Regression Scenarios (Test Command `manual`) — every overlapping scenario needs the live app, so NOT APPLICABLE here, each with its harness stand-in: S1/S2 (editor suite — no Tests.js change; run it post-deploy) · S24 weekly digests (the C2 driven pin) · S30 trigger gates (unchanged) · S51 Admin augment + S53/S70 tags (the A4 pin + the new scenario) · S74 dept requests (the A3 driven pin; a SET-but-broken id now refuses the auto-log instead of writing to ADP) · S75 onboarding/offboarding (the S7 driven pin) · S97 Admin System findings-first (the A2 driven pin; its visual half PASSES in the matrix, all-clear included) · S106 diagnostics (unchanged) · S108/S109 accrual + open punches reaching the digest (the A2 pin drives both kinds through to the tab).

REGRESSION RISKS:
- `automationProblems_` / `automationJobProblems_` return shapes are unchanged by default (strings); the digest and badge call them exactly as before — pinned (the text list equals the items' text).
- `managerSourceDrift_` third arg is optional; existing callers/pins pass two and behave as before.
- `getDeptRequestsSS_` now THROWS where it used to fall back. Every caller is inside a try (auto-log, dedupe, SLA digest) or returns `{error}`; with the property unset nothing changes.
- `writeAuditLog_` does one extra property write per AUTOMATION row (≈10 actions, at most a few per day) — never on punch/note rows.
- The monthly "has not run" line is now withheld when the evidence cannot reach the 1st — see the new failure mode below.

INVARIANTS AT RISK: None violated. Checked: INV-01 (the offboard list edits run under offboardEmployee's script lock; A8 deliberately uses the user lock and says why) · INV-14 (every stamp best-effort) · INV-109 (C2 restores it for the weekly digest) · INV-136 (S7 refuses to empty ADMIN_EMAILS precisely because that would widen the admin tier) · INV-179 (A2's kind set is DERIVED in its pin, and the fixture must fire every kind) · INV-185 (the mock mirrors `auditWindow` and `problems`) · INV-186 (the ledger and the S7 record keep both indicators clean on a healthy deployment) · Q-1 (every new property write goes through `propSetBounded_`).
NET SCORE: 2 − 2 = 0
- Production fixes (would have fired this month): A1 (a daily false "PTO accrual has not run this month" on the dot and digest once the 1st's row scrolls out of the 4,000-row window) · A2 (any day with open punches — the daily scan's own finding — put the dot red over a clean System tab).
- Defensive (fires only on a failure or an event not seen this month): C2, A3, S7, A4, A5, A6, A8.
- New failure modes: (1) A1 — in the bootstrap month, before the accrual credit first stamps the ledger, a GENUINELY missed credit under a truncated window is now silent on the dot (it was flagged, though mostly falsely); the panel row says "cannot confirm", and from the job's first ledger stamp the check is exact. (2) S7 — offboarding now edits Script Properties, and re-onboarding the same person does NOT restore their MANAGER_EMAILS / ADMIN_EMAILS entries (deliberately — access is re-granted by hand — but an offboard done by mistake costs a manual re-add). Both Low.

OPERATOR ACTIONS / DEPLOY:
- One-time review of MANAGER_EMAILS and ADMIN_EMAILS for anyone offboarded BEFORE this deploy — the S7 record starts empty, so the detector cannot see historical offboards | BLOCKS DEPLOY: N
- After deploy, open Manage → Admin → System: the "Automation last seen" rows read "no run on record — cannot confirm" until each job next runs; that is expected, not a fault. If "Open punches" now appears under Needs attention, it was already on the dot | BLOCKS DEPLOY: N
- If DEPT_REQUESTS_SS_ID is set, confirm Storage Health reads it reachable — a broken id now refuses the auto-log instead of writing to the ADP sheet | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → New version (with Batches 1–5, S2 and the follow-ons, none of which are deployed yet)

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- `sendCallNotesUrgentDigest` reads `managerAggregateUrgent_`'s `{error}` the same way C2's digest did (nothing sent, healthy heartbeat) — same fix, out of this batch's scope.
- `automationProblems_` does not carry `clientErrors.error` (A6), so a blind burst detector shows on the System tab but not on the dot/digest.
- A DAILY job with no run on record is still silent (the fresh-deploy posture): one that died before the ledger shipped and whose row already scrolled out stays silent. The panel row says "cannot confirm"; a job-enabled flag on the report would let it say more.
- Offboarding the account that INSTALLED the triggers stops every automation (they run as that account); nothing detects it.
- `QA_MEMBERS` / `SPANISH_INBOX_MEMBERS` are not edited by offboarding — check whether each gates on the roster email too (if so the offboard already revokes it).
- The "Automation last seen" rows repeat a long "cannot confirm" caption per job under a truncated window — one caption above the list would read better.

DOCUMENTATION UPDATES NEEDED:
- docs/gotchas.md + CLAUDE.md index: g151 — a health surface that derives its OWN subset of the list the dot counts can read clean under a red dot; render the dot's list (A2). Amend g02/g53 (a bounded tail scan's absence is only evidence within the window it read — A1), g128/g41 family (a configured store that will not open is an outage, never a fallback — A3), g25/g26 (offboarding and the Script Property gate lists — S7), the "Never cache/claim a failure" family for A4/A6, and g34-adjacent INV-109 for C2.
- docs/operator-state.md + CLAUDE.md inventory: `AUTOMATION_RUN_<action>` (auto-managed run ledger), `OFFBOARDED_EMAILS` (auto-managed, capped 100), offboarding now edits MANAGER_EMAILS / ADMIN_EMAILS and keeps a last entry; the Dept Requests store no longer falls back when configured.
- docs/modules.md (Manage → Admin: System tab renders the dot's list; offboarding), docs/design-decisions.md (automation health: ledger + audit window; the System tab consumes automationProblems_), docs/operator-log.md (Batch 6 round), docs/test-harness-log.md (the 11 pins, 14 bite-checks, new scenario).
- .cycle/config.md: new invariants (next INV-263) for the run ledger / window rule, the System-tab-renders-the-dot rule, the configured-store-no-fallback rule, and offboard-edits-the-gate-lists; scenario S117 (the Admin System tab under a red dot + offboarding a manager); walk steps on S24, S74, S75, S97.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
