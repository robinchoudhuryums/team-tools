---CYCLE SUMMARY BLOCK---
Scope: broad — Server (all fourteen files), Client (shell, Time Clock, Call Notes, Metrics, Intake, Reference, Training, QA, public forms), Test Suite | Cycle: 22 / 2026-09-23 → 2026-09-25 (deployed 2026-09-25, after-deploy steps done)
Production fixes: 22 — severity: 1 High (T1: Day Edit deleted the LunchOut of a rep on lunch) · 14 Medium (K6, K2, M1, M2, M8, K5, A1, A2, T4, T8, I5, T5r, U1, M6) · 7 Low (M3, M9, T9, FU B7-1, FU B7-3, U2, U3)
New capabilities/features: 2 (S6: no QA self-review with an admin exception; S10: score-only feedback on a failed quiz attempt — both operator policy decisions)
Defensive/structural: 84
New failure modes: 5 — severity: 5 Low (B6 A1: in the bootstrap month a genuinely missed accrual under a truncated window is silent on the dot · B6 S7: re-onboarding does not restore gate-list entries · B8 I2: an unexpected seat word drops that chair until the cell is fixed · FU B6-4: a newly enabled daily job alarms until its first run · DF M6: the legacy hours→days conversion can misread a target that meant business hours — closed by the operator's review-and-save after deploy)
Net score: 22 − 5 = +17
Corrections to the self-reports (blocks summed to 25 − 6 = +19): F4 demoted to defensive (FO: SpanishClaims passing 1000 rows is "plausible within months", not this month); D1 demoted (B4: "if coaching is in use" — occurrence unverified); M4 demoted (B4: the scheduled auto-assign is default OFF and use of the manual button is unconfirmed); B7's T5 failure mode removed (the mid-shift grading was replaced by the FU rework before anything deployed, so it never ran). Checks the blocks asked for that this reflection could NOT make (live data is not readable from here): the AuditLog for T1/T3 firings, ClientErrors/AuditLog for S3/S4, duplicate notes after an external send (C4), stored formulas (S2 — Admin → System → Stored formulas). Each would promote a defensive item, never demote a fix.
Invariant candidates: INV-301 | A stored threshold carries its unit, and a change to how a measured quantity is computed enumerates every stored value compared against it | Server config ↔ Script Properties (DR_SLA_TARGETS and any future target map) | Verify: the M6 pins; probe for any other stored target compared against businessMinutesBetween_ or a changed basis · INV-302 | No value derived from the code (build hash, template output, a partial list) is written to CacheService or Script Properties, which HEAD and every versioned deployment share | Server (shell / deploy beacon) | Verify: BCN-1/BCN-1b + a grep of every CacheService put for a code-derived key · INV-303 | A grading or reminder rule that depends on an operator POLICY (half day, SLA unit, self-review) ships only after the policy is recorded in STATE's Decisions; an assumption in a code comment is not a decision | Cycle machinery ↔ Server (Time Clock, DR, QA) | Verify: every "assumes"/"assumption" comment on a grading path cites a Decisions line
Most structurally significant change: S2 — every sheet write crosses one boundary (sheetSafe_/sheetText_, 183 sites) and a lint rule refuses a new unwrapped write, so formula injection is closed by construction rather than by review.
Should-have-been-deferred: B7's T5 — it shipped an ASSUMED half-day policy (graded from mid-shift) that the operator's actual rule (at least 4 hours, any time) replaced one batch later; the half-day grading should have waited for the policy.
---END CYCLE SUMMARY BLOCK---
