---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F-52 (four structural pins made behavioural, three of them replacing the originals) · F-50 (lint-server's advanced-service globals derived from appsscript.json) · F-51 (the gate-endpoint counts cover all three refusal literals) · F-53 (`_TEST_OVERRIDE_COACH_MAIL` moves out of production code) · F-31 (the CDR agent-metrics cache bypassed under the test override; the fixture hashes the roster through the one predicate) · F-33 (`cdrQueueInventory_` span-bound, and `truncated` describes the window) · F-34 (the Spanish stats card counts the voicemails the list shows) · F-26 ("manager-gated" over 21 admin gates) · F-39 (the timesheet `computeRange` ↔ `getCurrentBiweeklyRange_` mirror pinned) · F-48 (an unmeasurable day is a gap in the sparkline, not a 0h bar)
Files modified: scripts/lint-server.mjs · scripts/counts.mjs · test/client/run.js · test/visual/mock.js · test/client/server-split-manifest.json · web-app/00_config.js · web-app/10_core.js · web-app/20_timeclock.js · web-app/30_callnotes.js · web-app/40_metrics.js · web-app/51_spanish.js · web-app/70_kb.js · web-app/Tests.js · web-app/styles.html · web-app/tc/script_manager.html · web-app/metrics/script_metrics.html · CLAUDE.md · .cycle/config.md · docs/design-decisions.md · .cycle/STATE.md
Estimate: M (~4.5 h)
Actual: ~4.5 h

CHANGES:
F-52 | test/client/run.js, test/visual/mock.js | Four pins whose names promised a behaviour asserted only a source shape. THREE are rewritten as drives and the originals RETIRED (not left beside them — a weak pin that stays green under the mutation is the finding). `archiveSheetRowsOlderThan_` runs against a fake sheet: the bound really stops the scan, a second run drains the NEXT batch rather than re-appending the same one, an unbounded run takes every eligible row and only those, a slack bound is not a truncation, and a tab with nothing eligible appends nothing and flushes nothing. `getDepartmentEmails_` / `saveDepartmentEmails` run against a fake PropertiesService: an array, a number, a blank, a bare name and a whitespace key are each dropped entry-wise, an all-junk map falls back whole rather than to `{}`, and NOT ONE refusal writes the property. `clientBuildHash_` runs against a fake HtmlService: the hash MOVES when a partial's bytes move, MOVES when index.html moves, is STABLE when nothing does, and a lost partial is a different build. The fourth — the width-cap pin — is an ABSENCE assertion with no function to call; it stays structural, says so, and now also asserts the measured visual scenarios that carry the real claim still exist.
F-50 | scripts/lint-server.mjs, test/client/run.js | Six ADVANCED-service globals (Drive, Docs, Sheets, Gmail, BigQuery, People) were hardcoded in the linter's globals list while `appsscript.json` reads `"dependencies": {}`. An advanced service is a global only while the manifest enables it, so a use of any one would have linted clean and thrown ReferenceError on its first real call — the static net silencing exactly the class it exists to catch. `advancedServiceGlobals()` derives them from `enabledAdvancedServices`' `userSymbol`s; the pin drives it over a manifest the repo does not have (none enabled → none; two enabled → those two under their SYMBOLS; an entry with no userSymbol → no global, never `undefined` in the map) and separately checks no un-enabled service is used in the server, over comment- AND string-stripped source.
F-51 | scripts/counts.mjs, CLAUDE.md, test/client/run.js | "Manager-gated endpoints" counted one refusal literal, so the whole `assertManagerCaller_` family (34 endpoints that THROW rather than return) and the whole `canSeeQa_` family were invisible under a caption an operator reads to decide how wide MANAGER_EMAILS should be. Three families now, each counted by what it REFUSES with: admin 51, manager 95, QA 17 (a new block row). `canSeeQa_` gets its own row rather than joining manager — it admits `isManager OR QA_MEMBERS`, so folding it in would have replaced an undercount with a wrong claim. `getEmployeeState` calls `canSeeQa_` to SHIP the flag and is deliberately not counted; the pin asserts that by name, re-derives all three families independently of counts.mjs, and requires no endpoint to appear in two.
F-53 | web-app/00_config.js, web-app/Tests.js, test/client/run.js | `_TEST_OVERRIDE_COACH_MAIL` was declared in `00_config.js` while the block in Tests.js said the overrides live "in one place". A test seam in production code is shipped code. Moved beside the other eight; the consumer reads it through `typeof … === 'function'`, so production is byte-unchanged. The pin derives the ban from `filePushOrder` minus Tests.js, so a NEW override declared in a production file fails too.
F-31 | web-app/40_metrics.js, web-app/Tests.js, test/client/run.js | `getCdrAgentMetrics_` is the lowest CDR tier — every cached reader above it sits on it — and the only one that did not bypass under `_TEST_OVERRIDE_CDR_SS_ID`: a fixture read served production's cached numbers, and wrote the fixture's numbers back under production's key. Both ends gated now. The editor fixture that cleared those keys by hand built its roster set from every row with a non-empty NAME rather than through `empRosterEmail_`, so one offboarded-but-named row produced a different `cdrRosterHash_` and the removal hit a key nothing had written. The pin evaluates the bypass boolean under three override states and drives `cdrRosterHash_` to show one extra name really is a different key.
F-33 | web-app/40_metrics.js, test/client/run.js | `cdrQueueInventory_` tail-scanned the last `CDR_QUEUE_SCAN_MAX` rows whatever the window asked for, and set `truncated` from the SHEET's length: on a tab longer than the cap the diagnostic warned "possibly incomplete" on every run for ever, including runs that had read every row in the window — a caution that is always on is a caution nobody reads (g02). Span-bound through the existing `cdrDqeWindowSpan_` now, and `truncated` means the WINDOW outgrew the cap. Driven end to end against a fake sheet: a 4-row window on a 44-row tab under a cap of 10 reads exactly 4 rows starting at the window, reports no truncation, and keeps the sentinel row out of the agent/date tally; a 25-row window under the same cap reads exactly 10 and DOES report truncation; an empty window returns cleanly and reads nothing.
F-34 | web-app/51_spanish.js, web-app/metrics/script_metrics.html, test/visual/mock.js, test/client/run.js | 8x8 mails each member's individual inbox rather than the group address, so a voicemail never matches the group-address query. The pending LIST folded them in (operator 2026-08-25); the STATS card, computed from the same mailbox, counted none — the Spanish tab showed four pending cards under a card that said three, and the card is what a manager quotes. ONE fold now (`spanishVmFold_`), read by both; the duration gate runs before the resolution check so a hang-up is work on neither surface. The card states its own voicemail figures, with "fold not configured" rendered differently from "none came in" (INV-187). Cache key bumped `spanish_inbox_v2` → `v3` (INV-85). The visual fixture carried the defect — `pending: 3` above four cards — and now agrees with itself, pinned.
F-26 | web-app/10_core.js, web-app/30_callnotes.js, web-app/70_kb.js, .cycle/config.md, docs/design-decisions.md, test/client/run.js | Twenty-one places said "manager-gated" over a `callerEmp.isAdmin` gate: nine endpoint doc comments the scan named, ten more in `10_core.js` it did not, INV-31 (for nineteen endpoints), INV-82 and two design-decision paragraphs. A manager who is not in `ADMIN_EMAILS` is refused by every one of them, so an operator reading INV-31 to size `ADMIN_EMAILS` read a different product. All corrected; the pin DERIVES the check — it walks every server function, takes the doc comment that ends immediately above the declaration (not merely the nearest one behind it, which resolves to the previous function's and reports a phantom), and compares it against the refusal literal in the body.
F-39 | test/client/run.js | The timesheet client's `computeRange` mirrors `getCurrentBiweeklyRange_` — `floor((daysDiff + 13) / 14)`, back off 13 days — in two languages, in two files, with nothing holding them together (g120). Pinned by driving BOTH over 70 consecutive days spanning three period boundaries, including the anchor day and the day after, plus the client's `offset: -1` against the server one period back.
F-48 | web-app/20_timeclock.js, web-app/tc/script_manager.html, web-app/styles.html, test/visual/mock.js, test/client/run.js | `hours: sparkHoursMap[key] || 0` collapsed two different days onto one bar: a rep who did not work (a real zero, which V-10 deliberately gave a visible dim floor) and a rep whose day could not be measured — still clocked in at the moment of the read, or a stamp `calcHours_` refused. The second is an UNKNOWN, and an unknown is not an elapsed zero (g54): it told a manager "0 hours worked" about a day the server had no hours for. Three states now — no punch rows is a real 0, punched-but-unmeasurable ships null, measured ships its number (including a genuine 0.0h, which presence in the map now distinguishes). The sparkline renders null as a hatched full-height gap with a "no data" tooltip, the total counts only what could be measured and says how many days it could not, and an all-unknown week renders instead of collapsing to nothing. The visual fixture ships a null day so the state is photographable.

TEST RESULTS: passed. Test Command is `manual`, so: pure harness 908/908, DOM harness 126/126, `npm run lint:server` clean, `split-manifest --check` current (1300 declarations), `counts.mjs --check` agrees with the tree. Visual matrix re-shot for the three `manage-*` and three `spanish-*` scenarios: 0 missing elements, 0 overflow px on all six, and both changed surfaces verified by eye — the live-status cards render the hatched unknown bar with a "·1?" count, and the Spanish stats card now reads PENDING 4 above a list of 4 with "1 voicemail included · 2 short voicemails hidden (under 5s)".

Regression Scenarios overlapping a modified file — NOT RUN in this environment (every one needs the deployed Apps Script project with live Sheets/Gmail; this is the standing position for the whole cycle, not a new gap). Each is named with what an operator must look at after the deploy:
- S10 (cross-timezone live status) — `getManagerDashboard`'s `recentHours` changed shape. Check a rep who is still clocked in renders a hatched bar, not a 0h bar, and that a genuine day off still renders the dim zero bar.
- S41 (My Stats + sparkline) — `getCdrAgentMetrics_`'s cache gained a bypass. Check My Stats still serves from cache on a re-enter (no slowdown) on a normal instance.
- S80 (Spanish resolution share) / S105 (Spanish auto-assign) — the voicemail fold moved into `spanishVmFold_`. Check the pending list is unchanged, the KPI strip's Pending now equals the list's count, and the head note names the voicemails included and the hang-ups hidden. The resolver-share chart is untouched by design.
- S98 (Manage surface) — `tc/script_manager.html` changed. Check the Punctuality and Admin surfaces still fill the view width.
- S99 (Coaching) — `_TEST_OVERRIDE_COACH_MAIL` moved file. Production behaviour is unchanged; the check is that `runAllTests` on dev still reports the coaching mail cases passing.
- S111 (one answer rate) — `40_metrics.js` changed (cache bypass only, no formula change). Check a rep's % Answered still matches the Department Dashboard.
- S53 (tag taxonomy) / S106 (diagnostics retention) — comments only in the files they touch. NOT AFFECTED.

REGRESSION RISKS:
- `getSpanishInboxStats`'s figures CHANGE: resolved, pending and both duration series now include voicemails. That is the fix, but a manager comparing this week to last will see a step, and the cache-key bump means it appears immediately rather than after the TTL.
- The Spanish pending payload's `vmSuppressed` now counts a short voicemail even when it was resolved (the gate runs before the resolution check, so one fold serves both surfaces). The number can be slightly higher than before; its meaning is now "hang-ups the gate hid from both surfaces".
- `cdrQueueInventory_` reads the DQE date column (one column, full height) before its 3-column window read. One extra narrow read on an admin panel that is gated OFF by default.
- Widening `managerEndpoints` 61 → 95 changes a figure the docs quote. INV-136's admin row is unchanged at 51.
- Three pins were retired and replaced. Their claims were checked into the replacements: F3's caller half and the Punctuality CSS-absence half are carried verbatim; the dept-email pin's two source assertions are strictly weaker than the drives that replaced them.

INVARIANTS AT RISK:
- INV-85 (a changed cache payload bumps its key) — honoured: `spanish_inbox_v3`.
- INV-187 (a degraded or excluded figure is named, never absorbed) — extended, not at risk: the sparkline states its unmeasurable days, the Spanish card states its voicemail counts, and "fold not configured" is rendered apart from "none came in".
- INV-136 (the admin-gated set) — the count is unchanged; INV-31's claim about who may call nineteen of its endpoints is corrected to match it.
- INV-169 (a bounded read reports truncation) — honoured, and `truncated` now carries information rather than being permanently true.
- INV-185 (the fixture can produce the state the pin claims) — two fixtures fixed under it: the sparkline's null day and the Spanish stats/list agreement.
- INV-179 (a number is derived from what defines it) — the gate counts and the doc-comment check are both derived walks, not hand lists.

NET SCORE: 3 − 1 = 2
Production fixes: 3 — F-34 (Medium: the Spanish card and the list beside it disagreed every day, and the card is what a manager quotes), F-48 (Low: any rep still clocked in, or with one unparseable stamp, in the last 7 workdays rendered as a 0-hour bar on their manager's live-status card — the visual fixture had such a day), F-31 (Low: on the DEV instance the operator uses daily, a nightly full run wrote fixture CDR numbers under production-shaped keys for the hour TTL; it cannot fire on prod, where `runAllTests` refuses).
Defensive/structural: 7 — F-52, F-50, F-51, F-53, F-33, F-26, F-39.
New failure modes: 1 — Low. A voicemail resolved by a member reply now enters the response-time series, so the Spanish median mixes two populations: a real request→reply gap and the gap between an 8x8 notification and someone replying on a no-reply thread. Resolution semantics are unchanged (the list already counted such a reply as resolved); only the timing is new, and it can move the median without the underlying service changing.

OPERATOR ACTIONS / DEPLOY:
- None. No new Script Property, no sheet or tab, no migration. The `spanish_inbox_v2` cache entries expire on their own (5-minute TTL) and are simply no longer read. | BLOCKS DEPLOY: N
Deploy: Server + every client subsystem: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: **New version** → Deploy. Test Suite: same push; run `runSmokeTests()` on prod and `runAllTests()` on dev.

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- The counts block now carries THREE gate rows, and `gatedEndpointsFromSource_` in run.js (which F9's coverage pin and the omnibus tier-link pin both use) still knows only two. Widening it would require every `assertManagerCaller_` and `canSeeQa_` endpoint to have a gate test named in the omnibus; worth checking whether they already do, but it is F9's scope, not F-51's.
- Three bite-checks on this batch's own pins reported NO BITE and each was a real weakness, fixed in place: the archive fake accepted a zero-row `setValues` that real Sheets refuses; the missing-partial assertion varied index.html's own text so it could not isolate the marker; and — after two attempts to isolate it — the claim that the marker NAMES the lost file turned out not to be hash-observable at all, so the assertion was deleted rather than dressed up. A fourth NO BITE was an equivalent rewrite (`ceil(n/14) === floor((n+13)/14)` for every integer), verified rather than assumed.
- `docs/gotchas.md` has no entry yet for "a pin whose name promises a behaviour must drive it" — the F-52 lesson generalises past this batch and belongs beside g116.
- The F-34 fold changed what `vmSuppressed` counts (see REGRESSION RISKS). If an operator has been watching that number, the step is expected.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md's Common Gotchas index: a new entry for the F-48 three-state rule (an unmeasurable day is not an elapsed zero — the g54 family, now with a second instance) and one for the F-50 class (a static net that pre-declares names the runtime does not provide silences the bug it exists to catch — the g118 family).
- `.cycle/config.md`: invariant candidates for the F-52 rule (a pin that names a behaviour drives it, and an absence assertion says it cannot), the F-34 rule (two surfaces computed from the same source read ONE fold), and the F-26 rule (a doc comment's gate claim is derived from the refusal literal, never written by hand).
- `docs/modules.md`: the Spanish stats card now counts voicemails; the manager live-status sparkline has a third bar state.
- `docs/test-harness-log.md`: the batch's pins, the three pin weaknesses the bite-checks found, and the two fixture gaps (the sparkline null day, the Spanish stats/list disagreement).
- `docs/operator-state.md` / the CLAUDE.md storage map: no change — no new operator state.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
