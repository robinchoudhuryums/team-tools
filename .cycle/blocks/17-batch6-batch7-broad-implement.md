---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: (cycle-17 batch ⑥ structural/growth + batch ⑦ visual-lens expansion)
Batch ⑥:
- C17-9 — managerSaveDayRange no longer amplifies the global ScriptLock: a 31-day × 4-slot range ran up to 124 `findExistingPunch_` FULL-Timesheet reads + up to 124 personal-sheet `openById` round-trips inside the ONE project lock (every rep's punch waits out the 15s `waitLock` meanwhile — the INV-153 starvation reasoning, and an execution-limit kill mid-run was plausible). Now: `buildAdjustPunchIndex_` reads the Timesheet ONCE per run (last-match-wins, the INV-155 agreement) and `writeAdjustPunchForEmployee_` takes an optional ctx; the personal-sheet mirror memoizes its spreadsheet handle per execution (`openPersonalSs_`, the L-3 getAdpSS_ pattern). Single-punch callers unchanged.
- (lockout) — `getNextActions_` derives the state from the last RECOGNIZED punch type: a hand-edited/garbage COMMENTS value used to become the "state", collapse next-actions to `['Adjust']`, and — via the INV-155 live sequence guard — lock the rep out of live punching for the rest of the day. A garbage-only day now reads as not-clocked-in; all four known-type transitions byte-identical.
- (Spanish cap, INV-169) — the three Spanish-inbox readers share the named `SPANISH_THREAD_SCAN_MAX` (200) and return `truncated` (≥ cap ⇒ possibly more); the Spanish tab renders "scan capped at 200 threads — figures may be incomplete; narrow the window" on the stats note AND both list tabs. A 90-day window busier than the cap previously under-reported with no signal.
- (fan-in seq, INV-156) — `trainLoadMgr_` (5 RPCs), `edLoadMgr_` (3), `coachLoadMgr_` (2) carry same-view seq tokens guarding EVERY state write, not just the render: two overlapping runs for the SAME view (a post-mutation refresh while the first load is in flight) share the TRAIN/ED/COACH_STATE fields, and a stale round-1 response landing after round 2 rendered overwrote one field last-writer-wins — the view guard alone can't stop it because currentView never changed.
- (dead CSS + cnLoadDate_) — ~240 lines of INV-184-class dead CSS deleted from styles.html: the `.hero-clock*` cluster, `.actions-grid`/`.action-btn`/`.btn-*` tiles, the `.ledger` strip, `.leave-balance-row`/`.lb-*`, `.ts-summary`/`.ts-stat*`, the `.day-card`/`.day-list`/`.pt-chip` timesheet-render cluster — plus FOUR surviving compact halves C17-1's grid-only scan couldn't see (`.hero-clock` typography ×4, `.action-btn` ×2, `.ledger .cell-value`). Zero markup emits any of them (verified per class). `cnLoadDate_` (zero callers) removed together with its CLAUDE.md loader-gotcha mention (the A4 precedent).
Batch ⑦:
- (error states) — the visual mock gained a `?failrpc=name1,name2` forced-failure hook (fires the FAILURE handler before fixture lookup — a failed RPC is not a missing fixture), and the matrix gained the first three ERROR-STATE scenarios: metrics My-Stats, CN Log cold-failure (the C17-5 fix on camera: error toast + "this is a load failure, not an empty day" warn card while the pinned tray still renders), and Reference tree at mobile. The A12/INV-175 class is finally photographable.
- (Admin panel) — first-ever Admin scenarios (light + dark): full fixture set for `getAdminConfig` / `getAutomationHealth` / `getStorageHealth` / `getDeployReadiness` / `getRetentionConfig` / taxonomy / trends / audit-log / enrollment / unresolved-count, field names mirrored from the server return sites (INV-185). The shots render the deploy-readiness checklist, the three System-status cards (CDR ambering off `likelyMismatches` — the INV-186 tone, on camera), and the KPI strip.
- (dark parity) — `reference-dark-wide`, `training-dark-wide`, `coaching-dark-wide` close the known dark-coverage gap; all three render correctly (the batch-③ fixture fixes are visible: the coaching TRX chip and the review-due views/total/cap fields).
Matrix: 29 → 37 scenarios.

Files modified: web-app/Code.js, web-app/styles.html, web-app/metrics/script_metrics.html, web-app/train/script_training.html, web-app/train/script_empdocs.html, web-app/train/script_coaching.html, web-app/cn/script_callnotes.html, CLAUDE.md (loader-gotcha mention, paired with the code removal), test/visual/mock.js, test/visual/shoot.mjs, test/visual/README.md, test/client/run.js (+ .cycle checkpoint files)

CHANGES: detailed above. Pins: 5 new batch-⑥ (C17-9 one-read-indexed + memoized mirror; getNextActions_ BEHAVIORAL — garbage-row cases run against the real extracted function; Spanish named-cap + truncated on all three readers + client note; the three fan-ins' bump + per-handler guard counts; the dead-selector ban incl. cnLoadDate_) and 3 new batch-⑦ (failrpc-before-fixture-lookup; scenario coverage for admin/dark/error; the getAutomationHealth fixture keys DERIVED from computeAutomationHealth_'s own return block — INV-185/179, never a hand list). Pure 425→433, DOM 69.

TEST RESULTS: pure 433/0; DOM 69/69; `node --check` clean (Code.js, mock.js, shoot.mjs). Bite-checks (7): ctx pass-through dropped → trips; known-type scan reverted → behavioral trips; one literal 200 restored → trips; one seq guard removed → trips; a `.ledger` rule reintroduced → trips; a fixture key removed → derived-key pin trips; an error scenario removed → coverage pin trips. Visual matrix: FULL 37-scenario re-shoot (owed — batch ⑥ deleted styles.html rules) — **37/37, 0 missing fixtures, 0 horizontal overflow, no non-fonts console errors**; eyeballed admin light+dark, all three error shots, all three dark-parity shots, and clock-light-wide (byte-identical layout — the deleted clusters were genuinely dead). Scenarios: S25/S39 unchanged-by-inspection + full-matrix-verified; S67/S69 + the coaching flow markup untouched (seq tokens are load-path only); S1/S2 NOT APPLICABLE (editor-only).
NOTE (process): one bite-check used `git checkout -- Code.js` to revert a mutation and wiped ALL uncommitted batch-⑥ Code.js edits (the batch-3/4 accident repeated) — re-applied from the scripted edits and switched to python-reversal for every later bite; batch ⑥ was committed before batch ⑦ started so it can't recur within the session.

REGRESSION RISKS:
- C17-9's index is built inside the same lock every Timesheet mutator takes, so it cannot miss a concurrent append; each (date,type) is written once per run, so the index never needs mid-run updates. Single-punch approve (updatePunchAdjustStatus) is byte-identical (no ctx).
- getNextActions_: the only behavior change is for days containing unrecognized rows — previously Adjust-only lockout, now the last KNOWN punch drives the state. All-known days byte-identical (pinned).
- Seq tokens DROP stale responses instead of writing them; the newest round always writes its own state. A dropped analytics response can no longer resurrect a stale panel.
- Dead-CSS deletion verified by the full 37-scenario matrix; `PUNCH_META.cls` / `STATUS_META.cls` remain as inert unread data fields (noted, deliberately untouched — client constants, not the INV-184 CONFIG class).
- The Spanish `truncated` field is additive; an older client ignores it.

INVARIANTS AT RISK: none violated — INV-155 (last-match-wins) preserved by construction and pinned; INV-01 lock structure unchanged; INV-153's starvation reasoning is what C17-9 implements; INV-156 extended to three more loaders; INV-169/187 extended to the Spanish readers; INV-184 advanced (the cluster this entry predicted as "a follow-on with the rest of the cluster" is done); INV-185/179 advanced (derived fixture-key pin). Doc updates owed below.

NET SCORE: 4 − 0 = 4
(Production fixes: C17-9 (fires on the next multi-day Day-Edit range — every such edit previously held the global lock for up to minutes: YES), the punch lockout (hand-edit-gated: NO), the Spanish cap (fires whenever a 60–90d window exceeds 200 threads: plausible NO), the three fan-in races (slow-network post-mutation refresh: NO). The dead-CSS sweep + cnLoadDate_ are structural (INV-184 hygiene, not user-visible); batch ⑦'s three items are harness CAPABILITY (coverage, not app fixes) — not counted. New failure modes: none identified — the full matrix + behavioral pins verified each change's unchanged-path byte-equivalence.)

OPERATOR ACTIONS / DEPLOY:
- None beyond the standing post-deploy `runAllTests()` | BLOCKS DEPLOY: N
- After deploy, a multi-day Day Edit range apply should feel noticeably faster (one read instead of ~124) | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy.

FOLLOW-ON ITEMS:
- NEW (surfaced by the error-state scenario the moment it existed): when `getReferenceTree` fails, the Reference LANDING pane renders an indefinite `loSweep` loader below the tree's error card (the landing load depends on the tree and has no failure branch of its own) — Low, pre-existing, now photographable (`reference-error-light-mobile.png`).
- Still-uncovered visual surfaces: Manage → Coverage/Punctuality tabs, the Sent Forms tab, EmpDocs My Docs, and modal/overlay states (composer, day-edit) — the matrix shoots tab landings only.
- The remaining scan Lows not in any batch: full fixture-skeleton derivation from server return sites (the batch-③ promotion), `drStatus_` predicate (INV-183), acknowledgeDoc data:image validation.
- form_public accordions aria (outside A11Y_SCAN_PARTIALS) — carried.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md Visual Audit Stage: 29 → 37 scenarios; the failrpc error-state mechanism; the "still uncovered" list (dark Reference/Training/Coaching + Admin are now COVERED — replace with the new gap list above); "Open the 29 PNGs" → 37.
- INV-108 (managerSaveDayRange gains the one-read ctx); INV-155/recordPunch gotcha (getNextActions_ skips unrecognized types); INV-156 (three more tokened loaders); INV-31 Spanish amendment (truncated fields + the named cap); INV-184 (the base-cluster deletion closes the styles.html follow-on); INV-185 (the derived fixture-key pin).
- STATE.md open-follow-on list: drop the dead-CSS + cnLoadDate_ items (done), add the Reference-landing loader finding.
- Test count 425 → 433.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
