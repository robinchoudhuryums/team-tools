---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: PR 6 (design handoff, the Time Clock surface) — T1 `getMyPendingTasks` + the "Needs you" block leading `#dash-main`; T2 the denser clock card with a state line on a literal-colour scrim, the retired world-clock strip / shooting star / greeting pill, and the derived ban pins; T3 the greeting rotator HELD while a shift is active; T4 hours rendered ONCE (state line) and the break chips absorbing the next-break chip; T6 placement (card → actions → strip, MEASURED) + the compact gate before any RPC. T5: nothing to build (confirmed).
Files modified: web-app/Code.js, web-app/tc/script_clock.html, web-app/styles.html, web-app/Tests.js, test/visual/mock.js, test/visual/shoot.mjs, test/visual/fold-measure.mjs (new), test/client/run.js, test/client/dom/runDom.js, CLAUDE.md, .cycle/STATE.md, .cycle/blocks/19pre-design-pr6-broad-implement.md

CHANGES:
T1 | web-app/Code.js | `getMyPendingTasks()` — rep-callable read (bare `{error}` gate), per-rep CacheService 120s cached on CLEAN rounds only; six sources (training / coaching / notes via `prevWorkdayIso_` / dept requests mine+incoming / sched due today / docs needsAction), each in its own try/catch pushing its kind to `unavailable[]` (a `noteCountUnavailable` round THROWS rather than reading as 0 missing); pure `pendingTasksSort_` (overdue first, due asc blank-last, title); `PENDING_TASKS_CAP` 30 with pre-slice `total`; every item carries a `{tool, tab}` route + the notes item a `CLK_NAV_HINT`-shaped `hint`. Operator decisions: QA omitted; Requests = dept requests only; docs the sixth kind.
T1 | web-app/tc/script_clock.html | `#dash-needsyou` rendered BEFORE `#dash-cards`; `CLK_NEEDS` SWR (freshness stamped only on clean rounds; last-good kept on failure; null only on a cold miss); `clkNeedsYouHtml_` (skeleton / `errorStateHtml_` / clean-empty renders NOTHING / `Overdue` word with an `is-past` tone / 44px `<a>` rows / a warn line naming every unreadable kind); `clkLoadNeedsYou_` gated on `!COMPACT_MODE` before any RPC; `clkNeedsYouGo_` routes via `fileMissingCalls_` (notes) or `enterTool`. The pending-Training extras card (`clkDashTrainingCard_` + its RPC + skeleton twin) retired; extras = [Spanish | Requests] or Requests alone (`.dash-pair-single`).
T2 | web-app/tc/script_clock.html | `.clk-state` row inside the gradient card (dot + label + `Nh Mm worked`) on `rgba(10,13,20,.72)` under `#fff` (V-2/INV-166); world-clock strip, its rotation, `clkShootMaybe_`, the greeting "On the clock" pill removed; `CLK_REGION_ZONES` → `CLK_TZ_ZONES` (tz select only); tick = `clkTickTime_ / clkApplySky_ / clkUpdateBreak_ / rollover / clkPeriodicReconcile_`.
T3 | web-app/tc/script_clock.html | `CLK_GREET_ROT.held = onClock`; `clkGreetRotTick_` returns before rotating when held (after the hover check). The plan's `.greet-held` indicator chip was NOT built.
T4 | web-app/tc/script_clock.html | Hours once: the status sentence is trimmed to since + until-end; the strip header keeps `% logged` + `Nm lunch`; `#clk-next-break` retired — `clkBreakScheduleHtml_` chips carry `data-start`/`data-end` and `clkUpdateBreak_` paints `taken` / `now` / `next` + "· in Nm" (≤90 min), no toast (INV-190). `clkBreakScheduleHtml_` resets `_clkLastBreakMin` so a same-minute re-render (focus → `clkRefreshState_`) never ships plain chips — found on camera.
T6 | web-app/tc/script_clock.html, web-app/styles.html | Rail order card → `.clk-actions-block` (pending chip + `renderActions`) → `.shift-strip`; `.actions` base = 2 columns with `.prime` spanning both, the 540px re-columning removed, the compact override reduced to `gap` (A2 clean); `:root[data-compact] #dash-needsyou { display:none }`. Fold MEASURED at 1440×900 with `test/visual/fold-measure.mjs`: prime-button top 704 → 367 (the reorder; the card is 18px shorter).
tests | web-app/Tests.js | `test_pendingTasks_requiresEmployeeAndShape` (bare `{error}` unregistered; registered caller gets `items[]` with routes, `unavailable[]`, `total`, `todayIso`, overdue-first). Editor suite 307 → 308.
tests | test/visual/mock.js, test/visual/shoot.mjs | `getMyPendingTasks` fixture (six kinds, three overdue, `unavailable:['sched']`) + `EMPTY_FIXTURES` twin; scenarios `clock-needsyou-empty-light-wide` (`?fixture=empty`) and `clock-needsyou-error-light-wide` (`?failrpc=`). Matrix 90 → 92.
tests | test/client/run.js | PR6-1 (server contract + behavioural helpers + routes vs the TOOLS registry), PR6-2 (client wiring + vm-driven render + fixture keys DERIVED from `items.push({` / `var result = {`), PR6-3 (rail order, scrim, literal colours, hours once, trimmed sentence, rotator-hold ordering, break-chip states + the guard reset, `.actions` base + no 540 re-columning); five pins REWRITTEN in place (L-35 shooting-star → the derived `PR6 (T2)` ban; INV-184 photo/moon + star; V-4; the skeleton pin; ADJ-2 anchor); `OWED` gains `getMyPendingTasks`; the `clkSkyFor_` edge pin walks sixteen hours. Pure 738 → 741.
tests | test/client/dom/runDom.js | Needs-you lifecycle on the booted Dashboard (precedes `#dash-cards`; skeleton → list → error → clean-empty → unavailable → freshness stamps → the hand-off LAST since it navigates). DOM 103 → 104.
docs | CLAUDE.md, .cycle/STATE.md | Dashboard KDD PR 6 amendment; Clock-view KDD (strip retired); INV-190 (chip ROW); S25 compact note; operator-state entry (no state; `runAllTests()` → 308); count chain; S101.

TEST RESULTS: pure harness 741 passed / 0 failed; DOM harness 104 passed / 0 failed; 15/15 mutations bit (5 server: clean-round guard, praise filter, `noteCountUnavailable` throw, unregistered tab, dropped catch-push; 9 client/styles: compact gate, degraded-fresh, clean-empty card, slot order, rotator hold, hours in sentence, scrim, shooting star re-added, `.prime` span; 1 DOM: degraded-fresh). Visual: the seven clock scenarios (light/dark/compact/mobile/pendingadj/empty/error) all `missing: []`, `overflowPx: 0`, no page errors, eyeballed — the first shoot found three defects (plain break chips, single-line Needs-you rows, "Past due" wording), all fixed and re-shot clean. Fold measured 704 → 367 before/after. Editor suite not executable off-editor (expects 308 at the operator's post-deploy run).
Regression scenarios walked: S39 (Clock view layout) PASS with the new order; S25 (compact/pop-out) PASS — the block hidden and no RPC; S76 (reminders) PASS — `clkUpdateBreak_` still paints only; S94 (done-state) PASS — the state line reads "Shift complete" beside the named clock-out; S101 written for the new surface (on camera; the live-time chip walk is manual).
REGRESSION RISKS: `clkUpdateBreak_`'s signature/contract unchanged for the shell ticker (paint-only); the removed `getMyTraining` extras call means the Dashboard no longer touches that endpoint (Needs you does, inside `getMyPendingTasks`); `.actions` callers unchanged (`renderActions` markup is the same — only the grid rules moved); `CLK_REGION_ZONES` had one consumer, renamed with it. An OLDER server behind the new client renders the Needs-you error card (never a fake all-clear); an older client on the new server ignores the endpoint.
INVARIANTS AT RISK: None violated. Checked: INV-129/156 (clean-only cache + freshness), INV-169 (cap + total), INV-173/174 (real links), INV-175/187 (error ≠ empty; unavailable named), INV-184 (retired selectors banned), INV-185 (fixture keys derived), INV-190 (paint-only break chips), INV-166 (literal colours on the fixed card), A2 (no orphan compact grid), C8 (park-and-consume hint), the cycle-8 M-12 compact RPC gate.
NET SCORE: 2 − 0 = +2 (production fixes: the hours figure rendered twice and the break schedule three times — interface defects per R18 — and the rotator sliding the status sentence away mid-shift; T1's Needs-you block and T6's reorder are capabilities; the compact-gate cost avoided is defensive). New failure modes: none found — the one candidate (the minute-guard) was fixed before it shipped.

OPERATOR ACTIONS / DEPLOY:
- None — no Script Property, trigger, migration, CONFIG value or OAuth scope. | BLOCKS DEPLOY: N
Deploy: Server + Client (Time Clock views) + Client (shell) + Test Suite: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy; then `runAllTests()` (expect 308).

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- §3d of the handoff (an undo label with a countdown on the last punch) is NOT built — the self-undo button + its 5-minute window are unchanged.
- The `.greet-held` indicator chip was deliberately dropped (a held rotator needs no badge); revisit only if reps ask why the greeting stopped rotating.
- The Needs-you list refreshes on the next Dashboard enter (SWR) — an action taken elsewhere (acknowledging, signing) is reflected on re-enter, not pushed; per-source invalidation would need the writers to bust the `pending_tasks_v1:` key.
- The pre-existing DOM flake (the resume-request test's `'00:01:00'` fixture in the minute after IST midnight) stands.
- Three inline `\n`→`<br>` sites outside `buildCallNoteEmailHtml_` still owe the `cnNlBr_` consolidation (carried from the NLBR round).

DOCUMENTATION UPDATES NEEDED:
- Done in this session: CLAUDE.md (Dashboard KDD PR 6 amendment, Clock-view KDD, INV-190, S25, operator-state entry, count chain, S101) and .cycle/STATE.md.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
