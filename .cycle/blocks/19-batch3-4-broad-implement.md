---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F3 (MAIL_BCC_ALL is surfaced — a standing PHI copy that appeared nowhere in the app), F5 (qaOptionIsNumeric_ accepts what Number() accepts), F7 (the PAP purple has one home — the --intake-pap token), F8 (a failed intake-feedback read is named, never rendered as "no feedback"), D1 (two documentation drifts)
Files modified: web-app/Code.js, web-app/cn/script_callnotes.html, web-app/intake/script_intake.html, web-app/styles_design_tokens.html, test/client/run.js, test/visual/mock.js, CLAUDE.md

CHANGES:
F3 | web-app/Code.js (mailBccStatus_ added; getStorageHealth returns mailBcc) ; web-app/cn/script_callnotes.html (cnHealthFindings_ mailBcc item + cnMailBccHtml_ in cnRenderStoragePanel_) ; test/client/run.js (new pin; PR2-3 + DRV-4 updated) ; test/visual/mock.js (both getStorageHealth fixtures) | `MAIL_BCC_ALL` is merged into EVERY email the app sends — intake PPD/PMD/PAP bodies carry full patient answers, department emails carry the patient name + TRX — and it appeared NOWHERE in the app: not Admin, not Storage Health, not Automation Health, not the feature-flag surface. Its documented purpose is transient ("set it while testing, clear it after"), which is exactly the setting most likely to be left on, and a silent standing PHI copy is the kind of thing an audit finds rather than an operator remembers. It now rides Storage Health as a read-only `mailBccStatus_` and surfaces twice from ONE derivation (INV-186's client half): unset is an `ok` FACT, set-and-internal is a standing `warn` naming the address and what rides in those copies, and an address outside the deploying account's own domain is `fail`/blocking. It REPORTS rather than enforces — silently dropping an address the operator deliberately typed would leave them believing they get copies they do not, a worse failure than the one being guarded (INV-187's direction). The org domain comes from `Session.getEffectiveUser()` — the account that owns every store and sends every message — so there is no second copy of the domain to drift from `doGet`'s own check; `external` is `null` when that account cannot be read (unknown, never "all clear"). The send path is untouched: `mailBccAll_` and `mailMergeBcc_` are byte-identical and the new function is a sibling inserted between them.
F5 | web-app/Code.js (qaOptionIsNumeric_) ; test/client/run.js (QA-24 grew cases + a regex ban) | The guard that keeps a QA dropdown option from being a bare number used a decimal regex while every consumer asks `Number(v)` — so `"4."`, `"0x5"` and `"1e0"` cleared the guard and parse as 4, 5 and 1. A dropdown option so named would fold its answers into the QA scale averages, which drive coverage `avg`, the calibration means and the exemption eligibility thresholds — the numbers a manager grants a rep an exemption from sampling on. The guard now asks `Number()` too, so the accept-set cannot be narrower than the parse it exists to protect. This is the validator-vs-consumer width class the audit's Stage 3 named; with F5 landed it has a known instance to derive a tripwire from.
F7 | web-app/styles_design_tokens.html (--intake-pap-soft, light + dark) ; web-app/intake/script_intake.html (.intk-cat-pap .sec-title, rail) ; web-app/cn/script_callnotes.html (.cn-intake-pill.pap) ; test/client/run.js (new pin) | The PAP purple bypassed `--intake-pap` in four separate literal values across two partials, plus a hand-written dark override — so the PAP category tint and the CN intake pill were a DIFFERENT purple from the PAP progress ring the tokens partial defines, and changing the colour took five edits in three files. Both rules ride the token now, with one `--intake-pap-soft` tint declared beside its source (the `--warn-soft` shape). The dark override is gone because the token already flips per mode — that override was the parallel-source half. MEASURED rather than reasoned, across every palette × mode: the token on its own tint is 4.61:1 light and ~6.9:1 dark, AA in all ten combinations; the pill's alpha moved .16 → .14 because .16 measured 4.47, a hair under AA for 11px text. A `--intake-pap-deep` alias was evaluated and REJECTED by measurement — the dark card-mix regressed 7.69 → 4.04.
F8 | web-app/Code.js (intakeFeedbackFor_ carries its outcome; intakeFeedbackResult_ added; intakeGetSubmission) ; web-app/intake/script_intake.html (the Sent detail's feedback branch) ; test/client/run.js (new pin; the "intake feedback loop" pin updated) | `intakeFeedbackFor_`'s catch returned `[]`, which is EXACTLY what a clean submission with no feedback returns — so an unreachable Intake store rendered the Sent detail as "nobody has said anything about this recommendation", a reading the data cannot support (INV-187). The read outcome now rides back additively as `feedbackUnavailable` and the client renders `errorStateHtml_` in that block instead (INV-175). Best-effort is preserved by construction: the rest of the submission detail still renders, and a submission with genuinely no feedback still renders NOTHING — an empty section would read as "no complaints yet", which is the decision the feedback KDD already made.
D1 | CLAUDE.md | Two drifts the audit noticed. (a) The visual-matrix running total read `99 → 101` where `shoot.mjs` runs 99 scenarios; corrected to `97 → 99` and annotated that the SCENARIOS list in `shoot.mjs` is the authority, because the hand-carried chain had drifted two high — the same reason the Visual Audit Stage already tells you to read the count off the run rather than from prose. (b) STATE.md's scenario list stopping at S103 while CLAUDE.md defines S104: resolved by construction when cycle 18's STATE block moved to HISTORY.md and STATE.md was reset for cycle 19 (HISTORY.md is append-only and never edited in place). The stale follow-on line describing it is retired in this session's checkpoint.

TEST RESULTS: passed.
  - Pure harness `node test/client/run.js`: 777 passed, 0 failed (774 at the Batch 1+2 baseline; +3 new named tests — F3, F7, F8. F5 grew QA-24 IN PLACE, and PR2-3 / DRV-4 / "intake feedback loop" were updated as test doubles of the changed code, not reactively).
  - DOM harness `node test/client/dom/runDom.js`: 108 passed, 0 failed (unchanged — no DOM-layer behaviour in this batch).
  - `node --check` clean on Code.js, Tests.js, DevTools.js.
  - Visual: `cn-log-light-wide`, `cn-log-dark-wide`, `intake-pap-light-wide`, `intake-dark-wide`, `admin-system-light-wide` re-shot — 0 overflowPx, 0 missing fixtures, no non-font console errors. `intake-pap-light-wide.png` read back by eye: the token purple throughout the PAP category headers and rail.
  - Read back from the LIVE DOM via a Playwright probe against the Admin System tab: the two capability lines render in order — `cn-drive-line cn-drive-ok :: Drive access granted…` then `cn-drive-line cn-drive-warn :: Every outgoing email is BCC'd to <address>. That includes intake bodies (full patient answers) and department emails (patient name + TRX)…`. F3's inventory line on the real page, not only in a fixture.
  - EVERY change bite-checked: 9 mutations, 9 bites (F3 finding severity flattened to ok / external escalation removed / the inventory line deleted; F5 the Number() half reverted / the regex ban made vacuous-checked against old-vs-new source; F7 a literal restored / the dark override re-added / the alpha moved back to .16; F8 the catch returning [] restored). Every bite ran against a COMMITTED tree, per the documented `git checkout` hazard.
  - Regression Scenarios (Test Command is `manual`) — walked below.

REGRESSION SCENARIOS (Subsystems touched: Server, Client (Call Notes views), Client (Intake views), Client (shell), Test Suite):
  Every scenario in this project targets the DEPLOYED Apps Script project with live spreadsheets, so none is executable in this container. Recorded PASS where a harness / the visual matrix / a live-DOM probe covers the same assertion, and NOT APPLICABLE otherwise — with the blast radius named so the post-deploy walk is scoped rather than exhaustive.
  - S97 (Admin → System is findings-first and reaches all-clear) | PASS (client half, live-DOM probe + pins) — F3's item derives from the ONE `cnHealthFindings_` function, so the Needs-attention list, the three Overview cards and the System badge cannot disagree; unset renders as an `ok` fact inside "checks passing", which is what keeps the tab reachable-green (INV-186). Server half: NOT APPLICABLE (live project).
  - S104 (Drive capability — the Admin signal) | PASS (ordering half) — the Drive line still renders BEFORE the mail line and above the inventory table; DRV-4's ordering assertion was loosened and given an explicit Drive-before-mail check rather than left to a regex that only worked when nothing sat between them.
  - S57 (Compliance audit panel) | NOT APPLICABLE — live project. Same partial, different Admin sub-tab; `getCallNotesAuditLog` is untouched.
  - S103 (QA Log — typed rubric, the manual fallback) | NOT APPLICABLE — live project. F5's blast radius is exactly its "a bare-number option is refused BY NAME" step; the accept-set is now wider in the refusing direction only (more strings refused, none newly accepted).
  - S90 (QA module), S100 (QA surface — coverage, exemptions) | NOT APPLICABLE — live project. Named because F5 protects the numbers they assert (scale averages → coverage avg → calibration → exemption eligibility), not because their steps change.
  - S59 (Intake PPD recommendation + send), S60 (Intake PMD/PAP account creation) | NOT APPLICABLE — live project. F7's blast radius (PAP category tint, section headers, rail) and F8's (the Sent detail's Recipient-feedback block). Neither changes an assertion: F7 is colour-only with the layout untouched, F8 adds a branch that a clean read never enters.
  - S87 (Intake Catalog) | NOT APPLICABLE — live project. Same partial; no PAP tint and no feedback block on that tab.
  - S18 (Call Notes submit / rolling stack) | NOT APPLICABLE — live project. Carries `.cn-intake-pill.pap` on an intake-logged note; F7 changes its two colour values and nothing else. On camera in `cn-log-{light,dark}-wide`.
  - S73 (phone-width layout of the intake grids) | PASS (measured) — `intake-pap-light-wide` and `intake-dark-wide` re-shot at 0 overflowPx. F7 touches colour only; no box model, no breakpoint.
  - S25 (compact pop-out), S45 (resizable sidebar), S54 (uiConfirm/uiPrompt) | NOT APPLICABLE — live project. `styles_design_tokens.html` gained ONE new token pair and changed nothing existing; the INV-128 token-hygiene tripwire is green, which is the assertion that matters for a token-file edit.
  - S19, S24, S55, S56, S99 and every other mail-sending scenario | NOT APPLICABLE — and deliberately OUTSIDE the blast radius: `mailBccAll_` and `mailMergeBcc_` are byte-identical (verified against the diff hunks), so F3 adds a reader and changes no send. The M-7 no-mail-in-lock tripwire and the one-seam pin are both still green.
  - S1 / S2 (smoke + full editor suite) | NOT APPLICABLE — editor-only. Scanned Tests.js for doubles of every changed area first: the only two hits are omnibus GATE cases (`saveQaScorecardCriteria` with a scale-only criterion, so `qaOptionIsNumeric_` is never reached; `getStorageHealth` called as a non-manager, so it returns the gate error before touching `mailBccStatus_`). Neither asserts a payload shape. Expect 308/308 unchanged post-deploy.
  - Every other scenario in the touched subsystems | NOT APPLICABLE — outside the blast radius (no shared code path with the four fixes).

REGRESSION RISKS:
  - F3 adds one `getProperty` + one `Session.getEffectiveUser()` per `getStorageHealth` call. That endpoint is admin-gated, manually opened, and already opens eight spreadsheets — the added cost is not measurable against it. The read is best-effort try/catch'd, so a failure yields no `mailBcc` field and the client renders nothing (deploy skew is safe in both directions: an older client ignores the field, an older server makes the line absent rather than wrong).
  - F3 makes an existing PHI-routing configuration VISIBLE for the first time. On a deployment where `MAIL_BCC_ALL` is set, the Admin System tab will acquire a standing warn item that was not there yesterday, and the System badge count goes up by one. That is the finding, not a regression — but an operator should not be surprised by it.
  - F5 only ever REFUSES more strings than before; nothing previously refused is now accepted. The one behaviour change an admin can see is that `"4."` / `"1e0"` / `"0x5"` are now rejected by name as dropdown options. No stored criteria blob changes, and `qaCriteriaSanitize_`'s lenient read path is untouched.
  - F7 is colour-only. Measured AA across all ten palette × mode combinations before landing, and the two consumers now agree with the ring they always should have matched. The removed dark override is the only deletion; the token's own dark value replaces it, verified by measurement rather than by reading.
  - F8 widens `intakeGetSubmission`'s return by one additive boolean. `feedback` keeps its exact meaning and shape, so a not-yet-redeployed client renders precisely as before. The failure branch is only reachable when the read actually throws.
  - Old behaviour better anywhere? No case found. F3/F8 are additive; F5 narrows an accept-set toward its consumer; F7 removes duplicates of a value that already had a canonical home.

INVARIANTS AT RISK: None violated. Five touched, all in the strengthening direction:
  - INV-187 (carry the read outcome): F8 is a direct application — a failed read is named rather than rendered as an absence. F3 applies its DIRECTION too: an off-domain BCC is reported, not silently dropped, because a silent drop leaves the operator believing in copies they are not getting.
  - INV-186 (an indicator that can never be clean): F3's unset state is an `ok` FACT inside "checks passing", never a warning, so the System tab still reaches "Nothing needs attention" on a deployment that does not use the property.
  - INV-175 (a load failure renders errorStateHtml_, never an empty state): F8's client half; the A12 derived scan covers the new call site and is green.
  - INV-128 (design-token hygiene) / the V-1 `-deep` family: F7 adds `--intake-pap-soft` in BOTH mode blocks beside its source token; the tripwire is green and no `-deep` alias was introduced (one was evaluated and rejected by measurement).
  - INV-185 (fixtures mirror the server): both `getStorageHealth` fixtures gained `mailBcc` in `mailBccStatus_`'s exact shape — populated in the scenario twin, unset in the all-clear twin.
  - The mail seam's one-send-path pin and M-7 (no mail inside the lock) are untouched and green — F3 reads the property, it does not send.

NET SCORE: 0 − 0 = 0
  a) Would it have fired in production THIS MONTH?
     F3 NO (and a correction worth recording): mid-session I read `robin@umsupply.com` out of a live-DOM probe and briefly took it as evidence the property is SET on this deployment. It is not evidence — it is the value I authored in `test/visual/mock.js`, echoed back through the fixture-driven harness page. NOTHING in this container can read the deployed Script Properties, so whether `MAIL_BCC_ALL` is set right now is UNKNOWN from here. What the fix closes is a visibility gap that exists either way; the operator can settle the live question in one look at the Admin System tab after deploying.
     F5 NO — reaching it needs an admin to type a dropdown option spelled `"4."`, `"1e0"` or `"0x5"`. A latent width mismatch between a validator and its consumer, not a live failure.
     F7 NO — cosmetic and structural. The wrong purple was visible every day, but nothing computed wrongly and no contrast bound was breached (the .16 pill at 4.47 was under AA, which is the closest thing to a live defect here, and it is a hair).
     F8 NO — needs the Intake store to be unreachable while a rep opens a Sent submission. Real, and the failure is confidently wrong when it happens, but not something I can claim fired this month.
     D1 N/A — documentation.
  b) New failure mode introduced? F3 NO. F5 NO. F7 NO. F8 NO. D1 NO.
  Scored strictly per the template's question, as the cycle-17/18 reflections require: this is a batch of four structural/visibility fixes with no live failure among them. Net 0. That is the honest number for a batch the audit itself ranked below Batches 1–2 on production impact — F3 is a Medium because of what it GUARDS (a standing PHI copy), not because it was firing.

OPERATOR ACTIONS / DEPLOY:
- None new — no Script Properties, triggers, migrations, CONFIG values or OAuth scopes. | BLOCKS DEPLOY: N
- After deploying, open Manage → Admin → System and read the new mail line. If it reads "Every outgoing email is BCC'd to <address>", decide whether those copies are still wanted and clear `MAIL_BCC_ALL` if not — that is the whole point of the finding, and it is the only way to learn the live answer. | BLOCKS DEPLOY: N
- Post-deploy: run `runAllTests()` — expect 308/308, unchanged (no Tests.js change in this batch). | BLOCKS DEPLOY: N
Deploy: Server + all client partials ship together — `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. This rides the SAME deploy already owed for PRs #230/#232/#233 and for Batches 1+2; nothing here needs a separate one. Test Suite changes are local-only and never reach the Apps Script project.

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- The two tripwire promotions the audit's Stage 3 flagged are now cheap, because each has a landed instance to derive from rather than a guess: (a) a validator-vs-consumer WIDTH scan — a guard that decides whether a string is numeric must not be narrower than the parse its consumers run (F5's exact shape); (b) a colour-literal-vs-token scan over the scanned partials for values that duplicate a token the tokens partial already defines (F7's exact shape). Neither is written — a rule with one instance is worth deriving, but it is its own batch.
- `mailBccStatus_` reports; it does not enforce. If an operator ever wants an off-domain BCC BLOCKED rather than flagged, that is a deliberate policy change to `mailMergeBcc_` and an operator decision, not a defect.
- Everything the Batch 1+2 block logged as a follow-on is still open (the `reportBreakPairingChanges()` twin, the sheet doctor's blindness to F1's shape, F4's 120s dept-request residual, `submitCallNote` deliberately not hooked into F4).
- The audit's Deferred set is untouched and remains operator/feature decisions: agent-visible QA reviews, the blocked external form route, a retention tier for `ViewUsage`/`ClientErrors`, a per-rep working-days source, a manager pay-statement export.

DOCUMENTATION UPDATES NEEDED:
- The Storage Health Key Design Decision + INV-197's neighbourhood: the panel now leads with TWO capability lines (Drive, then mail routing) above the inventory table, for the same reason — neither is a spreadsheet, so no store row can carry them. Say that the mail line is derived from `mailBccStatus_` and rides `cnHealthFindings_` like Drive does.
- The `MAIL_BCC_ALL` operator-state entry: add that it is now VISIBLE in Admin → System, that an off-domain address reads as blocking, and that the report never changes what is sent.
- INV-186: F3's unset-is-an-ok-fact is a clean second example of the client-half rule; worth naming beside the Drive one.
- The Intake recommendation-feedback Key Design Decision: it says an empty list renders NOTHING. Add the third state — a FAILED read renders the error card, and why the two must not look alike.
- The QA typed-criteria note (INV-196 (k)): "the editor REFUSES a bare-number option by name" should say the guard asks `Number()`, so the next reader does not re-narrow it to a regex.
- The `--intake-pap` token: record that `--intake-pap-soft` is its tint and that the PAP pill/category tint ride the token, so a future palette change is one edit.
- Test-count lines: pure harness 774 → 777; DOM stays 108; visual matrix stays 99 (and the count is now annotated as read-off-the-run).
- The seven updates the Batch 1+2 block listed are still owed — `/sync-docs` should carry both sets in one pass.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
