---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F-14 — the boot-failure Retry called `location.reload()`, which reloads the sandboxed HtmlService iframe (g69) and renders blank. F-12 — the Admin KPI strip captioned the CALLER'S OWN `getCallNotesAmbient` counts "across team". F-40 — five static modals opened and closed by `classList`, bypassing the focus stash and restore every other overlay has. F-30 — the Call Notes shortcuts overlay was opened by hand: no accessible name, no focus lifecycle, outside the shared Escape handler. F-29 — one input carried two `aria-label` attributes, and the Q&A thread labelled the REP's entries "You" while a manager read them. F-42 — the public form's five accordion toggles exposed no state. F-43 — the Reference drawer had no dialog role, no name, and never handed focus back. F-36 — the ADP export's `window.open` ran after an async RPC, so a pop-up blocker swallowed the generated sheet's URL with the dialog already closed. F-37 — `window.open` returns NULL when blocked rather than throwing, so the pop-out's "blocked" toast was unreachable. F-28 — the quick-chip row said "this week" over all-time counts.
Files modified: web-app/script_core.html, web-app/modals.html, web-app/cn/script_callnotes.html, web-app/tc/script_clock.html, web-app/tc/script_manager.html, web-app/tc/script_timeoff.html, web-app/kb/script_kb.html, web-app/form_public.html, test/client/run.js, test/visual/mock.js, CLAUDE.md (generated block), .cycle/STATE.md

Estimate: M (~6 h) — recorded 2026-09-18 in STATE.md BEFORE the first edit
Actual: ~2.5 h

CHANGES:
F-14 | web-app/script_core.html | `renderError`'s Retry button calls `reloadApp_()`. This is the BOOT failure surface — the Retry most likely to be pressed, on the one screen where the app has nothing else to offer — and `location.reload()` reloaded the iframe, whose URL is session-bound and paints blank (g69). The finding also asked for a tripwire: the pin now allows `location.reload()` ONLY inside `reloadApp_`'s two documented fallbacks (not framed / no server URL, and the final escape hatch) and bans it across `script_core` and eight partials.
F-37 | web-app/script_core.html | `popOutCurrentView` captures `window.open`'s return. A blocked pop-up returns null WITHOUT throwing, so the `catch` that showed "Pop-out blocked by browser" could never run and a rep whose browser blocked the window got no feedback at all. A null window now toasts with the remedy ("allow pop-ups for this site") and returns before `focus()`.
F-12 | web-app/cn/script_callnotes.html, test/visual/mock.js | The strip's first two cells read cross-rep sources: `getCallNotesTagTaxonomy.totalNotes` (every enrolled rep's notes) and `managerGetUnresolvedActionCount` (the 2-min-cached cross-rep walk behind the Team Notes badge) — a FOURTH parallel fetch, so `maybeRender` waits on four. A partial walk renders `≥ N` and names why (INV-187: a walk that could not read every Sheet is a lower bound); an unreadable one is a dash plus "could not be read", never a zero; a real zero still renders 0. Each cell's sub-line states its OWN scope rather than the strip implying one. FIXTURE GAP found by the shoot: the server has always returned `totalNotes` and the visual fixture omitted it, so the Notes cell photographed as a dash — added, and pinned (INV-185).
F-40 | web-app/tc/script_clock.html, web-app/tc/script_manager.html, web-app/tc/script_timeoff.html | `adjust-overlay`, `export-overlay`, `mgr-timeoff-overlay`, `day-edit-overlay` and `day-overlay` open through `ensureOverlay(id)` and close through `closeOverlay(el)` on EVERY path (cancel, backdrop, Escape, post-submit) — thirteen bare `classList` calls in all. They keep their existing `role`/`aria-modal`/`aria-labelledby` from `modals.html` (ensureOverlay only writes those when told to), and gain the focus stash on open plus the restore to the trigger on close. `day-overlay` doubles as the hover POPOVER, and `ensureOverlay` rewrites `className`, so the `hover-mode` class is read before the call and carried back through `extraClass` — without it a hover preview would gain a backdrop and steal focus.
F-30 | web-app/cn/script_callnotes.html | `cnOpenShortcutsOverlay_` goes through `ensureOverlay('cn-shortcuts-overlay', { labelledBy: 'cn-shortcuts-title' })` (the heading gained that id, so the name and the heading cannot drift); both close paths — the X and the "take the tour" link — go through `closeOverlay`; the X gained an `aria-label`.
F-29 | web-app/cn/script_callnotes.html | The general manager-comment input carried `aria-label="Reply to this training question"` AND `aria-label="Add a comment for this rep"`; two on one element is invalid and which wins is the parser's business. The wrong one is gone. `cnRenderQAThread_` labels an agent entry "Rep" when `readonly` (the manager's read-only card) and "You" only on the rep's own view — it had been telling a manager they wrote the rep's question.
F-42 | web-app/form_public.html | All five accordion toggles ship `aria-expanded="false"` and `aria-controls="acc-<key>"`; each body carries the matching id; `wireAccordions` sets `aria-expanded` from the SAME `!isOpen` it toggles the class with (never a second read of the DOM).
F-43 | web-app/kb/script_kb.html | The drawer is `role="dialog"` named by its own heading (`aria-labelledby="kbd-title"`). Deliberately NOT `aria-modal`: it does not trap focus and the shell's trap exempts it by design, so claiming modality would be the kind of lie this a11y family exists to remove. `kbDrawerOpen_` stashes the trigger on a CLOSED→open transition only (a re-open must not overwrite it with the drawer's own search box); `kbDrawerClose_` restores it exactly once and only when the drawer was actually open (close runs on every navigation).
F-36 | web-app/tc/script_manager.html, web-app/modals.html | A new `#exp-result` slot above the export dialog's footer receives the generated sheet's link FIRST; the dialog stays OPEN on success (closing it is what lost the URL); then `window.open` is attempted as the convenience it is — success toasts as before, a blocked tab toasts a WARNING pointing at the link. The slot is cleared on each open so a previous run's link never carries into a new one.
F-28 | web-app/cn/script_callnotes.html | The label reads "Open in History · all time". The counts span the rep's whole Sheet by design (INV-39, and the server comment says so), so the label moved to the counts rather than the counts to the label — a weekly count here would contradict the same numbers everywhere else they appear.
Pins | test/client/run.js | Eight new, one per finding or pair: F-14/F-37 (the Retry's call, the `location.reload()` budget of exactly two inside `reloadApp_` and zero elsewhere across nine files, the pop-out's null check and its ordering before `focus()`); F-40/F-30 (all six overlays open through `ensureOverlay` and close through `closeOverlay`, no bare `classList` survives for any of them, the `hover-mode` carry, `closeDayPopover_`, the shortcuts heading id and both close paths); F-12 (BEHAVIOURAL — `cnRenderAdminAugmentHtml_` driven with the admin's own counts present and asserted to reach no cell, the team-wide values, the crit band, the `≥` lower bound, the unreadable and null cases, a real zero, the four-source loader, the fixture); F-29 (a regex sweep for any input with two `aria-label`s, plus the Rep/You rule and the flag that drives it); F-42 (five toggles with the aria pair, five bodies with the id, the handler's single source of truth); F-43 (role, name, the absence of `aria-modal`, the closed→open stash and the once-only restore); F-36 (the link renders before the blockable call, the escaped href with `rel=noopener`, no close on success, the warn/success split, the clear-on-open, the slot's position); F-28 (the label, and that the counts are untouched). One existing pin repointed: the D1 modal-ordering pin now follows `closeOverlay` rather than the bare `classList.remove` it replaced.

TEST RESULTS: passed. Pure harness 898/898 (was 890 — eight new pins), DOM harness 126/126 (unchanged), `lint-server` clean (16 files), `split-manifest --check` current (no server file changed), `counts --check` green. Editor suite NOT RUN HERE (editor-only); registrations unchanged at 337 — no server function changed. Visual matrix: `admin` (16 scenarios), `clock` (10) and `reference` (6) re-shot after the fixture fix — 0 missing fixtures, 0 overflow; the Admin Overview strip now photographs "NOTES 1284 · across team · all time" beside "UNRESOLVED 1 · action flags open across team". `a11y-names.mjs`: all twelve surfaces report `unnamed 0`.
Bite-checks — THIRTEEN mutations, THIRTEEN bite (committed file mutated, the named pin went red, file restored): B1 the Retry reloads the iframe again · B2 `reloadApp_` loses a fallback (the budget assert) · B3 the pop-out's null check disabled · B4 the adjust modal opens by hand again · B5 the `hover-mode` carry removed · B6 the shortcuts overlay opens by hand again · B7 the Notes cell reads the admin's own count again · B8 a partial walk hidden · B9 the manager sees "You" again · B10 the chip label says "this week" again · B11 `aria-expanded` never updated · B12 the drawer claims `aria-modal` · B13 a successful export closes the dialog again.
Regression Scenarios (Test Command is `manual` — no editor or browser here):
- S89 Every form control announces a name | Client (all view partials), Client (public forms) | PASS — `a11y-names.mjs` re-run over all twelve surfaces including the three public forms: `unnamed 0` everywhere. The shortcuts X and the accordion toggles were in scope of this batch.
- S51 Call Notes Admin tab augment | Server + Client (Call Notes) | NOT RUN HERE — browser; ON CAMERA by fixture: the strip's four cells, their new labels and the team-wide values. Walk the partial state once (un-share one rep's Notes Sheet): Unresolved should read "≥ N · a rep Sheet was unreadable".
- S97 Admin → System findings-first | Client (Call Notes) | PASS by fixture — unchanged by this batch; re-shot clean alongside the Overview.
- S25 Compact mode + per-tool pop-out · S78 Pop-out header strip | Client (shell) | NOT RUN HERE — browser. New step worth one walk: BLOCK pop-ups for the site, press Pop out — the toast now says so and names the remedy (it was silent).
- S94 Done-state names its evidence; the update prompt actually reloads | Client (Time Clock), Client (shell) | NOT RUN HERE — browser. Same family as F-14; the deploy-beacon Reload already used `reloadApp_`, and the boot Retry now matches it.
- S47 Hover-triggered day modal | Client (Time Clock) | NOT RUN HERE — browser. The one scenario this batch could plausibly regress (the `hover-mode` carry): hover a calendar cell — the popover must stay tethered and transparent-backdropped and must NOT steal focus; click one — it becomes the centred modal and DOES take focus, and Escape hands focus back to the cell.
- S8 ADP-format export (manual range) | Server | NOT RUN HERE — editor/browser. The export ITSELF is unchanged; the dialog now stays open with a link. Walk it once with pop-ups blocked.
- S29 Call Notes bulk export | Server, Client (Call Notes) | NOT APPLICABLE — `cn-export-overlay` was not in F-40's scope and is untouched (see FOLLOW-ON).
- S61 Fillable form — consent stored, tamper-evident | Server, Client (public forms) | NOT RUN HERE — browser. Only the accordions' aria attributes changed; no field, value or submit path was touched.
- S64 KB reference drawer — mid-call lookup | Server, Client (Reference), Client (shell), Client (Call Notes) | NOT RUN HERE — browser. New step: open the drawer with Ctrl/⌘+K from inside the Issue field and close it with Escape — focus returns to the Issue field, not to the top of the page.
- S113 Call Notes modals open AND close | Client (Call Notes) | NOT APPLICABLE — the sched/scratchpad modals are dynamic (`ensureOverlay` already) and untouched; the shortcuts overlay joined them and is pinned the same way.
- S53 Tag taxonomy admin actions | Server + Client (Call Notes) | NOT APPLICABLE — the tag table is untouched; only the KPI strip above it changed.

REGRESSION RISKS:
- F-40's `ensureOverlay` REWRITES `className` on every open. The five static modals carry only `overlay`, so nothing else is lost — except `day-overlay`'s `hover-mode`, which is explicitly carried and pinned. Any future class added to one of these five must be carried the same way.
- `ensureOverlay` also moves focus INTO the dialog on a closed→open transition (deferred to after the caller's mount). For the four true modals that is the fix; for `day-overlay` in hover mode it is suppressed by the existing `hover-mode` guard in `ensureOverlay`.
- F-12 adds a cross-rep RPC to the Admin tab's load. It is the same walk the Team Notes landing already runs and is 2-minute cached, and the tab already runs a full taxonomy scan across every rep, so the marginal cost is small — but it is a fourth round trip before the strip paints, and `maybeRender` now waits for all four (a failure still counts toward `done`, so one dead RPC cannot hang the strip).
- F-12 changes what the first cell MEANS: all-time team notes rather than the admin's own week. A reader comparing the number to last week's screenshot will see a jump; the sub-line says why.
- F-36 leaves the export dialog OPEN on success, so the manager must close it. That is the point (the link lives there), but it is a behaviour change on a path that used to self-dismiss.
- F-43's focus restore fires on `kbDrawerClose_`, which is also called on every navigation (`kbDrawerOnNavigate_`). It is guarded on `wasOpen` and clears the stash, so a navigation with the drawer shut restores nothing.
- F-29's "Rep" depends on `readonly` meaning "a manager is reading". That is its only caller today (`{ readonly: true }` from the manager card); a future read-only rep-facing use would need a distinct flag.

INVARIANTS AT RISK: none violated. INV-145 (a close hook may refuse — none of these five registers a hook, so `closeOverlay` takes the plain-hide path and then restores focus), INV-173/174 (the disclosure/button contract — the accordion toggles now satisfy the exposed-state half), INV-182 (the `mtRenderTable_` detail-row contract is untouched), INV-186 (nothing new nags a clean deployment: the strip's new cells report facts, and the "could not be read" state needs a real failure), INV-187 (the partial cross-rep walk is a lower bound that says so), INV-39 (the chip counts still span the whole Sheet — the label moved, not the numbers), INV-185 (the fixture gained the field the server always returned), g69 (now enforced by a tripwire rather than convention), g49 (no `data-*` value is re-rendered raw — the new attributes feed `aria-controls` and a function argument).

NET SCORE: 6 − 0 = 6
  a) Would this have fired in production this month? F-14 YES (the boot Retry is pressed exactly when the app failed to load, and it produced a blank iframe — a user-visible dead end on the least forgiving screen). F-12 YES (an admin opening the tab read their own quiet week as the whole team's, every time). F-40 YES (five modals a manager opens daily; the interface defect is a keyboard user's focus landing at the top of the page after every close, which fires on every open). F-30 YES (same, plus a dialog announcing as "dialog" with no name). F-29 YES (a manager reading any training thread was told they wrote the rep's question; the duplicate aria-label is invalid markup on a live control). F-42 YES (five collapsed sections on the PUBLIC form with no exposed state — the one surface reached by people outside the org). F-43 NO-as-observed (the drawer is usable; the missing name and focus restore are real a11y defects but the scan rated it Low and no user report exists — counted defensive). F-36 NO-as-observed (needs a pop-up blocker; counted defensive, though the loss is total when it fires). F-37 NO (the toast was unreachable, so nothing user-visible changed until a block occurs — defensive). F-28 NO (a label that overstated recency; copy — defensive/interface, and I have scored it defensive rather than claim a sixth interface fix).
  b) New failure mode? NO for all ten. The one behaviour a reader should know: the export dialog no longer self-dismisses on success.

OPERATOR ACTIONS / DEPLOY:
- `clasp push -f` + cut a New version. | BLOCKS DEPLOY: Y
- `runSmokeTests` on prod — registrations still 337; read the count off the run. | BLOCKS DEPLOY: N
- Walk S47 once (hover a calendar day, then click one) — the hover popover must stay tethered and not steal focus. It is the one surface this batch could plausibly regress. | BLOCKS DEPLOY: N
- Generate an ADP export with pop-ups BLOCKED: the dialog stays open with an "Open the export sheet" link. | BLOCKS DEPLOY: N
- The Admin Overview's first cell now reads team-wide all-time notes, not your week — expect a much larger number. | BLOCKS DEPLOY: N
Deploy: Client (shell, Time Clock views, Call Notes views, Reference views, public forms) — `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. No server file changed in this batch.

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- `cn-export-overlay` (the Call Notes bulk-export modal, `cn/script_callnotes.html`) is a SIXTH static modal with exactly F-40's defect — opened by `classList.add('open')`, closed by `classList.remove('open')` in three places. It was not in the finding's list of five, so it is untouched; it is a five-line change of the same shape and should ride the next batch. Its export also does the F-36 `window.open` after an async RPC.
- `train/script_training.html` and `train/script_empdocs.html` close overlays with `classList.remove('open')` + `innerHTML = ''` in four places. Those are dynamic overlays (created via `ensureOverlay`), so they have the stash but skip `closeOverlay`'s restore — the same half-fix, out of scope here.
- The pop-out's doc comment claims "a repeat click on the same tool's pop-out just focuses that tool's window". `window.open` on an existing named window does not reliably focus it; F-37 added `win.focus()`, which makes the claim true where the browser allows it, but the claim is still stronger than the guarantee.
- `ensureOverlay`'s `className` rewrite is a trap for any static modal carrying a second class (F-40 hit it once, on `day-overlay`). A variant that preserves unknown classes — or a pin banning extra classes on the six static overlays — would close it properly.
- The F-12 strip fetches four sources serially-in-parallel and paints only when all four land; a slow cross-rep walk now delays the whole strip, where before it delayed three cells. A per-cell skeleton would be better than the all-or-nothing gate.

DOCUMENTATION UPDATES NEEDED:
- `docs/gotchas.md` + CLAUDE.md index: g69 amended (the tripwire exists now — `location.reload()` is allowed only inside `reloadApp_`, and the boot Retry was the last offender); a new gotcha for "a blocked `window.open` returns NULL rather than throwing, so a `catch` can never see it" (F-37) and for "`ensureOverlay` rewrites `className`, so a static modal's second class must be carried" (F-40); g100 amended (every static modal is on the lifecycle now, and closing goes through `closeOverlay` for the focus restore).
- `.cycle/config.md`: an invariant for "every overlay opens through `ensureOverlay` and closes through `closeOverlay`" (the six static ones are the population, `cn-export-overlay` the known exception); INV-187 amended with the Admin strip's lower-bound rendering; scenario steps for S47 (hover-mode survives), S51 (the strip's scopes and the partial state), S64 (focus returns to the Issue field), S25 (the blocked pop-out toast), S8 (the export link with pop-ups blocked).
- `docs/design-decisions.md`: the Admin-tab KPI decision (the strip reports team scope, and each cell states its own), and the modal/overlay accessibility entry (the focus lifecycle now covers the static modals).
- `docs/modules.md`: Call Notes (the Admin strip's sources), Time Clock (the modals' focus behaviour, the export link), Reference (the drawer's dialog role and focus restore).
- `docs/operator-log.md`: a Batch 6 entry. `docs/test-harness-log.md`: the eight pins, the `a11y-names` re-run, and the fixture gap the shoot exposed (`totalNotes`).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
