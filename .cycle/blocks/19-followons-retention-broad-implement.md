---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: RT (the diagnostics retention tier — the audit's deferred "retention tier for ViewUsage/ClientErrors", cycle-18 F11), BP (the two break-pairing suggestion follow-ons — `reportBreakPairingChanges()` + the sheet doctor sees the shape F1 survives), SA (the scheduled Spanish auto-assign trigger — operator note 4's "might follow"), TW (the two Stage-3 tripwire promotions — the F5 numeric-guard width scan and the F7 colour-literal-vs-token scan), DR (the bounded `getDeptRequestDetail` read — the C-N6 follow-on)
Files modified: web-app/Code.js, web-app/Tests.js, web-app/cn/script_callnotes.html, web-app/tc/script_manager.html, web-app/qa/script_qa.html, web-app/train/script_empdocs.html, test/client/run.js, test/visual/mock.js, .cycle/STATE.md

CHANGES:
RT | web-app/Code.js (CONFIG `VIEW_USAGE_RETENTION_DAYS` / `CLIENT_ERR_RETENTION_DAYS` = 0; `retentionWindowDays_`, `viewUsageRetentionDays_`, `clientErrRetentionDays_`, `diagRetentionText_`, `diagTsMs_`, `contiguousRowRunsDesc_`, `diagPurgeTab_`, `purgeOldDiagnostics`; `AUTOMATION_AUDIT_ACTIONS` + an `AUTOMATION_JOB_CHECKS` row; TARGETS ×2 + the 2am trigger; `getRetentionConfig`/`saveRetentionConfig` carry the two windows; Storage Health's ADP row names them) ; web-app/cn/script_callnotes.html (`CN_HEALTH_RUN_LABELS.DiagnosticsPurge`; the Retention panel's "Diagnostics tabs (PHI-free)" rows; the save sends each key only when its row rendered) ; web-app/Tests.js (`test_triggerGate_diagnosticsPurge_nonManagerThrows`) ; test/client/run.js (RT-1) ; test/visual/mock.js (retention + storage fixtures) | `ViewUsage` and `ClientErrors` were the only two stores with NO retention tier at all (cycle-18 F11) — "trim manually" was an operator obligation nobody was reminded of. `purgeOldDiagnostics` is trigger #20 (daily manager-tz 2am beside the other purges): INV-44 gate, INV-01 lock, BOTH windows default 0 so installing it changes nothing, Script Property first then CONFIG, a timestamp that cannot be parsed is NEVER deleted (fail-safe), rows are deleted as CONTIGUOUS bottom-up `deleteRows` runs under a 2000-row per-run budget (the ONE project lock — row-by-row deletes starve punches, INV-153/INV-159's reasoning) with the spare-row guard against Sheets' "cannot delete all non-frozen rows" refusal, and the counts-only `DiagnosticsPurge` audit row is the job-liveness heartbeat, checked only while a window is set (INV-186 in code). A failed run is stamped into `AUTOMATION_LAST_ERRORS` and cleared on the next clean one (the F4 rule). Admin → Config → Retention gains the two windows (danger-confirmed like the CN purges); the client sends each key ONLY when its row rendered, so an older client can never reset a window to 0. Storage Health's ADP row says what the live windows are.
BP | web-app/Code.js (`tsPunchDaysWithArchive_` extracted from `reportMultiBreakDays`; `breakPairsPositional_`; `reportBreakPairingChanges`; `tsDoctorLegitBreaks_` widened; `getTimesheetDoctor` `unpaired`/`totalUnpaired`/`truncated`) ; web-app/tc/script_manager.html (`loadSheetDoctor_` all-clear + summary; `renderSheetDoctorCard_` "Unpaired break stamps") ; test/client/run.js (A5 rewritten onto the reader, A2/A3 extended, BP-1..BP-4) ; test/visual/mock.js (`getTimesheetDoctor` fixture fields) | Two logged follow-ons of cycle-19 F1. (a) `reportBreakPairingChanges()` — the twin of `reportMultiBreakDays()`: manager-gated, read-only, lists every completed rep-day whose deducted break minutes DIFFER between the pre-F1 positional pairing and the greedy one. The old figure is `new + diffMin/60` from ONE `calcHours_` call plus the verbatim pre-F1 loop kept as `breakPairsPositional_` (defined once, called once, from the report — nothing else may reach for it), never a second arithmetic. Both reports now read the sheet through ONE `tsPunchDaysWithArchive_` (live + archive, a row in both tabs counted once) so a third report cannot walk it a third way. (b) The doctor's collapse guard widened from "equal counts" to "two-plus stamps of BOTH lunch types": the equal-count day leaves [12:00, 17:00] / returns [11:00, 19:00] read as legal while pairing into one 7-hour break, and a double-punched leave on a two-break day (counts UNEQUAL) was a duplicate group whose collapse would have kept the LAST leave and deleted a real one. Such days are now REPORTED (`unpaired` — the dropped stamps NAMED, the pairing greedy produced shown beside them, one finding per day, capped + `truncated`, INV-169) and never touched by the collapse; the doctor never picks which half is real — Day Edit does. The count-disagreeing one-leave/two-return shape stays the classic duplicate group and is never double-reported.
SA | web-app/Code.js (`spanishAutoAssign` FEATURE_FLAGS entry, server scope, default OFF; `SPANISH_AUTO_ASSIGN_DAYS`; `autoAssignSpanishThreadsScheduled`; `DIGEST_STALE_HOURS.spanishAutoAssign: 2` + the reported digest set DERIVED from the map; TARGETS ×2 + the hourly trigger) ; web-app/cn/script_callnotes.html (`CN_DIGEST_LABELS_` + `DIGEST_LABELS`) ; web-app/Tests.js (`test_triggerGate_spanishAutoAssign_nonManagerThrows`) ; test/client/run.js (SA-1) ; test/visual/mock.js (both Automation Health digest fixtures) | Operator note 4's "a scheduled trigger might follow". Trigger #21 runs the SAME `spanishAutoAssignCore_` as the manager button — one scope rule, one voicemail fold, one picker, one claim-row shape — behind a server-scope flag that defaults OFF (the managerDailyBrief posture), so a fresh deploy is a behavioural no-op. It heartbeats BEFORE the flag check (INV-151 — liveness observable while off), acts ONLY inside business hours through `businessMinutesBetween_` (the one definition of a working hour; a null window reads as NOT inside), takes the installer's roster row as the actor with the SYSTEM placeholder as the fallback (the reconcile precedent), and stamps a refused run (no members configured, a Gmail read that threw) into `AUTOMATION_LAST_ERRORS`. The reported digest-heartbeat set is now `Object.keys(DIGEST_STALE_HOURS)` (INV-179) — a heartbeat with a window but no row would otherwise be stamped and never read.
TW | test/client/run.js (TW-A, TW-B) ; web-app/qa/script_qa.html + web-app/train/script_empdocs.html (three canvas fallback hexes aligned to their tokens) | TW-A: every function whose NAME says it decides numeric-ness must decide via `isFinite(Number(…))` and carry no digit regex — the F5 shape, derived over Code.js + the scanned partials, ≥1 match required so the derivation cannot go empty by accident. TW-B: a hex literal in a scanned partial that EQUALS a value the tokens partial declares is banned outside two named categories — canvas fallbacks (`getPropertyValue('--t') || '#hex'`, which must EQUAL that token's Console-light value so a palette change fails here until the fallback moves) and INV-166 freezes named with their reason (`.instance-banner`, exactly once, inside its selector) — and every other chromatic literal (hex/rgb/hsl, neutrals and scrims excluded) is a two-sided per-file RATCHET with the reason recorded beside each baseline (the A14 shape). Writing it found the three canvas fallbacks STALE: the qa waveform fell back to `#0b6e40` for `--accent` (that is `--accent-2`'s value), the EmpDocs signature pad to `#101418` for `--ink` and the qa muted to `#9aa3b2` — values no token declares. Aligned to `#0f8a52` / `#0f1623` / `#a5acb8`. form_public.html is excluded (the INV-128 standalone-palette exemption).
DR | web-app/Code.js (`drFindRowByReqId_`; `getDeptRequestDetail` routes through it) ; test/client/run.js (DR-1) | The detail read fires per Expand click on a rep-facing list and read the whole `DeptRequests` tab — every column, every request's PatientTrx cell included — to find one row. `drFindRowByReqId_` scans ONLY the RequestId column then fetches ONE row at `DR_HEADERS` width (the `findFormTokenRow_` / `findCallNoteRow_` shape; a blank id costs no read, a miss costs the column read). The employee gate, `drCanAct_` on the row and the single not-found for unknown + out-of-scope are byte-unchanged (the C-N6 pin holds).

TEST RESULTS: passed.
  - Pure harness `node test/client/run.js`: 796 passed, 0 failed (785 at the operator-round baseline; +11: RT-1, BP-1..4, SA-1, TW-A, TW-B, DR-1, plus ONE auto-generated by the derived trigger-wiring/gate-type nets when `autoAssignSpanishThreadsScheduled` entered the TARGETS arrays and ONE when `purgeOldDiagnostics` did — the INV-179 promise paying out twice; A2/A3/A5 grew or were rewritten IN PLACE as test doubles of the changed code).
  - DOM harness `node test/client/dom/runDom.js`: 113 passed, 0 failed (unchanged — no DOM-layer behaviour in this batch).
  - `node --check` clean on Code.js, Tests.js, DevTools.js.
  - Editor suite (`Tests.js`, editor-only): +2 gate tests → expect 312 post-deploy (`triggerGate_diagnosticsPurge_nonManagerThrows`, `triggerGate_spanishAutoAssign_nonManagerThrows`). Scanned Tests.js for doubles of every changed area first: the omnibus gate cases touch none of the five paths; `test_managerSaveDay_multipleBreaks` and `test_sheetDoctor_detectsAndCollapsesDuplicates` build single-lunch-type duplicate groups, which the widened guard still classifies as damage.
  - Visual: full matrix re-shot after the round (`node build.mjs && node shoot.mjs`): 102 scenarios, 0 `missing` fixtures, 0 `overflowPx`, no non-font console errors. Read back by eye: `admin-config-light-wide.png` shows the new "Diagnostics tabs (PHI-free)" retention rows beneath the three call-note windows (RT); `admin-system-light-wide.png` shows the Storage inventory's ADP row reading "Kept · diagnostics tabs — ViewUsage kept · ClientErrors kept" (RT) and the "Spanish Inbox auto-assign" heartbeat row in the Automation detail (SA) — whose expectation text wrapped under the row at its first length and was trimmed to the length its siblings fit at. The sheet-doctor card with an `unpaired` finding is NOT on camera (the fixture is empty by design; BP-4 pins the render) — listed as a follow-on.
  - EVERY pin bite-checked against a COMMITTED tree with python inverse edits (the documented `git checkout` hazard): 26 mutations, 26 bites — BP 6 (the guard back to equal-count; the unpaired scan disabled; the report's sign flipped; the positional pairing rewritten as greedy; the reader's live-dedupe dropped; the client all-clear ignoring unpaired), SA 5 (heartbeat after the flag; the window gate dropped; dropped from the remove TARGETS — ALSO caught by the veteran derived net; flag default ON; failure no longer stamped), TW 6 (the numeric guard back to a regex — ALSO caught by QA-24; a stale canvas fallback; a token-equal literal added; a clock literal replaced by a token — the ratchet LOWERS; a new non-token literal; a second copy of the frozen banner hex), DR 3 (the whole-tab read restored; the row at getLastColumn width; the wrong column scanned), RT 6 (recorded in the earlier turn). ONE first-write correction, in the pin not the code: DR-1 declared `DR`/`DR_HEADERS` with `const` inside the vm, which is a lexical binding and not a context property — `var`.
  - Regression Scenarios (Test Command is `manual`) — walked below.

REGRESSION SCENARIOS (Subsystems touched: Server, Client (Time Clock views), Client (Call Notes views), Client (QA views), Client (Training views), Test Suite):
  Every scenario targets the DEPLOYED Apps Script project with live spreadsheets, so none is executable in this container. PASS is recorded where a harness / the visual matrix covers the same assertion, NOT APPLICABLE otherwise with the blast radius named.
  - S30 (trigger handlers reject non-manager callers) | PASS (pinned) — both new handlers (`purgeOldDiagnostics`, `autoAssignSpanishThreadsScheduled`) are in BOTH TARGETS arrays, gate via `assertManagerCaller_` and reference no `.isAdmin` (the derived trigger-wiring + gate-type nets generated a test each), and each has an editor gate test. Live console walk: post-deploy.
  - S95 (a day with more than one break) | PASS (pinned, BP-1..4) — `reportBreakPairingChanges()` lists the F1 day at 9.0h → 8.5h and nothing else; the doctor still flags a stray unpaired leave and repeated clock punches, still never offers to collapse a matched pair, and NOW lists a protected day's unpairable stamps under "Unpaired break stamps". The editor run of `reportBreakPairingChanges()` is an operator action (below).
  - S7 (Day Edit) | NOT APPLICABLE — live project; the doctor's `unpaired` finding names Day Edit as the fix and Day Edit itself is untouched.
  - S74 (department-request tracking) | PASS (pinned, DR-1 + C-N6) — the Expand path returns the same detail from ONE column + ONE row read; scope refusal, unknown id and blank id are still the single `Request not found.`; the in-place resolve is untouched.
  - S105 (Spanish Inbox manager auto-assign) | PASS (pinned, SA-1) — the button is byte-unchanged and still delegates to the one core; the scheduled twin is OFF by default, so the live walk is unchanged until the flag is flipped (then: an hourly run inside business hours assigns exactly as one button press would).
  - S97 (Admin → System) | PASS (on camera + pinned) — the Automation detail's digest list gains the `Spanish Inbox auto-assign` row (`DIGEST_LABELS` ⊇ `DIGEST_STALE_HOURS` still holds by the coupling registry); the `AUTOMATION_JOB_CHECKS` table gains `DiagnosticsPurge`, checked only while a window is set, so a healthy deployment still reaches "Nothing needs attention" (INV-186).
  - S69 (EmpDocs signature pad), S90 / S100 (QA waveform) | NOT APPLICABLE — live project; TW's blast radius is a canvas FALLBACK that fires only on a page with no tokens partial (never the shell), so the drawn colours are unchanged in the app.
  - S46 / S79 / S91 / S92 / S96 (Time / PTO, pay statement, team calendar, adjustments, resume) | NOT APPLICABLE — outside the blast radius: `punchDayAdd_`, `calcHours_`, `breakPairs_` and every hours builder are byte-unchanged (A1/A2 pins green); only the two operator REPORTS and the doctor changed.
  - S1 / S2 (smoke + full editor suite) | NOT APPLICABLE — editor-only; expect 312/312.
  - Every other scenario in the touched subsystems | NOT APPLICABLE — outside the blast radius.

REGRESSION RISKS:
  - RT: `purgeOldDiagnostics` is destructive by design once a window is set. Both windows default 0 and the audit-row heartbeat is checked ONLY while a window is set, so installing trigger #20 changes nothing and nags nothing. An unparseable timestamp cell is never deleted; deletes are contiguous runs bottom-up so a partial run leaves a prefix of the tab intact rather than holes. Deploy skew: an older client's `saveRetentionConfig` payload lacks the two keys and the server writes neither (`hasOwnProperty` gate) — it cannot reset a window.
  - BP: a genuine double-punched LEAVE on a two-break day can no longer be collapsed by the doctor — it is reported and Day Edit fixes it. That is the intended direction (report over guess); the classic single-lunch-type duplicate collapse is unchanged. `reportMultiBreakDays()`'s output is byte-identical (same reader, same arithmetic — A5 asserts the `lastOf` old-figure and exactly two `calcHours_` calls).
  - SA: with the flag ON, the business-hours gate does not consult PTO — a member on approved leave can be handed claims. The button has the same limit; documented as a follow-on, not silently widened here. The heartbeat window is 2h (hourly trigger), so the FIRST hour after enabling shows "stale" only if the trigger was never installed — which is what the panel exists to show.
  - TW: two new scans can turn CI red on a legitimate new colour or a new numeric helper; each failure message names the token to use, the FROZEN entry to add with its INV-166 reason, or the ratchet line to raise — a deliberate one-line edit, never a silent pass.
  - DR: the one-row fetch is `DR_HEADERS` wide (14). `getOrCreateDeptRequestsSheet_` self-heals the header to that width and the established `drFindOpenRequest_` reader already fetches at it, so the grid width is a shipped assumption, not a new one.

INVARIANTS AT RISK: None violated. Touched and held: INV-01/INV-44 (both new handlers locked/gated, the derived nets generated their tests), INV-32/INV-161 (counts-only audit rows; `AUTOMATION_JOB_CHECKS` gains a row with an `enabled()` predicate — INV-186 in code), INV-151 (heartbeat-before-flag reused), INV-97 (a second server-scope flag, default OFF), INV-179 (the digest set and both TW scans are DERIVED), INV-176 (`breakPairs_` untouched; the doctor gap it named is now partly closed), INV-155/INV-159 (the collapse guard widened toward report-only — AMENDMENT owed), INV-169/INV-187 (`unpaired` carries totals + `truncated`, the dropped stamps are named), INV-138/INV-131 (the detail read bounded; scope + not-found unchanged), INV-166/INV-128 + the C4 rule (canvas fallbacks pinned EQUAL to their tokens), INV-185 (every fixture updated in the same commit as the shape). INV-176's "a logged follow-on" sentence and INV-31 (iii)'s are now stale → sync-docs.
NET SCORE: 0 − 0 = 0
  (REFLECT: RT — Q1 NO (a retention tier; F11 said growth costs nothing today) / Q2 NO (default OFF, fail-safe stamps, bounded runs). BP — Q1 NO (`reportMultiBreakDays()` came back CLEAN on 2026-09-01, so no multi-break day exists to have been mis-collapsed this month; the doctor gap is defensive) / Q2 NO. SA — Q1 NO (capability, default OFF) / Q2 NO (PTO-blind is the button's existing limit, documented). TW — Q1 NO (the stale canvas fallbacks fire only on a no-stylesheet page) / Q2 NO. DR — Q1 NO (a cost, not a defect) / Q2 NO. Tally: 0 production fixes, 3 capabilities (RT, the BP report, SA), 5 defensive/structural (the doctor widening, TW-A, TW-B + the fallback alignment, DR, the shared reader), 0 new failure modes.)

OPERATOR ACTIONS / DEPLOY:
- Re-run `installAutomationTriggers()` ONCE after the deploy so triggers #20 (`purgeOldDiagnostics`, daily 2am) and #21 (`autoAssignSpanishThreadsScheduled`, hourly) exist. Both are inert until configured: the purge while both windows are 0, the auto-assign while the `spanishAutoAssign` toggle is off. | BLOCKS DEPLOY: N
- OPTIONAL: set the diagnostics windows from Manage → Admin → Config → Retention (`VIEW_USAGE_RETENTION_DAYS` / `CLIENT_ERR_RETENTION_DAYS`; e.g. 365 / 180) — irreversible per the danger confirm; a large first run drains at 2000 rows per night and the `DiagnosticsPurge` audit row says `hitPerRunCap` meanwhile. | BLOCKS DEPLOY: N
- OPTIONAL: flip `spanishAutoAssign` in Manage → Admin → Feature Toggles once the bilingual-members list is set; it heartbeats even while off, so Admin → System shows the trigger is alive first. | BLOCKS DEPLOY: N
- Run `reportBreakPairingChanges()` once from the editor (read-only, MANAGER_EMAILS gate) — `reportMultiBreakDays()` was CLEAN on 2026-09-01 but that report cannot see F1's one-leave/two-return shape; this one can. A listed day means the greedy pairing deducts a break the positional one had paid; nothing needs correcting either way. | BLOCKS DEPLOY: N
- Post-deploy `runAllTests()` — expect **312**. | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy (Server + Client (Call Notes views) + Client (Time Clock views) + Client (QA views) + Client (Training views) ship in the one push; the Test Suite ships with it and runs from the editor). This rides the SAME deploy every round since PR #176 has been waiting on.

FOLLOW-ON ITEMS:
- web-app/Code.js (`resolveDeptRequest`, `markDeptRequestResolved_`) — still whole-tab reads by RequestId; `drFindRowByReqId_` fits both (DR bounded only the per-click detail read).
- web-app/Code.js (`autoAssignSpanishThreadsScheduled` / `spanishAutoAssignPick_`) — PTO-blind: a member on approved leave can be handed claims. A PTO-aware picker is an operator decision (the presence-chip class).
- web-app/tc/script_clock.html — the ribbon's `rgba(15,138,82,…)` / `rgba(183,121,31,…)` colour-mix FALLBACK pairs duplicate `--accent` / `--warn` in rgb form; a `--accent-glow`-style token pair would retire them from the TW-B ratchet baseline.
- web-app/Code.js (sheet doctor) — the one-leave/two-return shape is a classic duplicate LunchIn group whose collapse keeps the LAST APPENDED row (INV-155), which is the stray one if it was appended later; the doctor cannot tell which return is real. Day Edit is the fix; a "which is real" heuristic would be a guess.
- test/visual — no scenario shoots the sheet-doctor card WITH findings (the fixture is empty by design); the render is pinned in BP-4 only.
- The deliberate NON-implementations (operator decisions): presence-chip schedule/PTO gating, BLOCKING an off-domain `MAIL_BCC_ALL`, the `getTeammateStatus` full-Timesheet read, the `dept_req_v1` cache-key bump, the quiz editor's "+ Question" per-block append.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md, Operator State Checklist — "installer now wires nineteen triggers" → twenty-one; add the two trigger entries (`purgeOldDiagnostics` daily 2am; `autoAssignSpanishThreadsScheduled` hourly, flag-gated, business-hours-gated); a new dated operator-state entry for this round (re-run `installAutomationTriggers()`; the two optional windows; the optional flag; run `reportBreakPairingChanges()`; expect 312).
- CLAUDE.md, INV-44 — the handler list gains `purgeOldDiagnostics` (destructive, the retention family) and `autoAssignSpanishThreadsScheduled`; the trigger-handler Common Gotcha list likewise.
- CLAUDE.md, the `ViewUsage` / `ClientErrors` operator-state entries + the cycle-18 F11 wording ("the only two stores with NO retention tier") → the tier exists: `VIEW_USAGE_RETENTION_DAYS` / `CLIENT_ERR_RETENTION_DAYS` (Script Property first, CONFIG 0 = disabled), the Retention panel rows, the Storage Health line, the `DiagnosticsPurge` heartbeat.
- CLAUDE.md, the `AUTOMATION_DIGEST_LAST_RUNS` entry — the key set gains `spanishAutoAssign` (hourly, stale > 2h); the runtime-feature-toggles KDD's "the registry's FIRST pure-server-scope flag" now has a sibling (`spanishAutoAssign`).
- CLAUDE.md, INV-31 (iii) + the Spanish operator-state entry — "the scheduled trigger the operator said might follow is a logged follow-on" → shipped (trigger #21, flag default OFF, PTO-blind limit stated).
- CLAUDE.md, INV-176 — "the sheet doctor still cannot SEE that shape … a logged follow-on" → the doctor reports protected multi-break days' dropped stamps (`unpaired`); the residual (the duplicate-group shape's last-appended-wins collapse) stated.
- CLAUDE.md, INV-155 + INV-159 — the collapse guard is "two-plus stamps of BOTH lunch types", not equal counts; `getTimesheetDoctor` returns `unpaired` / `totalUnpaired`; the Workstream-A operator-state entry gains `reportBreakPairingChanges()` beside `reportMultiBreakDays()`.
- CLAUDE.md, INV-138 — `getDeptRequestDetail` reads through the bounded `drFindRowByReqId_`.
- CLAUDE.md, the design-token KDD / the C4 fallback rule — the canvas-fallback rule (a `getPropertyValue('--t') || '#hex'` fallback must equal the token's Console-light value; TW-B pins it) and the TW-B ratchet's existence.
- CLAUDE.md, Test Command section — the harness totals 785 → 796 pure, editor 310 → 312, and the TW-A/TW-B/RT-1/BP-1..4/SA-1/DR-1 pins in the running narrative.
- CLAUDE.md, Regression Scenarios — S95 gains the `reportBreakPairingChanges()` + "Unpaired break stamps" steps; S105 gains the scheduled-twin step; a new scenario for the diagnostics retention tier (Admin → Config → Retention rows, the trigger, the audit row, the Storage Health line).
- README.md — the visual-matrix count still reads 99 (102 real, stale since before this round); the harness counts.
- INV-198/INV-199 were proposed by the 19pre reflection and never written to the library; /reflect owns that.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
