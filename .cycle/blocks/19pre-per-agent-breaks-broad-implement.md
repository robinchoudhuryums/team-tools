---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: PAB-1 — per-agent break schedules (operator 2026-09-02: the Admin break-schedule editor assumed everyone in a timezone shares one slot list; each agent has their own staggered breaks so the desk is never empty)
Files modified: web-app/Code.js, web-app/cn/script_callnotes.html, test/visual/mock.js, test/client/run.js, CLAUDE.md, .cycle/STATE.md

CHANGES:
PAB-1 | web-app/Code.js | `SHIFT_BREAK_SCHEDULES` gains an optional `employees: {<rosterId>: [...]}` map. `breakSchedSanitize_` whitelist-rebuilds it (key regex `BREAK_EMP_KEY_RE`, cap `BREAK_EMP_MAX_KEYS`=200, per-list `breakListSanitize_`, explicit-empty kept, key emitted only when non-empty). `empShiftSchedule_(empLike, tz)` — the ONE resolver (INV-149) — layers `prop.employees[id]` ABOVE the tz/DEFAULT entry via `!== undefined` (explicit-empty honoured) and reports `perEmployee`; column O still governs start/length only. Callers pass the rep id: `getEmployeeState` (already did), `getCoveragePlan`, `getPunctualityReport`, `breakSchedulesAdminView_`. The admin view ships `employees[] {id,name,onRoster,timezone,tzMismatch,breaks}`, `roster[]`, `workAnchorTz`. `saveBreakSchedules` accepts `payload.employees` — map shape / malformed id / id not on the roster (offboarded rows still resolve, INV-183) / per-break rule all refused BY NAME before any write; blob carries `employees` only when non-empty in the sanitizer's key order; delete-on-reset requires employees empty too; audit note counts per-agent entries.
PAB-1 | web-app/cn/script_callnotes.html | "Per-agent breaks" section under the tz sections: agent sections (`data-emp="1"`, name · id, "(not on roster)", "times in <tz>" pill with a warn variant + ⚠ on `tzMismatch`, Remove (use default) / Keep), a roster picker seeded from the DEFAULT section, `cnBreakSectionMeta_` shared by the override/revert/add handlers, save collects custom-only agent sections into `employees` and posts `{reminderMin, schedules, employees}`; CSS `.cn-brk-pill.warn`, `.cn-brk-sub`.
PAB-1 | test/visual/mock.js | `breakSchedules` fixture mirrors the new view shape (two agents incl. one Asia/Kolkata tzMismatch row; a four-row roster; `workAnchorTz`).
PAB-1 | test/client/run.js | BRK-1..4 grown in place (see the count-chain note); BRK-5 new — `empShiftSchedule_` behavioural.

TEST RESULTS: pure 745/0 (was 744), DOM 104/0, `node --check` Code.js + Tests.js clean. 6 mutations / 6 bites (per-employee guard → `.length`; sanitizer drops employees; roster-id check deleted; coverage caller drops `id`; client payload drops `employees`; the first `!== undefined → truthy` mutation was an EQUIVALENT MUTANT — `[]` is truthy — and BRK-4's source pin still caught it). Visual: `admin-config-light-wide` + `-mobile` re-shot, overflow 0, no missing fixture; the card cropped and eyeballed at both widths (warn pill, picker, mobile stacking all as designed). Regression scenarios (Test Command = manual): S76 PASS (per-agent step added and walked on the fixture: seed-from-default, warn pill, remove/keep, save payload; the reminder + chip behaviour rides the unchanged `getEmployeeState.schedule` path); S72 Coverage NOT APPLICABLE beyond the wiring pin (the planner reads breaks through the same resolver — no visual change unless a per-agent entry exists; no fixture entry yet); S98 Punctuality NOT APPLICABLE (lunch adherence reads the same resolver; behaviour unchanged with no per-agent entries). Editor suite not run (no Apps Script runtime off-editor) — `saveBreakSchedules` keeps its omnibus admin-gate case; expected count unchanged at 308.
REGRESSION RISKS: (1) `empShiftSchedule_` now returns a NEW object (not the memoized `base`) whenever a per-agent entry exists — callers that mutated `base` would have been broken before too; none do. (2) A pre-existing property blob without `employees` is read byte-identically (the sanitizer emits the key only when non-empty). (3) `getBreakSchedules_` is now consulted per call of `empShiftSchedule_` when an id is present — it is memoized per execution, so the coverage walk's per-rep-per-day cost is unchanged. (4) The admin view builds a roster map via `getEmployeeRosterRows_` (cached roster) — one extra cached read per Admin open.
INVARIANTS AT RISK: INV-149 (single resolver — held: no bare `getShiftSchedule_` caller added, the F-tripwire scan still passes); INV-183 (roster inclusion — the save deliberately accepts an email-less row's id because the id is reserved; the view marks `onRoster:false` rather than hiding the section); INV-136 (admin count unchanged — no new endpoint); INV-129/156 n/a (no cache); INV-193 (client `data-*` values are re-`esc()`'d on render — `cnBreakSectionMeta_` feeds `esc()` sinks only).
NET SCORE: 0 − 0 = 0 (a capability, not a fix — nothing was firing wrong in production this month; the per-timezone model was merely unable to express staggered breaks)

OPERATOR ACTIONS / DEPLOY:
- None to set up: the same `SHIFT_BREAK_SCHEDULES` property gains an `employees` key only once you save one | BLOCKS DEPLOY: N
- After deploy: open Manage → Admin → Config → Break schedules and enter each agent's slots; fix any roster row the warn pill flags (the times are read in that zone) | BLOCKS DEPLOY: N
Deploy: Server + Client (Call Notes views): `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. Test Suite: same push; `runAllTests()` expects 308.

FOLLOW-ON ITEMS:
- Break COVERAGE planner in the same card (the operator's next ask — planned separately below the summary): a per-15-min "agents away" strip from the effective per-agent breaks, optional background layer of average call volume by time-of-day, integration with the Coverage planner's hourly bands.
- The Coverage planner fixture has no per-agent entry, so the planner's per-agent effect is verified by the wiring pin only; a `?fixture=` variant carrying one would put it on camera.
- An ad-hoc Playwright crop script (scratchpad `crop-brk.mjs`) was useful for eyeballing ONE card at both widths — worth promoting into `test/visual/` as a `--crop <selector>` option on shoot.mjs.

DOCUMENTATION UPDATES NEEDED:
- Done in this session: CLAUDE.md (the SHIFT_SCHEDULE KDD per-agent paragraph + BRK-1..5; the 2026-08-27 operator-state entry; S76 per-agent step; the count chain → 745), STATE.md, this block.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
