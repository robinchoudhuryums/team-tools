---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- M4 (Medium) — Spanish auto-assign balances on the claims each member holds on PENDING requests, not their whole history
- M1 (Medium) — a rep with no call data in the Team Metrics range has NO answer rate (null → "—", sorted lowest), never a red 0%
- X3 (Low, struct) — the Team Metrics fixture no longer shows call data on Today; the no-data state is photographed
- M2 (Medium) — the Dashboard's "Yesterday" is the previous WORKDAY (weekends + company holidays), and an all-empty window is never cached
- M8 (Low-Medium) — the MTD prior-month window is lag-aligned (days 1..d-1), and the run-rate projection divides by the days that have data
- M3 (Medium) — a range's average talk time is the ANSWERED-WEIGHTED mean of the daily averages, in both DQE readers
- M7 (Low-Medium) — a missing DQE tab is an error at the three callers that dropped it (onboarding readiness, Dashboard, Team Metrics), never "no calls", never cached
- M9 (Low) — a workday with no CDR row is null (no data) in the My Stats and Team trend series, never 0 (g136)
- D1 (Medium) — coaching stamps are parsed in CONFIG.TIMEZONE, so ages against the real clock (overdue, business minutes) are right
- D2 (Low-Medium) — nothing acknowledged has no median: coachMedian_([]) is null and the card says "nothing acknowledged yet"

Files modified:
- web-app/51_spanish.js (M4 spanishOpenLoad_; spanishAutoAssignCore_ builds pendingIds from the same pending read and derives load inside the lock)
- web-app/40_metrics.js (M1 getTeamMetrics row; M2/M8 dashboardPeriodRange_ (prevWorkdayIso_, date label, dataThrough) + dashboardPrevRange_ (d-1, null on the 1st) + getDashboardMetrics (v6 key, dataThrough, no put when nobody reported); M3 both DQE readers; M7 cdrAgentsOrThrow_ + its three callers; M9 the four trend builders)
- web-app/00_config.js (M3 CDR_CACHE_KEY cdr_metrics_v4 → v5)
- web-app/82_coaching.js (D1 coachParseTs_ via Utilities.parseDate in CONFIG.TIMEZONE; D2 coachMedian_ null)
- web-app/metrics/script_metrics.html (M1 mSortReps_ null-lowest)
- web-app/tc/script_clock.html (M8 both cards project from res.dataThrough)
- web-app/train/script_coaching.html (D2 coachMedianAckText_)
- test/visual/mock.js (X3 getTeamMetrics is a function of the range — a range ending today returns the no-call-data payload; the populated payload gains a no-CDR rep; ?teamrange=7d; M8 dashboard fixture ships periodKey + dataThrough; M9 one null trend day; D2 null medians)
- test/visual/shoot.mjs (X3 the three team shots preset a past 7-day range; new metrics-team-today-light-wide)
- test/client/run.js (+8 new pins — M4, M1, M2/M8 endpoint, M3, M7, M9, D1, D2 client; 5 updated: the dashboardPeriodRange_ pin that encoded the calendar-day defect, dashboardPrevRange_'s windows, the v6 cache key, coachMedian_'s empty case, and coachAnalytics_'s empty shape)
- test/client/server-split-manifest.json (regenerated for every server declaration change)
- CLAUDE.md (running totals: pure 956 → 964, visual 114 → 115)
- .cycle/STATE.md, .cycle/blocks/22-B4-broad-implement.md
Estimate: L (~11 h) — M4 S 1.5h · M1 S 1h · X3 S 0.5h · M2 S 1h · M3 S 1.5h · M7 S 1.5h · M8 S 1h · M9 S 1h · D1 S 1.5h · D2 S 0.5h
Actual: ~3.5 h (M8 reached the client: the projection needed a dataThrough field, not just a narrower prior window; D1's comment claimed "only differences" while five callers compared against the real clock)

CHANGES:
M4 | 51_spanish.js, run.js | The auto-assign's load map counted every claim a member had EVER made (the live claim map keeps resolved requests), so a new member received every unclaimed request until their history caught up. `spanishOpenLoad_(liveMap, pendingIds)` counts only claims on requests that are still pending, from the SAME pending read that decides what is unclaimed, inside the lock. Pinned behaviourally (40 resolved + 1 open vs a new member → alternating picks) plus the core's wiring.
M1 | 40_metrics.js, script_metrics.html, run.js | getTeamMetrics shipped `pctAnswered: 0` for a rep with no CDR row, rendered as a red 0% (on the Today default, for every rep). The row now ships null — the H2 formula's own answer when there is no denominator; the cell already drew null as "—" and the TSV as blank; mSortReps_ sorts null below a real 0% both ways. Pinned (server literal + driven sort).
X3 | mock.js, shoot.mjs | The fixture answered every range with populated call data, including Today, which the real CDR never has — which is why M1 was never photographed. A range ending today now returns the no-call-data payload; the three existing team shots preset a past 7-day range (`?teamrange=7d`) and the populated payload gains a no-CDR rep ("—", sorted last). New scenario `metrics-team-today-light-wide` photographs the Today state.
M2 | 40_metrics.js, run.js | `dashboardPeriodRange_('yesterday')` resolves through `prevWorkdayIso_` (weekends + the company calendar) and labels a non-literal yesterday by its date ("Fri Feb 27"), so Monday reads Friday instead of an empty Sunday. A window NOBODY reported in is no longer cached — the pre-import morning pinned an empty card for the 6-hour TTL. Cache key v6. The pin that asserted the calendar day was the defect's pin; rewritten (weekend, holiday, literal yesterday).
M8 | 40_metrics.js, script_clock.html, mock.js, run.js | MTD runs to today, but the CDR holds nothing for today, so the current window carries d-1 days of data while the prior window took days 1..d. `dashboardPrevRange_` now takes 1..d-1 (clamped into a shorter month; no comparison on the 1st; a one-day window reads "Jul 1"). MTD/YTD ship `dataThrough` (calendar yesterday, or null before the first complete day), and both cards project from it — the projection divided by a day with no data. The mock never shipped `periodKey`, so no projection had ever been photographed; it is now (`clock-light-wide`).
M3 | 40_metrics.js, 00_config.js, run.js | Both DQE readers averaged the daily ATTs with equal weight; each day now counts by its answered calls (a row with an ATT but no answered calls carries no weight), matching the team aggregate. The per-rep-per-day entry is weighted too (one row per agent-day in practice, so unchanged there). CDR_CACHE_KEY v5 (INV-85). Driven through both readers: a 2-call 10-min day and a 60-call 2-min day read 135 s, not 360.
M7 | 40_metrics.js, run.js | `cdrAgentsOrThrow_(res)` turns the reader's named `meta.error` into a throw that each caller's existing catch already handles: the onboarding readiness panel returns ok:false (every rep "unknown", not "missing"), the Dashboard returns {error} (the F-13 warn card) and caches nothing, Team Metrics returns {error} before building a row. Driven: the helper both ways and the onboarding endpoint end to end; the other two callers' wiring pinned, plus a ban on a bare `.agents` read in all three.
M9 | 40_metrics.js, mock.js, run.js | The four trend builders (My Stats, the range self-view, both Team trends) defaulted a no-row workday to 0 rung/answered/missed; they ship null. Every consumer already skipped null (mMiniSparkSvg_, mTrendAvg_, mBestWorstDays_). Pinned by a sweep over the three endpoints (no `: 0` default; all eleven null fields found) plus the sparkline's gap, driven. The fixture carries one null day, so the rail sparklines are photographed bridging it.
D1 | 82_coaching.js, run.js | coachParseTs_ read the CONFIG.TIMEZONE wall-clock stamps as UTC, under a comment that said it was only differenced — but five callers compare against the real clock (overdue, the recap window, business minutes). It now parses through Utilities.parseDate in CONFIG.TIMEZONE; loaded without an Apps Script runtime (the Node pins) it keeps the wall-clock-as-UTC reading. Driven with an Intl-backed parseDate: 09:00 IST is 03:30 UTC, an item exactly seven days old on the real clock is overdue at 7 (read as UTC it was 6.8 days), and a DST zone resolves through its own offset.
D2 | 82_coaching.js, script_coaching.html, mock.js, run.js | coachMedian_([]) returned 0 and the board read "median 0 business days to ack" for a team that had acknowledged nothing; it returns null, and the card renders through `coachMedianAckText_` ("nothing acknowledged yet"; a real 0 still reads as one). coachNum_ maps null to 0, which is why the server fix alone was not enough.

TEST RESULTS: pure 964/964 (+8 new pins, 5 updated), DOM 156/156, `lint:server` clean, `counts --check` clean. Visual: 13 touched scenarios re-shot (clock ×4, metrics ×6 incl. the new metrics-team-today-light-wide, coaching ×3) — 0 px overflow, no missing fixture, no console error beyond the proxy's font-certificate noise; read: the Today team table all "—", the 7-day table with the no-CDR rep last, the Dashboard MTD projections, the null trend day bridged, the coaching median sub-line. 16 bite-checks, all BITE (M4, M1 ×2, M2 ×2, M8 ×3 incl. the dataThrough derivation, M3 ×2, M7 ×2, M9, D1, D2 ×2). Editor suite: no Tests.js change; `test_metrics_getMyMetrics_cdrIntegration`'s single-row ATT (150 s) is unchanged by the weighting.
Regression scenarios (Test Command `manual`) overlapping this batch — S41 (My Stats), S42 (Team Metrics range + presets), S43 (CDR unavailable), S44 (shift-stats enrichment — its reader's ATT is single-day, unchanged), S75 (onboarding CDR readiness), S99 (Coaching), S105 (Spanish auto-assign), S110 (holiday "Yesterday"), S111 (one answer rate) — all NOT APPLICABLE here (they need the live app and the CDR Report); the walk is under OPERATOR ACTIONS.

REGRESSION RISKS:
- M7: with the DQE tab missing, Team Metrics now shows an error instead of a table of note counts beside zero calls; the note counts are unavailable until the tab is back. Deliberate — a table of confident zeros was the defect — but it is the whole view.
- M2: the Dashboard "Yesterday" card on a Monday shows Friday under a "Fri Sep 18"-style label; a rep used to "Yesterday" sees a date. The company-calendar read now happens on that card (memoized per execution, failure → weekends only, as prevWorkdayIso_ already did).
- M2: a window nobody reported in re-reads on every load (the pre-import morning, and the first days of a month); these are the cheapest windows there are.
- M8: MTD deltas on the 1st of the month are gone (there is no complete day to compare); the prior window label reads one day shorter ("Jul 1–11" on the 12th).
- M3: every multi-day ATT figure moves (My Stats range, Team Metrics per rep, the Dashboard own card); single-day figures are unchanged.
- D1: coaching ages shift by the zone offset (5.5 h on an Asia/Kolkata deployment): items turn overdue and appear in the recap window at the right hour, i.e. earlier than before.
- M1/M9/D2: three fields that were always numbers can now be null (`reps[].pctAnswered`, the trend `rung/answered/missed`, `medianDaysToAck`); every in-repo consumer was checked, and the rep view (`teamMetricsRepView_`) ships no reps.

INVARIANTS AT RISK: None broken. INV-187 / g136 (absence is not zero) — M1, M9 and D2 apply it; INV-129 (never cache a degraded round) — M2's empty window and M7's throw both skip the put; INV-85 (versioned cache keys) — CDR v5, dashboard v6; INV-210 (one holiday calendar) — M2 goes through prevWorkdayIso_'s company-calendar read, never the federal list directly; INV-211 (the dashboard's answer formula) — M1 ships that formula's own null; g131 (a shipped null is one rule) — M1's null is the H2 rule, rendered by the existing cell and sorted by the one sort; g128 (not found ≠ could not look up) — M7.

NET SCORE: 7 production fixes − 0 new failure modes = +7. Would fire this month: M1 (every rep, every Today view — the default), M2 (every Monday), M8 (every MTD view, daily), M3 (every multi-day range), M9 (any PTO day in a 30-day trend), D1 (every coaching age, if coaching is in use — S99), M4 (every auto-assign run once any request has been resolved). NOT counted: M7 (needs the DQE tab missing), D2 (needs a team with open items and no acknowledgements — plausible early, unconfirmed), X3 (structural). Confidence is highest for M1/M2/M8, which are calendar facts rather than data-dependent.

OPERATOR ACTIONS / DEPLOY:
- Deploy (rides with Batch 1, S2, Batch 2, the follow-ons and Batch 3) | BLOCKS DEPLOY: N
- Post-deploy walk: (1) Dashboard on a Monday (or after a company holiday) — the Yesterday card shows the last workday, labelled by its date; MTD shows "vs <prior month> 1–<yesterday's day>" and an "On pace" line that matches answered ÷ days-with-data × month length. (2) Team Metrics — Today: every rate "—", no red 0%; a 7-day range with a rep who was out all week: that rep "—", sorted last on % Answered. (3) My Stats / Team trends over a range containing a PTO day: the sparklines bridge the day, no dive to 0. (4) Average talk over a week vs the daily figures: it sits closer to the busiest days. (5) Coaching — an item logged exactly N business days ago turns overdue at the right hour; a team with open items and no acknowledgements reads "nothing acknowledged yet". (6) Spanish auto-assign with a long-tenured member and a new one: requests alternate rather than all going to the new member. | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy.

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- D1's client twin `coachTsMs_` (train/script_coaching.html) still reads the stamps as UTC for the 30-day / quarter windows and "last" dates on the manager board — a 5.5 h edge error, not in the finding.
- M7's class survives in callers the finding did not name: `getMetricsAmbient` (the badge silently absent, cached for the CDR TTL), `managerGetShiftStats`' enrichment, and `getCdrDailyBreakdown_`, which returns `{daily:{}}` on a missing tab with no error field at all.
- The Dashboard MTD note coverage counts notes through TODAY against calls through yesterday (the same lag as M8, on the coverage ratio) — not in the finding.
- The dashboard fixture's prior-window label ("Jul 1–23") is a static string not aligned with the M8 window.
- D2's "nothing acknowledged yet" sub-line is not photographed: the empty-team shot renders a different KPI strip, and no fixture has open items with zero acknowledgements.

DOCUMENTATION UPDATES NEEDED:
- docs/gotchas.md + index: amend g136 (a second instance — the Metrics trend builders, M9) and g124/g131 (a rep with no calls is a NULL rate at the ROW level too, M1); amend g123 (the Dashboard's "Yesterday" is a workday too, M2); amend g128 (a reader's named error must reach the caller — M7's helper); a new rule for a LAGGED source: a period-to-date window and its comparison must count the same days of DATA, not of calendar, and a projection divides by the days that have data (M8); a new rule that a stamp parser documented as "differences only" is a claim to re-check whenever a caller compares it to the real clock (D1); a note that an average of daily averages must be weighted (M3).
- docs/modules.md: Metrics (Dashboard Yesterday/MTD/projection, weighted ATT, null rates and trend days, the DQE-missing error), Coaching (tz-correct ages, null median), Spanish auto-assign (open load).
- docs/design-decisions.md: "Metrics hero + rail layout" / the Dashboard period entry — Yesterday is the previous workday and MTD is lag-aligned; "Cross-rep manager aggregates" / Team Metrics — a no-call rep has no rate.
- .cycle/config.md: walk steps on S41/S42/S43/S75/S99/S105/S110/S111; invariants for "a period-to-date comparison is lag-aligned" and "a reader's named failure is a throw at every caller, never an empty map".
---END BROAD SCAN IMPLEMENTATION SUMMARY---
