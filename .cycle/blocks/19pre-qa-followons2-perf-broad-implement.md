# 2026-08-28 #2 — /broad-implement follow-on items + QA mock + Training/Manage load-time check

---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- FO-WAVE | My Reviews player waveform + click-to-seek (the Phase-2 detail chrome on the agent player, decoration-only)
- FO-RET | QA review-record retention tier — purgeOldQaReviews trigger #18, default 0 = disabled (the deferred operator decision made shippable by the CN/forms 0=disabled precedent: the machinery ships, the window choice stays the operator's)
- PERF | Training (My + Team) + Manage Time SWR paint-last-good (the operator's "these pages take longer to load" report — investigated first: they were NOT optimized; the 2026-08-13 SWR round never covered them)
- (also delivered mid-command: the QA module MOCK — the four real fixture-backed screenshots (queue, detail, stats, my-reviews) sent via SendUserFile rather than a drawing)

Files modified: web-app/Code.js, web-app/qa/script_qa.html, web-app/script_core.html, web-app/tc/script_manager.html, web-app/train/script_training.html, web-app/cn/script_callnotes.html, web-app/Tests.js, test/visual/mock.js, test/visual/shoot.mjs, test/client/run.js, CLAUDE.md, .cycle/STATE.md, .cycle/blocks/

CHANGES:
FO-WAVE | web-app/qa/script_qa.html, test/visual/mock.js, test/visual/shoot.mjs | qaDrawWaveOn_ extracted as the ONE shared waveform painter (qaDrawWave_ is now a thin delegate over #qa-wave/QA_STATE.peaks); new qaMyRevWave_ decodes at 8 kHz mono behind the QA_WAVE_MAX_BYTES gate, drops a stale card's decode via the myRevAudioSeq guard (INV-156), inserts a canvas before the audio and paints through the shared painter (initial + timeupdate tint + click-to-seek). Wired in qaMyRevPlay_ inside its OWN try/catch AFTER the audio src is set — strictly decoration, a waveform failure never costs playback. The mock aliases FIXTURES.getMyQaReviewAudioChunk to the reviewer WAV fixture (both server routes delegate to qaAudioChunkFor_, so one fixture serves both — INV-185), and the qa-myreviews-light-wide scenario now PRESSES Play via the post hook so the player + waveform are on camera.
FO-RET | web-app/Code.js, web-app/cn/script_callnotes.html, web-app/Tests.js | CONFIG.QA_REVIEW_RETENTION_DAYS (0 = disabled) + qaReviewRetentionDays_ (property → CONFIG; negative/garbage → 0, never NaN) + qaStoreConfigured_ + purgeOldQaReviews: INV-44 assertManagerCaller_ gate, disabled/unconfigured early-returns BEFORE the lock, INV-01 locked, deletes QaComments + QaScorecards rows older than the window — the QaRecordings INDEX and the Drive audio files are NEVER touched; ms>0 fail-safe (a 0/garbage CreatedMs is never deleted), bottom-up delete, counts-only QaReviewPurge audit row every enabled run (the liveness heartbeat). Wired: both TARGETS arrays (18th trigger, daily manager-tz 2am beside the CN 3rd-tier purge), AUTOMATION_AUDIT_ACTIONS, AUTOMATION_JOB_CHECKS row gated on window>0 AND store configured (INV-186), CN_HEALTH_RUN_LABELS panel label. Editor: test_triggerGate_qaReviewPurge_nonManagerThrows (runAllTests 295→296).
PERF | web-app/script_core.html, web-app/tc/script_manager.html, web-app/train/script_training.html | Session-state SWR for the three slow enters. enterManagerView paints renderManagerView(area, mgrData) + the refreshing pill when mgrData is warm (first enter of a session keeps the loader — getManagerDashboard is the app's heaviest live read); loadManagerDashboard writes mgrData BEFORE the render guard, and a REFRESH render (mgr-refresh present) is deferred by mgrSwrRenderBlocked_ (checked .mgr-pending-chk or any open overlay); mgrLoadError_ degrades a painted view to a warn toast (C17-5 preserve-last-good) and only a cold failure renders the Retry error screen. enterTrainingHomeView paints from TRAIN_STATE.my + pill; trainLoadMy_(painted) keeps last-good on a failed refresh (toast) and renders the A12 error card only cold. enterTrainingManageView paints only when ALL FOUR load-bearing fan-in pieces are warm (dash/kbTree/emps/quizzes — analytics degrades on its own, E12); trainLoadMgr_'s done() consults trainMgrFormDirty_ (picked assign item, typed due date, checked employee boxes, open overlay) before the refresh render — the fresh data stays warm in TRAIN_STATE either way; onFail splits painted (toast) from cold (error card). Argless post-mutation callers keep cold semantics (unconditional re-render after a save/assign).
PINS | test/client/run.js 683→686 | QA-17 (one shared painter; qaMyRevWave_ size gate + seq guard + shared-painter-only; decoration-only call site; fixture alias + Play post hook), QA-18 (index untouched — comment-stripped per INV-188 since the doc comment names the tab it must not touch; ms>0 fail-safe; bottom-up; early-returns before the lock; counts-only audit anchored to the CALL; job-check row gated per INV-186; getter behavioural in a vm; editor gate test registered), PERF (all of the above shapes incl. state-write-before-guard). A FOURTH test was auto-generated by the derived trigger-wiring/gate-type nets the moment purgeOldQaReviews entered TARGETS (682→683 — INV-179, the accrual trigger's exact precedent).

TEST RESULTS: pure 686 passed / 0 failed; DOM 82 passed / 0 failed. 6 mutations / 6 bites, commit-before-bite: (A) trainLoadMgr_ dirty-form guard dropped → PERF; (B) mgrLoadError_ preserve-painted branch removed → PERF; (C) mgrSwrRenderBlocked_ consult removed → PERF; (D) ms>0 fail-safe removed → QA-18; (E) purge retargeted at QA_RECORDINGS_TAB → QA-18; (F) qaMyRevWave_ seq guard removed → QA-17. Regression Scenarios walked: S90 (QA module — the waveform/My Reviews/retention deltas are additive; player paths unchanged, retention default-off = byte-identical nightly behaviour) PASS by construction + pins; S67/S68 (Training flows — enter paths only changed on the re-enter branch; cold path byte-equivalent) PASS; S4/S7/S13 (Manage Time flows — render path unchanged, only WHEN it renders) PASS; visual matrix 54/54 clean (full re-shoot; qa-myreviews now shows the player + waveform on camera).

REGRESSION RISKS: (1) A background Manage refresh landing while a manager stares at a stale dashboard — bounded: the deferred render only occurs while bulk boxes are checked or an overlay is open, and the next enter/refresh renders the warm data. (2) Team Training's painted view can be one refresh behind while the assign form is dirty — deliberate (a re-render would wipe the form; the pre-change behaviour was worse: no paint at all until the slowest RPC). (3) The retention purge is destructive by design — mitigated by default 0, the ms>0 fail-safe, the never-touched recordings index, and the INV-44 gate.

INVARIANTS AT RISK: None violated. INV-196 AMENDED (retention clause + Verify QA-1..18); INV-44 grew to eighteen handlers (doc + derived nets agree); INV-161/186 followed for the job-check row; INV-156 followed for the waveform seq guard; C17-5/INV-175 posture followed for the SWR failure split.

NET SCORE: 1 production fix (the Training/Manage load-time report — a real, operator-reported UX defect) − 0 new failure modes = +1. FO-WAVE and FO-RET are capabilities (defensive/additive), the mock is a deliverable, not a fix.

OPERATOR ACTIONS / DEPLOY:
- Re-run `installAutomationTriggers()` once from the editor — the 18th trigger (`purgeOldQaReviews`, daily 2am) is created; harmless while `QA_REVIEW_RETENTION_DAYS` is 0/unset | BLOCKS DEPLOY: N
- (Optional, later) set Script Property `QA_REVIEW_RETENTION_DAYS` to enable review-record retention — irreversible deletes of QaComments/QaScorecards past the window; recordings index + Drive files never touched | BLOCKS DEPLOY: N
- Post-deploy `runAllTests()` — expect **296** | BLOCKS DEPLOY: N
Deploy: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy (ships with the whole undeployed PR set).

FOLLOW-ON ITEMS:
- Re-opening agent visibility of My Reviews remains one registry line (drop managerOnly+also from qaMyReviews) + a deliberate QA-14 pin rewrite — waiting on the operator, per their fresh gate decision.
- Storage Health does not yet surface the QA store's retention state (the CN retention panel precedent) — an Admin read-only line is a candidate if the operator enables the window.
- The remaining un-SWR'd manager surface is Team Notes (its five sub-tabs each fetch on enter; INV-146 seq tokens exist, state persists in CN_STATE) — same pattern would apply if reported slow.

DOCUMENTATION UPDATES NEEDED: None — applied in this session (CLAUDE.md: eighteen-trigger counts at the three real sites + the new installer bullet, INV-44 list + destructive clause + gate-test cite, storage-map QA retention column, the 2026-08-28 gate entry's deferred-retention text amended, new 2026-08-28 #2 operator entry with runAllTests 296, INV-196 retention clause + Verify range, narrative counts → 686; STATE.md NEWEST #7 + Test Command counts + operator list).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
