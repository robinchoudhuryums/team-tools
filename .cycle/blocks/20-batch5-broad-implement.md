---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F-16 — the agent-facing QA reads (`getMyQaReviews`, `getMyQaReviewAudioChunk`) scoped a shared review by ROSTER NAME against a free-text `Agent` cell, so two roster rows with one name (or a typo) released one agent's review to another. F-10 — `applyTagTransformAcrossReps_` swallowed an unreadable rep Sheet: rename/merge reported success and the audit row counted N−1 reps while that rep's notes kept the old tag. F-11 — the DeptRequests tab carries `PatientTrx` (operator testing note 6) yet two comments called the store PHI-free, the storage map omitted it, and Storage Health never probed `DEPT_REQUESTS_SS_ID`; by default those rows sit on the payroll sheet. F-17 — the QA exemption button's `onclick` literal broke on an apostrophe (`esc()` had already turned it into `&#39;`, so the `.replace(/'/g, "\\'")` was a no-op and the handler threw). F-24 — a doc with a blank `ContentHash` skipped the integrity gate at signing and verified as `tampered:false`. F-23 — "TX, 100 miles of Dallas" parsed as the radius alone, dropping the state. F-27 — the intake email rendered whatever LABEL text the client sent beside each answer (the English rule was client-side only).
Files modified: web-app/00_config.js, web-app/90_qa.js, web-app/30_callnotes.js, web-app/50_deptrequests.js, web-app/10_core.js, web-app/60_intake.js, web-app/70_kb.js, web-app/81_empdocs.js, web-app/qa/script_qa.html, web-app/cn/script_callnotes.html, web-app/train/script_empdocs.html, test/client/run.js, test/client/server-split-manifest.json, test/visual/mock.js, CLAUDE.md (storage map + generated block), docs/operator-state.md, .cycle/STATE.md

Estimate: M (~6 h) — recorded 2026-09-18 in STATE.md BEFORE the first edit
Actual: ~2.5 h

CHANGES:
F-16 | web-app/00_config.js, web-app/90_qa.js | `QA_RECORDINGS_HEADERS` gains a trailing `AgentId` (`QAR.AGENT_ID: 14`, plain-text column O, header self-heals via `getOrCreateQaSheet_`, the sync row carries its empty cell). `qaSetRecordingAgent` writes the roster id resolved at attribution beside the name; `qaRosterIdByName_` resolves through new pure `qaRosterIdsForName_` and returns `''` for an AMBIGUOUS name (two included roster rows) instead of the first row. New pure `qaRowIsMine_(row, emp, nameUnique)`: an `AgentId` cell wins outright (equal to the caller's id or not mine); a legacy row (blank id) matches by name ONLY when `qaMyNameUnique_(emp)` says the caller's name has exactly one roster row. Both agent-facing reads scope through it; no bare name compare survives.
F-10 | web-app/30_callnotes.js, web-app/cn/script_callnotes.html | `applyTagTransformAcrossReps_` returns `skippedReps: [{id, error}]` (the skip is still right — one unreachable Sheet must not fail the other N−1 — but it is reported); `renameCallNoteTag` / `mergeCallNoteTags` append `cnTagSkippedNote_` to the audit row (ids only, INV-32) and return `skippedReps`; the Admin toast (`cnTagTransformToast_`) is a WARNING naming the reps whose notes still carry the old tag, with the remedy.
F-11 | web-app/10_core.js, web-app/50_deptrequests.js, web-app/30_callnotes.js, CLAUDE.md, docs/operator-state.md, test/visual/mock.js | `getStorageHealth` probes a "Dept Requests (PHI-adjacent)" row on `DEPT_REQUESTS_SS_ID` with the Forms fallback posture (unset → `ADP fallback`, note recommends the Intake spreadsheet; `cnHealthFindings_` warns on it as a fallback-prone store); the two "PHI-free" comments now say PHI-adjacent and why; the storage map gains the row and counts NINE stores; the DeptRequests operator entry carries the recommendation; both Storage Health fixtures and the deploy-readiness fixture carry the row.
F-17 | web-app/qa/script_qa.html | the Grant/Revoke buttons carry `data-qa-exempt="<esc(name)>"` + `data-qa-exempt-on`; ONE delegated document listener (registered once) dispatches to `qaSetExemption_` — the attribute value comes back decoded (g49) and goes to a function argument, never into innerHTML.
F-24 | web-app/81_empdocs.js, web-app/train/script_empdocs.html | `acknowledgeDoc` refuses a doc with no `ContentHash` by name ("not issued by the app — ask your manager to re-issue it") BEFORE the mismatch check; `verifyDocSignature` carries `warning: 'No content hash on record — integrity cannot be verified…'` on both its returns when `contentMatch` is null; the client renders it as a warn toast ("Integrity NOT verified") instead of the info "legacy row" toast.
F-23 | web-app/70_kb.js | in `oopEligibilityParse_`'s radius branch, the distance phrase and each matched warehouse name — together with an adjacent UPPERCASE state code that is the warehouse's own address ("Dallas TX", "Dallas, TX", "Dallas OR") — are stripped, and any UPPERCASE state code left standing makes the value `unknown` (g41). Lowercase prose ("or") is not Oregon.
F-27 | web-app/00_config.js, web-app/60_intake.js | the server holds `INTAKE_PPD_Q_EN` / `INTAKE_PPD_NOTES_EN` / `INTAKE_PMD_Q_EN` / `INTAKE_PAP_Q_EN`, copied byte-for-byte out of the client partial (the F-27 pin holds them equal — a drift costs a wrong label, never a refused send, g120); new pure `intakePpdRowsEn_(answers)` / `intakeAcctRowsEn_(formType, answers)` reproduce the client collectors' walks (headers, secondary flags, the notes pseudo-question; headers/secondary from the server layouts' 1-based rows) from the bank + the client's ANSWERS map; every preview and send path builds its rows there and the amend banners label from the bank (`intakePpdLabelEn_`). `payload.rows` is no longer read anywhere. The config comment that said "labels arrive from the client (no server bank to drift)" now records the reversal and the pin.
Pins | test/client/run.js | seven new: F-16 (`qaRowIsMine_` through id-wins / not-mine / legacy-unique / legacy-shared / unattributed / no-name; `qaRosterIdsForName_` with a duplicate and an offboarded row; the ambiguous-name resolver; both reads structurally; the sync row), F-10 (the walker DRIVEN with a throwing `getCallNotesSheet_` for one rep; the audit tail; both callers; the toast), F-11 (the probe + fallback, the recommendation, no PHI-free comment, the map, both fixtures, the warn finding via `cnHealthFindings_`), F-17 (no onclick; both data-* forms; the delegated dispatch; registered once; the `esc` round trip), F-24 (gate order; the conditional gate is gone; both warning returns; the client), F-23 (six shapes: state-before, state-after-"and", the warehouse's own state in both address forms, prose "or", uppercase "OR" adjacent vs leading), F-27 (the three banks + notes mirrored by value across vm realms; the PPD walk driven — first header, question 1, the secondary 31a, an unanswered blank, the notes tail, 49 non-header rows; the amend label resolver; the PMD/PAP walks incl. numeric vs string keys, 1-based secondary rows, the PAP Details header; the four preview/send paths never read `payload.rows`; the shared question-number regex). Eight existing pins repointed (the PPD base-body regex, the QA headers/enum/text-columns, the two QA scope regexes, the exemption manager gate, the ELIG radius case kept as-is).
Tooling | test/client/server-split-manifest.json, CLAUDE.md | manifest regenerated three times (seven new server functions; 1300 declarations); counts block pure 883 → 890.

TEST RESULTS: passed. Pure harness 890/890 (was 883 — seven new pins), DOM harness 126/126 (unchanged), `lint-server` clean (16 files), `split-manifest --check` current, `counts --check` green. Editor suite NOT RUN HERE (editor-only); registrations unchanged at 337 — but see REGRESSION RISKS for the editor tests the QA column and the intake rows change touches. Visual: `admin-system` and `qa` scenarios re-shot (see the shoot line below the block if it landed before the checkpoint; the default Admin System shot gains a fourth "Needs attention" item — the unset Dept Requests store — which is the finding).
Bite-checks — TWELVE mutations, TWELVE bite (committed file mutated, the named pin went red, file restored): B1 an id that is not mine falls through to the name → F-16 · B2 a legacy row ignores uniqueness → F-16 · B3 an ambiguous name resolves to the first row → F-16 · B4 the skip is silent again → F-10 · B5 the probe drops the ADP fallback → F-11 · B6 the onclick literal returns → F-17 · B7 a hash-less doc signs again → F-24 · B8 verify says nothing → F-24 · B9 the leftover state is ignored → F-23 · B10 the preview reads client rows → F-27 · B11 the server bank drifts by one word → F-27 · B12 the secondary flag is lost → F-27.
Regression Scenarios (Test Command is `manual` — no editor or browser here):
- S1 / S2 | Test Suite | NOT RUN HERE — editor-only. Two families may need attention on the next full run: (a) any QA integration test that asserts a recordings row WIDTH or writes a 14-cell row (the header self-heals, `QA_RECORDINGS_HEADERS.length` is now 15); (b) `test_intake_send_unauthorizedRejected` and the intake preview/send tests pass `payload.rows` — ignored now; a test that asserts a client-sent label appears in the email body would fail (none was found by grep; the labels the server renders are the same English strings).
- S97 Admin → System | Client (Call Notes) | PASS by fixture: the default fixture raises a fourth warning ("Dept Requests (PHI-adjacent) not set — falls back to the ADP sheet"); the all-clear fixture stays clear (its row is configured).
- S59 / S60 Intake PPD · PMD/PAP | Server, Client (Intake) | NOT RUN HERE — browser. The preview body is byte-identical for an English completion (same labels, same order); walk ONE Spanish-language PPD to the preview and confirm the labels are English (they were already, from the client; now they are from the server).
- S62 Reference / S112 OOP eligibility | Server | NOT RUN HERE. An OopPricing Area Eligibility cell of the form "TX, 100 miles of Dallas" now reads UNKNOWN in the diagnostics instead of a radius — if the operator's sheet carries such a cell, it will show up in Manage → Admin → OOP pricing diagnostics as unreadable, which is the honest verdict until the cell is split.
- QA scenarios (S9x My Reviews / coverage) | Server, Client (QA) | NOT RUN HERE — browser + Drive. Walk: attribute a recording, confirm the AgentId cell fills; share it; the agent sees it in My Reviews; a second agent with the same NAME on the roster does not. Grant/revoke exemption on a coverage row whose name carries an apostrophe.
- S75 Admin tag taxonomy (rename/merge) | Server, Client (Call Notes) | NOT RUN HERE — browser. Un-share one rep's Notes Sheet from the deployer and rename a tag: the toast is a WARNING naming that rep, and the audit row carries `skipped=1 (<id>)`.
- S46 / S91 / S98 | Time Clock | NOT APPLICABLE — untouched.

REGRESSION RISKS:
- F-16 tightens the agent-facing reads: a legacy shared recording (blank AgentId) whose agent's name is SHARED by two roster rows is now invisible to both until a reviewer re-attributes it (which fills AgentId). Reviewers attributing a recording to an ambiguous name get `agentEmpId: ''` in the return (the coaching hand-off shows no id) — honest; the row's name is still written.
- F-16 widens the QaRecordings header; a sheet provisioned before this deploy self-heals on the next `getOrCreateQaRecordingsSheet_` call (a write path); the read-only paths read width 15 on a 14-column grid safely (blank).
- F-27 changes the email's ROW SOURCE: an English completion renders identically; a client that sent Spanish labels (the g43 incident) now renders English. Any client-side field whose label differs from the bank's (there is none — the collector reads the same bank) would differ.
- F-27's bank is a client↔server mirror by construction; the pin holds them equal, so a client bank edit without the server copy turns the harness red rather than shipping a drift.
- F-23 narrows the radius parse: a real sheet cell that names a state beside a radius flips from "radius" to "unknown" (fail-closed; the diagnostics name it). Prose "or" and an address-form state are unaffected.
- F-24 refuses to sign a hash-less doc; every app-issued doc has a hash, so only a hand-entered `EmpDocs` row is affected — it now says why instead of signing blind.
- F-11's Storage Health row adds one probe (the ADP sheet again when unset — already open) and one warning on every deployment that has not set the property, which is the finding.

INVARIANTS AT RISK: none violated. INV-32 (the tag audit tail carries ids only; the QA audit rows are unchanged), INV-41 (the intake preview/hash contract: both halves now build rows the same way from the same payload), INV-96 (unchanged validation posture), INV-113/122 (the doc-signing witness path is unchanged; the gate is stricter), INV-185 (fixtures updated for the new store row and the readiness item), INV-186 (the all-clear fixture still reaches "Nothing needs attention"; the new warning fires only on the unset property, which is a real condition), INV-196 (the QA gate tier is unchanged — the agent-facing read stays employee-gated, now id-scoped), INV-209 (the eligibility fail direction is still UNKNOWN on unreadable data), the g43 rule is now held SERVER-side.

NET SCORE: 3 − 0 = 3
  a) Would this have fired in production this month? F-16 NO-as-observed (needs two roster rows with one name, or a typo in the free-text Agent cell — defensive; the class is a PHI-adjacent release, so it was worth doing). F-10 YES for the class (a rep Sheet the deployer cannot open is the g47/g89 recurring condition; whether a rename ran against one this month is unverified — counted because the success report is wrong on every occurrence). F-11 YES (every DeptRequests row since 2026-09-10 names a patient on the payroll sheet by default, and nothing said so — a visibility fix on a live condition). F-17 YES (any agent name with an apostrophe made Grant/Revoke throw on click — a user-visible interface defect on a path managers use). F-24 NO (no hash-less issued doc is known — defensive). F-23 NO-as-observed (no such cell is known on the operator's sheet — defensive). F-27 NO (the client already sends English; this moves the enforcement server-side — defensive/structural, the g43 hardening).
  b) New failure mode? NO for all seven. The one behaviour a reader should know: a legacy shared recording under a duplicated roster name is invisible until re-attributed.

OPERATOR ACTIONS / DEPLOY:
- `clasp push -f` + cut a New version. | BLOCKS DEPLOY: Y
- `runSmokeTests` on prod — registrations still 337; read the count off the run. | BLOCKS DEPLOY: N
- Manage → Admin → System after the deploy: the Storage inventory shows "Dept Requests (PHI-adjacent)" — set `DEPT_REQUESTS_SS_ID` to the Intake spreadsheet's id to clear the warning and move the patient-naming rows off payroll (existing rows stay on the ADP tab; move them by hand or leave them — the tracker reads the configured store only). | BLOCKS DEPLOY: N
- QA: re-attribute any shared recording whose agent's name is shared by two roster rows (the AgentId cell is blank on rows attributed before this deploy). | BLOCKS DEPLOY: N
- If an OopPricing Area Eligibility cell names a state beside a radius, split it (or accept UNKNOWN). | BLOCKS DEPLOY: N
Deploy: Server — `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. Client (QA, Call Notes, Training/Employee Docs views) — same push, same New-version step.

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- `intakePpdLabelEn_` rebuilds the row list per lookup (the amend banner maps a handful of keys; trivial cost) — a memo would be tidier.
- The states branch of `oopEligibilityParse_` reads "TX or CA" as Texas + Oregon + California ("or" is a token that IS a state code when uppercased) — the same class F-23 just closed for the radius branch, untouched here.
- Existing DeptRequests rows stay on the ADP sheet after `DEPT_REQUESTS_SS_ID` is set; there is no migration tool (the SLA reminder and tracker read the configured store only).
- The GATE-SHAPE pin follows the FIRST `return helper_(` in an endpoint as a "delegate" — a callback's return inside the endpoint (as F-27's first draft had) is mis-read as delegation. Worth a tighter regex (top-level `return` only).
- A DOM pin for F-17 (render a coverage row with an apostrophe and click) would beat the structural one; the DOM harness has no QA boot path today.

DOCUMENTATION UPDATES NEEDED:
- `docs/gotchas.md` + CLAUDE.md index: g43 amended (the English rule is now held server-side: the banks + `intakePpdRowsEn_` / `intakeAcctRowsEn_`; the client bank is mirrored and pinned); g49/g91 family — a new line for "an onclick literal cannot carry a name (F-17)" or an amendment to g91; g41/INV-209 — the F-23 states-beside-radius rule.
- `.cycle/config.md`: INV-196 amended (the agent-facing read is ID-scoped; `AgentId` column; ambiguous names release to nobody); INV-209 amended (states + radius → unknown; the address-form exception); a new invariant for "a cross-rep walk that skips a member REPORTS the skip" (F-10); the storage map's ninth store in the Subsystems section if it lists stores; scenarios: the QA My Reviews scenario (AgentId + duplicate-name step), S75 (skipped-rep toast), S59/S60 (Spanish completion → English labels from the server).
- `docs/operator-state.md`: the QA module entry (the `AgentId` column, header self-heals); a `DEPT_REQUESTS_SS_ID` inventory line in CLAUDE.md (it is documented only inside the DeptRequests entry today).
- `docs/design-decisions.md`: the intake-email decision (labels from the server bank; the mirror + pin), the QA gate-tier entry (id scoping), the tag-taxonomy entry (skips reported).
- `docs/modules.md` (QA, Intake, Call Notes admin), `docs/operator-log.md` (a Batch 5 entry), `docs/test-harness-log.md` (the seven pins; the cross-realm array comparison; the GATE-SHAPE delegate mis-read).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
