---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F1 (breakPairs_ pairs with independent cursors — a dropped `in` is dropped alone), F6 (buildAdjustPunchIndex_'s precondition is re-derived for the bulk-approve caller and the resume path drops the stale index), F2 (getMyPendingTasks tells a deliberately-unset store from a failed read), F4 (every completing flow clears the rep's cached Needs-you list)
Files modified: web-app/Code.js, test/client/run.js, test/client/dom/runDom.js, test/visual/mock.js

CHANGES:
F1 | web-app/Code.js (breakPairs_) ; test/client/run.js (A1 pin) | Pairing walked the two sorted lists with ONE index, so an `in` that could not close its `out` advanced BOTH cursors: a single stray early LunchIn shifted every later `in` one slot and un-paired the whole day, so the rep was PAID for every real break they took — the over-payment the 2026-09-01 multi-break round exists to prevent, arriving through the other door. Now greedy with independent cursors; each `out` takes the earliest `in` that can close it, and a dropped `in` is dropped alone. Verified by executing the real function: every pre-existing case is byte-identical (single 8.5h, two pairs 8.0h, extra out 7.5h, inverted single 8.0h, overnight two-break 7.167h), and the two broken shapes now pair. The A1 pin gained the IN-direction cases it never had — which is exactly why this survived the round that wrote it.
F6 | web-app/Code.js (buildAdjustPunchIndex_ doc, punchAdjustDecideAll_) ; test/client/run.js (B3 pin) | The index's doc comment asserted a universal — "each (date, type) is written at most once per run, so the index never needs updating mid-run" — that `managerSaveDayRange` satisfies and `punchAdjustDecideAll_` does not: a `resume` decision writes through resumeShiftForEmployee_ instead, retyping the ClockOut row to ADJ-LunchOut and appending an ADJ-LunchIn, so three keys for that date go stale mid-batch. The dangerous read (a later ClockOut `set` landing on the row the resume just converted, undoing the unpaid gap) is unreachable today ONLY because of a distant (date, punchType) dup guard at submit — too far away to rest a payroll write on. The loop now discards that employee's cached index after a resume, and the comment states the precondition a caller owes plus the exception, so the next write path added to a ctx-bearing loop inherits a true statement.
F2 | web-app/Code.js (storePlaceholder_/storeConfigured_ hoisted + added, getStorageHealth delegates, getMyPendingTasks) ; test/client/run.js (PR6-1 + a new behavioural pin) ; test/client/dom/runDom.js (PR6 silence assertions) ; test/visual/mock.js (both fixtures) | A store the operator has deliberately NOT configured was reported as a failed read, and that cost three ways at once. On a deployment with no HR_DOCS_SS_ID (no fallback store, by design — INV-122) both HR-backed sources threw on EVERY call, so: every rep saw "Couldn't check coaching, employee docs" on every Dashboard paint forever — an indicator that can never be clean (INV-186); the block never reached its clean-empty state and so never disappeared, defeating PR 6's own "renders NOTHING when there is nothing outstanding" decision; and `!unavailable.length` meant the 2-minute cache was NEVER written, so the app's first-paint surface re-paid six server reads on every render and every focus wake, permanently. Those kinds now ride an additive `notConfigured[]`: honest in the payload, silent in the UI (an unset store is Storage Health's to report, and it already does), and never a reason to skip the cache. The gates ASK the store rather than string-matching an error message, and an active test override counts as configured so a fixture run never classifies as unset. The placeholder test is hoisted out of getStorageHealth so "is this store set up?" has ONE definition. The CLIENT needed no change — `unavailable` keeps its exact old meaning — which is the sign the shape is right: deploy skew is safe in both directions.
F4 | web-app/Code.js (pendingTasksBust_ + 7 call sites) ; test/client/run.js (new pin) | The Needs-you block is the first thing on the Dashboard, and a rep who had just DONE the thing it names went back to a list still showing it for up to two minutes. Every completing flow now drops that rep's cached entry: training marked read, a quiz PASS (hooking only the read path would have been a half-fix that looks whole), a coaching acknowledgement, a signed or completed doc, a scheduled call closed, and both dept-request resolve paths — the shared writer clears the SENDER (both paths route through it), and the in-app path also clears the RESOLVER, the one case where the acting rep is not the owner. Keyed per rep, so a targeted delete is enough and no generation salt is needed; a salt would evict every rep's entry on every resolve, too blunt for a 2-minute cache on the busiest surface. Best-effort by construction — a cache miss is the correct fallback, so it can never throw into a write that already succeeded.

TEST RESULTS: passed.
  - Pure harness `node test/client/run.js`: 774 passed, 0 failed (772 at baseline; +2 new tests — the F2 storeConfigured_ behavioural and the F4 wiring pin. The A1 / PR6-1 / B3 / PR6-2 additions grew IN PLACE).
  - DOM harness `node test/client/dom/runDom.js`: 108 passed, 0 failed (assertions added in place: a not-configured source renders NOTHING, real items still render beside it, and it never borrows the unreadable-source warning or the degraded-freshness gate).
  - `node --check` clean on Code.js, Tests.js, DevTools.js, mock.js.
  - Visual: `clock-light-wide`, `clock-needsyou-empty-light-wide`, `clock-needsyou-error-light-wide`, `clock-light-wide-pht` re-shot — 0 overflowPx, 0 missing fixtures, no non-font console errors. Read back from the live DOM, the block names ONLY "scheduled calls" (the `unavailable` source) while `notConfigured: ['docs']` is silent — the F2 client half, on camera.
  - EVERY change bite-checked: 11 mutations, 11 bites (F1 index-locked loop restored; F6 invalidation removed / moved before the write / false universal restored; F2 coaching re-classified as unavailable / notConfigured gating the put / test-override escape removed / fixture drift; F4 quiz-pass bust removed / sender busted by email / helper made throwing). The fixture-drift mutation initially read as NO-BITE because I ran the DOM harness — it is the PURE harness's PR6-2 derived key check that holds it (INV-185). Every bite-check ran against a COMMITTED tree, per the documented `git checkout` hazard.
  - Regression Scenarios (Test Command is `manual`) — walked below.
  - One transient: the first `shoot.mjs clock` run timed out navigating `clock-light-wide-pht` after 9 scenarios succeeded; re-run alone it passes clean. Environmental (the sandbox's blocked Google-Fonts request occasionally holds `load`), not caused by these changes.

REGRESSION SCENARIOS (Subsystems touched: Server, Test Suite):
  Every scenario in this project targets the DEPLOYED Apps Script project with live spreadsheets, so none is executable in this container. Recorded below as PASS where a harness / direct execution / the visual matrix covers the same assertion, and NOT APPLICABLE otherwise — with the blast radius named so the post-deploy walk is scoped rather than exhaustive.
  - S95 (multi-break day counted, displayed, repaired) | PASS (arithmetic half) — the operator's split-shift day re-verified at 10.5h by executing the real calcHours_/breakPairs_; three-pair and overnight-two-break cases hold. Day Edit + sheet-doctor halves: NOT APPLICABLE (live project).
  - S7 (Day Edit diff-and-apply, A4 break list) | NOT APPLICABLE — live project. In the F1 blast radius: managerPlanDay_ pairs SHEET ROWS through breakSortKey_, not breakPairs_, so the reconcile is untouched; the DISPLAYED pairs on the modal come from the same greedy rule as the arithmetic, which is the property S7 asserts.
  - S3, S8, S39, S46, S79, S88, S91, S96 (hours on the clock path, ADP export, accrual credit, pay statement, team calendar, resume) | NOT APPLICABLE — live project. All read hours through calcHours_, so all move together with F1 and none has an independent pairing rule (pinned by A2).
  - S92 (approved adjustment catches up), S96 (resume) | NOT APPLICABLE — live project. F6's blast radius; the bulk-approve contract is pinned in B3.
  - S101 (Time Clock surface — Needs you) | PASS (client half, on camera + DOM) — the block leads the main column, names only genuinely unreadable sources, renders nothing on a clean-empty round, and renders the error card on a failed round. NOTE: F2 makes S101's "with nothing outstanding the block is ABSENT" reachable on a store-unset deployment, where it previously could not be.
  - S67, S68, S69, S74, S99 (training complete, quiz pass, doc sign, dept requests, coaching ack) | NOT APPLICABLE — live project. F4 adds a best-effort cache delete after each of their existing writes; no assertion in any of them changes.
  - S97 (Admin → System findings-first), S104 (Drive capability) | NOT APPLICABLE — live project. getStorageHealth's behaviour is unchanged (its local isPlaceholder now delegates to the hoisted twin; pinned).
  - S1 / S2 (smoke + full editor suite) | NOT APPLICABLE — editor-only. Scanned Tests.js for doubles of every changed area: the five calcHours_ smoke cases and test_calcHours_multipleBreaks all use shapes whose pairing is byte-identical under the fix (re-verified by execution); the getMyPendingTasks and updatePunchAdjustStatusBulk cases assert gate/shape only, both unchanged. Expect 308/308 unchanged post-deploy.
  - Every other Server-subsystem scenario | NOT APPLICABLE — outside the blast radius (no shared code path with the four fixes).

REGRESSION RISKS:
  - F1 CHANGES COMPUTED HOURS on any historical day carrying a break stamp that could not be paired positionally — always DOWNWARD, because such a day previously computed as if no break had been taken. That is the fix, but an operator should know it before someone asks. No existing report enumerates the affected days: the sheet doctor's inverted-lunch test is `last LunchIn <= first LunchOut`, which does not match this shape, and `reportMultiBreakDays()` compares against the last-wins rule, not against index-locked pairing. Logged as a follow-on.
  - F1 interface unchanged ({out, in, minutes}); all four breakPairs_ consumers keep their shape, and the two display builders now agree with the arithmetic on exactly the days where they previously disagreed.
  - F6 costs one extra Timesheet read per bulk batch that CONTAINS a resume (never otherwise). It sits inside the project ScriptLock, so it is a real cost — bounded at one read, against the alternative of reasoning about which stale key a later request might read.
  - F2 narrows what the rep is warned about: on a store-unset deployment they no longer see "couldn't check coaching, employee docs". Deliberate — the feature does not exist there, Storage Health reports the unset store, and the previous warning could never clear. `notConfigured` keeps it visible in the payload for any diagnostic that wants it.
  - F4 adds a CacheService.remove inside two already-locked regions (markDeptRequestResolved_, and the rep-side flows). Matches the existing drBumpCacheGen_ placement; not a mail call, so the M-7 no-mail-in-lock rule is untouched (its tripwire still passes).
  - Old behaviour better anywhere? No case found. F1 is strictly more pairs (never fewer) and never pairs an `in` with an `out` it precedes; F2/F4 are additive; F6 only discards a cache.

INVARIANTS AT RISK: None violated. Four touched, all in the strengthening direction:
  - INV-176 (every break pair deducted): strengthened. Its wording "Pairing is then positional" is now INACCURATE and owes a doc update.
  - INV-129 (cache only clean rounds): F2 redefines "clean" to exclude deliberately-unset stores. The spirit — never pin a DEGRADED round — is preserved, because an unset store is not degraded: it answers the same way on every call. Owes an amendment note.
  - INV-186 (an indicator that can never be clean is worse than none): F2 is a direct application of it to the Needs-you warn line.
  - INV-187 / INV-156: preserved — `unavailable` keeps its exact meaning, and the client's freshness gate still reads only it.
  - INV-185 (fixtures mirror the server): both getMyPendingTasks fixtures updated and held by PR6-2's derived key check.
  - C17-9 (one-read index) / INV-155 (last-row-wins agreement): F6 preserves both and makes the caller's obligation explicit.

NET SCORE: 1 − 0 = +1
  a) Would it have fired in production THIS MONTH?
     F1 NO — needs a stray/inverted break stamp on a day that also has a good pair; the live punch path cannot produce one (getNextActions_), so it takes a hand edit, a mis-keyed adjustment, or a Day Edit slip. A real payroll defect whose blast radius was the WHOLE day rather than the one bad row, but not one I can claim fired this month.
     F6 NO — latent. The submit-time (date, punchType) dup guard makes the harmful read unreachable today; this fixes the false precondition, not a live failure.
     F2 NO on this deployment — coaching and Employee Docs are in active use, so HR_DOCS_SS_ID is configured and both sources read normally. It fires on any deployment where a no-fallback store is unset, which is the documented supported state.
     F4 YES — ordinary daily use. Any rep who completes a training item, acknowledges a coaching note, signs a doc or closes a call-back and returns to the Dashboard inside two minutes saw the finished item still listed. The block shipped 2026-09-02.
  b) New failure mode introduced? F1 NO. F6 NO. F2 NO (the narrowed warning is a deliberate, documented trade, not a silent loss — Storage Health owns it). F4 NO.
  Strict re-derivation per the template's question, as the cycle-17/18 reflections require: three of the four are structural/latent fixes and score 0, one is a live fix. Net +1.

OPERATOR ACTIONS / DEPLOY:
- None new — no Script Properties, triggers, migrations, CONFIG values or OAuth scopes. | BLOCKS DEPLOY: N
- After deploying, expect computed hours to be LOWER on any day that carried an unpairable break stamp (F1). Nothing needs correcting — the figures were wrong before and are right after — but the direction is always down, never up. | BLOCKS DEPLOY: N
- Post-deploy: run `runAllTests()` — expect 308/308, unchanged (no Tests.js change in this batch). | BLOCKS DEPLOY: N
Deploy: Server + all client partials ship together — `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. This rides the SAME deploy already owed for PRs #230/#232/#233 (see STATE.md); nothing here needs a separate one. Test Suite changes are local-only and never reach the Apps Script project.

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- A `reportBreakPairingChanges()` twin of `reportMultiBreakDays()` — editor-run, read-only, manager-gated — would enumerate the historical days whose hours move under F1 (old index-locked pairing vs greedy), the way the multi-break round enumerated its own. Not written: out of this batch's scope, and F1's affected shape is rarer than the multi-break one.
- The sheet doctor does not detect F1's shape. Its inverted-lunch test is `last LunchIn <= first LunchOut`, so a day with outs=[12:00] and ins=[11:00, 12:30] passes it while being exactly the damaged data F1 now survives. Widening that detector is a separate, operator-visible change.
- F4 residual, deliberate and stated in the code: another member of the same receiving desk keeps a resolved dept request in their incoming list for up to PENDING_TASKS_CACHE_TTL (120s). Closing it needs a generation salt, which would evict every rep's entry on every resolve.
- `submitCallNote` is NOT hooked into F4. Filing the missing notes would change the notes row, but that row derives from getMyMetrics' own 5-minute cache, so busting the pending-tasks key alone cannot change the answer — a claimed fix that does not fix. Closing it means invalidating the metrics cache too, which is a wider decision.
- Batches 3 and 4 of the audit plan (F3; F8/F7/F5/D1) remain unimplemented and were explicitly out of scope.
- The visual matrix count in CLAUDE.md reads 101 while `shoot.mjs` runs 99, and STATE.md's scenario list stops at S103 while CLAUDE.md defines S104 — two documentation drifts noticed during the audit, unrelated to these fixes.

DOCUMENTATION UPDATES NEEDED:
- INV-176: "Pairing is then positional." is now inaccurate — pairing is greedy with independent cursors, and the reason (a dropped `in` must be dropped alone, or one stray stamp un-pairs the whole day) belongs in the invariant, not only in the code comment.
- INV-129: amend with F2's rule — cache only a round where every CONFIGURED source was readable; a deliberately-unset store is not a degraded read.
- The PR 6 "Needs you" Key Design Decision: it enumerates six sources "each in its own try/catch that pushes its kind to `unavailable[]`". Add `notConfigured[]`, and the reason the UI is silent about it (Storage Health already reports an unset store — INV-186).
- The C17-9 / buildAdjustPunchIndex_ note in the Key Design Decisions: state the precondition a ctx-bearing caller owes and name the resume exception, as the code comment now does.
- Common Gotchas: F4's rule is worth an entry — a per-rep result cache on a surface that lists TASKS owes an invalidation from every flow that completes one, because the user's next act after completing is to go and look.
- Test-count lines: pure harness 772 → 774; DOM stays 108.
- S95's expectation could name the in-direction case explicitly, so the scenario covers what the A1 pin now covers.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
