---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: BCP-1 — the break coverage planner integrated into the Admin "Break schedules" card (operator 2026-09-03): a 15-minute who-is-on-the-desk strip from the editor's UNSAVED state, over a background layer of average inbound call volume by time of day from the CDR Report's `Inbound Calls` export tab (confirmed by the operator to exist in the call-data-reporting repo); BCP-2 — the Coverage planner now SUBTRACTS breaks (it counted a rep on lunch as present — found while planning BCP-1, in scope because the strip and the planner must agree about who is on the desk).
Files modified: web-app/Code.js, web-app/cn/script_callnotes.html, web-app/Tests.js, test/visual/mock.js, test/visual/shoot.mjs, test/client/run.js, CLAUDE.md, .cycle/STATE.md

CHANGES:
BCP-1 | web-app/Code.js | CONFIG: `CDR_INBOUND_TAB`, `CDR_INBOUND_PST_TO_CST_HOURS` (a hand-mirror of call-data-reporting's `INBOUND_HEATMAP_CST_SHIFT_HOURS`), `BREAK_COVERAGE_SLOT_MIN` 15, `BREAK_COVERAGE_VOLUME_DAYS` 28, `BREAK_COVERAGE_VOLUME_MAX_ROWS`, `BREAK_COVERAGE_VOLUME_CACHE_SEC` 3600, `BREAK_COVERAGE_VOLUME_QUEUES` (the four Customer Success entry queues; empty = all inbound). `withBreakSchedulesProp_(prop, fn)` swaps the per-execution property memo so the ONE resolver (INV-149) sees a draft, restored in `finally`. Pure `breakCoverageSlots_` (slot bucketing; partial overlap = away; `away[]` names who + which break) and `inboundVolumeBuckets_` (header-NAME column discovery, PST→CST shift with `% 1440` wrap, internal/weekend/queue filters, distinct-weekday denominator, missing column NAMED with no slots). `breakCoverageAgents_` resolves every INV-183-included rep under the draft with the rep id, converts to the CST anchor on today's date via `convertDateTime_`, names a rep it cannot place. `getCdrInboundVolume_`: display-value reads only (INV-64), widening tail scan bounded by the max-rows key with truncation reported, `unavailable:` on every failure path (tab missing / empty / pre-extension columns / no rows / read error), cached on a clean untruncated round only (INV-129), bypassed under the CDR test override. `getBreakCoverage(payload)`: admin-gated READ (bare `{error}` — INV-136's 49th), `payload.draft` run through `breakSchedSanitize_` (what the strip previews is exactly what a Save would store), `withVolume` opt-in.
BCP-2 | web-app/Code.js | Pure `coverageSplitAtBreaks_(absStart, absEnd, breaks)` (shift-relative offsets — tz-free; clip/ignore/union); `getCoveragePlan` pushes the split sub-intervals instead of the whole shift. Hourly granularity still counts a rep present for an hour they left for 15 minutes — stated in the code; the strip is the finer view.
BCP-1 | web-app/cn/script_callnotes.html | `cnBreakCollectDraft_(lenient)` — ONE collector for Save (strict, unchanged messages) and the strip (lenient: a half-typed row is skipped, never blanks the preview). The strip host leads the editor; `cnBreakCovRefresh_` is seq + view guarded (INV-156), fetches the demand layer ONCE per open, keeps last-good with a warn line on a failed refresh and renders `errorStateHtml_` cold (C17-5); debounced 450 ms on input/change and on the editor's structural clicks (bound AFTER the mutating handler). `cnBreakCovStripHtml_`: hour labels, real `<button>` cells with `aria-label` + `aria-pressed` toned by the planner's thresholds, the volume as a background bar scaled to the window max, legend, a who-is-away detail line (`[hidden]` companion rule), the volume caption (through-date, queues, truncation) or the NAMED unavailable reason, the "Live preview" note incl. skipped reps. Grid columns computed from the slot count (FO-A2); `.cn-bcv-wrap` scrolls internally with a 560px min grid.
BCP-1 | web-app/Tests.js | omnibus admin-gate case + `ADMIN_GATED` entry for `getBreakCoverage`.
BCP-1 | test/visual/mock.js, shoot.mjs | `getBreakCoverage` fixture is a FUNCTION of the payload (F14 — volume only when asked), keys mirroring three server literals (INV-185); eight staggered agents so all three tones + the away list are on camera. New `admin-config-covfail-light-wide` (`?failrpc=getBreakCoverage`).
BCP-1/2 | test/client/run.js | BCV-1..4 (see the count chain); BRK-4 rewritten in place.

TEST RESULTS: pure 749/0 (was 745), DOM 104/0, `node --check` Code.js + Tests.js clean. 11 mutations / 11 bites (partial-overlap rule; the split's cursor/union; the internal filter; the missing-column early return; the PST shift; the memo swap's finally; the admin gate; the planner's whole-shift push restored; the strip's lenient collector; the failure handler's seq guard; a dropped fixture key). Two pins were wrong about the CODE on first write and were corrected: the vm-realm `deepStrictEqual` trap, and a return-literal regex that ran past the endpoint's own `catch` and read `{ error }` as a missing fixture key. Visual: `admin-config-light-wide` / `-mobile` / `-covfail-light-wide` shot, overflow 0, no missing fixture; the strip cropped and read at wide (all three tones, the lunch trough to 0 with six names in the detail line), mobile (scrolls inside its own box), and the cold-failure card; the demand bars were too faint on the first crop and were darkened. Regression scenarios (Test Command = manual): S102 (new) PASS on the fixture for every step except the live-sheet rename, which is an operator check; S72 PASS via BCV-3 + the split pin (the planner fixture carries no breaks, so the amber shift is not on camera — noted as a follow-on); S76 PASS unchanged (the reminders read the same resolver); S98 NOT APPLICABLE (Punctuality reads breaks for lunch adherence only; no change). Editor suite not run (no Apps Script runtime off-editor); expected count unchanged at 308.
REGRESSION RISKS: (1) Managers WILL see more amber/red hours on Coverage — the fix reporting a gap that was always there; documented as a behaviour change in the operator entry. (2) `getBreakCoverage` runs the roster through `convertDateTime_` per rep per open plus a debounced refresh per edit — one cached roster read + N cheap conversions; the heavy CDR read rides only on `withVolume` and is server-cached 1h. (3) The memo swap is scoped to one read-only endpoint; a THROW inside `fn` still restores via `finally`. (4) A pre-extension `Inbound Calls` tab (15 columns) yields a named unavailable, never wrong bars. (5) `cnBreakCollectDraft_(false)` reproduces the old Save messages verbatim; `rm` falls back to 10 only in lenient mode.
INVARIANTS AT RISK: INV-149 (single resolver — HELD: the draft reaches both layers through the memo swap, no second merge path; the F-tripwire zero-bare-`getShiftSchedule_` scan passes); INV-136 (count 48 → 49, list + omnibus + F7/F9 tripwires updated in the same commit); INV-127 (amended: breaks subtracted); INV-64 (display values throughout the foreign-tab read — pinned); INV-129 (clean-round cache — pinned); INV-187 (five named failures; no strip of zeros — pinned); INV-156 (seq guards both handlers — pinned); INV-173/174 (real pressed cells — pinned); INV-185 (fixture keys derived from three literals — pinned); INV-193 n/a (no `data-*` round trip — the strip re-reads server state, not attributes).
NET SCORE: 1 − 0 = 1 (BCP-2 is a production fix — the Coverage planner has drawn a fully staffed noon over a team at lunch since it shipped; BCP-1 is a capability)

OPERATOR ACTIONS / DEPLOY:
- Confirm the CDR Report spreadsheet carries the `Inbound Calls` export tab with columns 16–17 (`Call Start`, `Is Internal`) and that call-data-reporting's "Inbound Calls tab export" trigger is installed (its Op State #49) — otherwise the strip renders without the demand layer and says so | BLOCKS DEPLOY: N
- Check `CONFIG.BREAK_COVERAGE_VOLUME_QUEUES` names the queues you want counted as THIS team's demand (seeded with the four Customer Success entry queues; empty = every inbound queue) | BLOCKS DEPLOY: N
Deploy: Server + Client (Call Notes views) + Test Suite: `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy; `runAllTests()` expects 308.

FOLLOW-ON ITEMS:
- The Coverage planner fixture carries no breaks, so BCP-2's amber shift is verified by pin only; a fixture rep with a noon lunch would put it on camera.
- The strip is a SCHEDULE view; a dated variant (today's approved PTO overlaid, the Coverage planner's read) is a small extension once the operator wants "today" rather than "the plan".
- `CDR_INBOUND_PST_TO_CST_HOURS` is a hand-mirror across two repos; a cross-repo pin is not expressible here — the operator entry names both constants.
- The ad-hoc Playwright crop script (scratchpad `crop-brk.mjs`) earned its keep twice today; promote it into `test/visual/` as a `--crop <selector>` option.

DOCUMENTATION UPDATES NEEDED:
- Done in this session: CLAUDE.md (operator-state entry; a Common Gotcha on break subtraction + the memo swap + the hand-mirrored shift; the SHIFT_SCHEDULE decision's planner paragraph; INV-127 and INV-136 amendments; S72 expected; new S102; the count chain → 749 / matrix 94), STATE.md, this block.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
