---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F-01 — oopVerifyQuotes_ keyed the live OopPricing sheet by column A (HCPCS on the operator's sheet) while the picker's name came from the header-discovered Item column, so every price-quoted external send was refused. F-04 — checkOopEligibility scored column A too, and the diagnostics named unknown eligibility values by code. F-03 — calcHours_ wrapped `out <= in` as overnight and timeToMins_ drops seconds, so a same-minute clock-in/clock-out paid a 24-hour day into totals, the pay statement, the accrual index and the sparkline. F-02 — the Scheduled-reminders and Scratchpad close hooks removed nothing, and closeOverlay delegates entirely to a registered hook, so Close / Escape / backdrop did nothing. F-05 — Day Edit's Save was enabled while the prefill was pending; an early Save sent blank slots, which delete punches. F-44 — intakePpdControlHtml_'s default branch referenced an undefined identifier.
Files modified: web-app/70_kb.js, web-app/20_timeclock.js, web-app/00_config.js, web-app/cn/script_callnotes.html, web-app/tc/script_manager.html, web-app/intake/script_intake.html, web-app/Tests.js, test/client/run.js, test/client/dom/runDom.js, test/client/server-split-manifest.json, CLAUDE.md (generated block), .cycle/STATE.md

Estimate: M (~7.5 h) — recorded 2026-09-17 in STATE.md BEFORE the first edit
Actual: ~1.5 h

CHANGES:
F-01 | web-app/70_kb.js | `oopVerifyQuotes_` builds `byName` from `rows[i][oopNameCol_(headers)]` — the same resolver `oopRowObj_` uses — instead of `rows[i][0]`. The fail direction is unchanged (closed); the spurious "no longer lists" refusal is gone.
F-04 | web-app/70_kb.js | New pure `oopMatchScore_(o, q)` (name OR code, whichever scores higher) is the ONE scorer for both `searchOopPricing` and `checkOopEligibility`; the eligibility filter scores `oopRowObj_(headers, rows[i])`, and `getOopPricingDiagnostics` names an unknown eligibility value by `o.name || o.code`.
F-03 | web-app/20_timeclock.js, web-app/00_config.js | `calcHours_` wraps on strict `<` (an equal-minute pair is 0 h). New pure `managerClockOrderError_(cleanSlots)` refuses an equal Clock In / Clock Out and both `managerSaveDay` and `managerSaveDayRange` apply it after format validation and before the plan. The sheet doctor's inverted-pair test compares at minute granularity (`timeToMins_`) so an equal-minute pair is REPORTED (report-only, as before). The 00_config.js doctor comment says `out<in`.
F-02 | web-app/cn/script_callnotes.html | `cnCloseSchedModal_` and `cnCloseScratchpadModal_` remove their overlay (the scratchpad still flushes a dirty save first — `cnScratchSave_` reads the field synchronously before the RPC).
F-05 | web-app/tc/script_manager.html | `deSetLoading_(true)` disables Save and labels it "Loading…"; `deSetLoading_(false)` re-enables it once the prefill lands; the failed branch is unchanged.
F-44 | web-app/intake/script_intake.html | the default branch's `aria-label` reads `ariaLabel` (the parameter), not the undefined `text`.
Pins | web-app/Tests.js, test/client/run.js, test/client/dom/runDom.js | Editor smoke test `calcHours_equalMinuteIsZeroNotADay` (registrations 336 → 337). Node: the OOP-B fake sheet now has the OPERATOR'S shape (code in A, item in C) with a guard that column A is never the item; an F-04 pin drives `oopMatchScore_` and asserts both search surfaces, the diagnostics and the verifier read no OopPricing cell at position 0; A1 gains the equal-minute cases on both sides of the wrap boundary; an F-03 pin drives `managerClockOrderError_` and checks both writers call it after format validation, that the wrap is strict in source, and that the doctor compares in minutes. DOM: Save is disabled while the Day Edit prefill is pending; the two modals close via closeOverlay, Escape and the hook path, the Close button is wired through closeOverlay (jsdom cannot run inline onclick), the dirty scratchpad flushes on close, and a sweep closes every registered hook's overlay.
Tooling | test/client/server-split-manifest.json, CLAUDE.md | Manifest regenerated for the two new server functions (1283 declarations); the generated counts block updated (pure 867, DOM 122, registrations 337).

TEST RESULTS: passed. Pure harness 867/867 (was 865 — two new pins), DOM harness 122/122 (was 121), `node scripts/lint-server.mjs` clean across 16 files, `node scripts/split-manifest.mjs --check` current, `node scripts/counts.mjs --check` green. Editor suite: NOT RUN HERE (editor-only); the new smoke test and the existing integration pin `test_oop_verifyQuotes_currentStaleAndDeleted` (which would have FAILED against the real-shape fixture before this batch) are the acceptance tests.
Bite-checks — SEVEN written, SEVEN bite (each mutation applied to a committed file, harness went red on the named pin, file restored): C1 the verifier keys column A again → OOP-B behavioural pin · C2 the eligibility filter scores column A → F-04 pin · C3 the `<=` wrap returns → A1 · C4 managerSaveDay drops the order rule → F-03 pin · C5 the doctor goes back to the string compare → F-03 pin · D1 the sched hook is a no-op again → F-02 DOM pin · D2 Save is live during the pending prefill → the Day Edit DOM pin.
Regression Scenarios (Test Command is `manual` — no editor or browser in this container):
- S1 Smoke test suite | Test Suite | NOT RUN HERE — editor-only. Expect registrations 337 (smoke 119); the new test runs on prod safely (pure arithmetic, no writes).
- S2 Full integration suite | Test Suite | NOT RUN HERE — editor-only, and `INSTANCE_IS_PROD` is unset so it must run on the dev instance. This is where `test_oop_verifyQuotes_currentStaleAndDeleted` executes against the real-shape fixture — the acceptance test for F-01.
- S3 Employee golden path | Server, Client | NOT RUN HERE — browser. Behaviour unchanged on every ordinary day; a same-minute in/out now reads 0 h instead of 24 (A1 pin).
- S7 Manager edit-day diff-and-apply | Server, Client (Time Clock) | NOT RUN HERE — browser. Two additions to walk: Save reads "Loading…" and is dead until the punches land (DOM-pinned); an equal Clock In / Clock Out is refused by name and nothing is written. Every listed step's expected outcome is otherwise unchanged (the plan is untouched; the rule sits before it).
- S8 ADP-format export | Server | NOT APPLICABLE beyond calcHours_ — reads through the same arithmetic; an equal-minute day exports 0 h, not 24.
- S18 / S19 / S31 / S32 Call Notes submit, composer, optimistic, sticky draft | Call Notes | NOT APPLICABLE — the changed hooks belong to the reminders and scratchpad modals, which these scenarios do not open.
- S59 Intake PPD | Server, Client (Intake) | NOT APPLICABLE — the default branch is unreachable while every control kind has a case; the fix is for the first new kind.
- S79 / S88 pay statement · S91 team punches calendar · S107 / S108 accrual | Server, Client (Time Clock) | NOT APPLICABLE beyond calcHours_ — all read through `punchDayAdd_` + `calcHours_`; an equal-minute day now contributes 0 h to every one of them instead of 24.
- S95 multi-break day | Server, Client (Time Clock) | PASS by harness — every listed figure (10.5 h, 8.5 h, the overnight cases) is byte-preserved in A1 and the editor smoke tests; the doctor's report-only posture on the pairs it kept is unchanged. NOT RUN in the editor.
- S110 OOP pricing end to end | Server, Reference, Call Notes | NOT RUN HERE — browser + real sheet. **This is the walk that FAILED before this batch at "Re-insert at the new price and Send for real" (refused as "no longer lists") and at the eligibility check by item name ("No item matched"). Both are expected to pass now; walk it first after the deploy.** The diagnostics now name unknown eligibility values by ITEM.

REGRESSION RISKS:
- calcHours_ equal-minute pairs now compute 0 h where they computed 24 h. Any historical day in that state changes its total on the next read (timesheet, pay statement, calendar, accrual index). This is the correction, but a payroll period already exported with a 24 h day will differ from a re-export. The sheet doctor now lists such days, so they can be found.
- managerClockOrderError_ refuses an equal pair that was previously writable. A manager clearing a day would blank a slot, not type equal times, so the refusal should not block real work; the message names the time.
- The two modal hooks now REMOVE the overlay; `ensureOverlay` recreates it on the next open (the sibling hooks' pattern), and `cnSchedListChanged_` re-renders only while the node exists, so a late list refresh after close is a no-op.
- Day Edit Save is disabled for the prefill's duration (typically 1–3 s). A manager who opens the modal purely to blank a day must wait for the load; the pending window is exactly the one that used to delete punches.

INVARIANTS AT RISK: none violated. INV-01 (no lock change), INV-02 (gates untouched; both manager writers still gate first), INV-04 (format validation still precedes the new order rule), INV-05 (future-time reject unchanged), INV-145 (the close hooks still OWN the close — they now perform it; a hook may still refuse), INV-176 (every break pair still deducted), INV-187 (no new zero-that-is-an-absence: an equal pair is a genuine zero and the doctor reports it), INV-208 (the send-time re-verify still refuses a stale, vanished, priceless or edited quote — bite-checked), INV-209 (eligibility still fails to "unknown", never a verdict).

NET SCORE: 5 − 0 = 5
  a) Would this have fired in production this month? F-01 YES (every OOP-quoted send on the deployed sheet shape refuses; deployed 2026-09-17). F-02 YES (both modals have been unclosable since they shipped on 2026-09-02; deployed 09-14 and 09-17). F-03 YES for the CLASS (a 30-second fat-finger is legal on the live path; whether one landed this month is unverified — counted because the doctor did not see it and nothing else would). F-04 YES (any filtered eligibility query on the real sheet). F-05 YES for the class (a Save inside the prefill window deletes punches; not observed). F-44 NO (unreachable today — defensive).
  b) New failure mode? NO for all six. The one behaviour change a reader should know about — equal-minute days recompute — is intended and reported by the doctor.

OPERATOR ACTIONS / DEPLOY:
- `clasp push -f` + cut a New version. | BLOCKS DEPLOY: Y (nothing here is live until pushed)
- `runSmokeTests` on prod: expect `Expected: 337 registrations (119 smoke · 91 integration-A · 127 integration-B)` — read the count off the run. | BLOCKS DEPLOY: N
- Walk S110 first (the composer send and the by-name eligibility check both failed before this batch), then S7's two new steps. | BLOCKS DEPLOY: N
- Run the sheet doctor once and read "inverted pairs" for any equal-minute day that was paying 24 h; fix via Day Edit. | BLOCKS DEPLOY: N
- Still owed from cycle 19's walk: step 2 (fourteen files, no Code.js), steps 5–8 (OOP diagnostics, `previewPtoAccruals('2026-08')`, S108/S109/S97, `INSTANCE_IS_PROD=true` + the dev instance). Step 5's diagnostics now also name unknown eligibility values by item. | BLOCKS DEPLOY: N
Deploy: Server — `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. Client (Time Clock views, Call Notes views, Intake views) — same push, same New-version step. Test Suite — same push; `runSmokeTests()` from the editor.

(Not complete in production until blocking operator actions are done AND the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- The integration tier has still never executed against the deployed project: `test_oop_verifyQuotes_currentStaleAndDeleted` was red for a day and counted as green because smoke skipped it. Standing up the dev instance (cycle-19 walk step 8) is what makes the nightly full run real.
- The live punch path still accepts a Clock Out in the same minute as a Clock In (it now pays 0 h, and the doctor reports it). A live-path refusal would be a UX decision (a rep may legitimately clock out immediately); not taken here.
- Batches 2–7 of the 2026-09-17 scan are in `.cycle/blocks/20-scan-batch-plan.md`.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md Common Gotchas: a rule for "two readers of one operator sheet share ONE column resolver" (the F-01/F-04 shape — g120 frames the mirror as client↔server; this one was server↔server) with the narrative in docs/gotchas.md; and a Shell-family rule that a registered `onClose` hook OWNS the close, including the removal (INV-145's contract, the F-02 incident).
- docs/gotchas.md / docs/design-decisions.md: the C3 overnight-wrap decision should state the equal-minute rule (zero hours, refused by the manager writers, reported by the doctor).
- .cycle/config.md: S7 gains the two Day Edit steps above; S110's Expected already covers by-name eligibility. A regression scenario for the two modals (open → Close / Escape) would close the class the visual matrix only opens.
- The running-totals block is already regenerated (counts --check green).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
