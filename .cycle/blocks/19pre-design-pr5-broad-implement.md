# 19pre — design handoff PR 5 (QA surface) — broad-implement block

Scope: docs/design_handoff_five_surfaces/IMPLEMENTATION_PLAN.md §4 PR 5 (Q1–Q8 + X2).
Written 2026-09-02 on branch claude/ums-team-tools-design-r8ar3o.

---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- Q1 | Pause-and-pin comments — the first keystroke in the composer PAUSES playback and PINS the moment (`qaOnCommentInput_` bound at render; `qaRenderPinRow_` with an editable m:ss + `qaPinnedAtSec_`); Post & resume / Post & stay paused / Discard replace the single button; `qaSubmitComment_(resume)` posts the PIN, never the drifted playhead, and refuses a null pin
- Q2 | `skipped` reachable — a **Skip…** action in the detail header (`qaSkip_` → `uiPrompt` reason → `qaSetRecordingStatus(fileId,'skipped',reason)`); server `SkipReason` trailing column (500-char cap, written only on skip); Skipped chip with count on the queue + the reason under the pill on the row
- Q3 | My Reviews transport parity — `qaRenderTransportFor_(host, audio, state, showKbd)` is the ONE transport (±5s / speed chips, listener-bound `data-qa-seek`/`data-qa-speed`); the detail passes `showKbd=true`, My Reviews `false` (no key handler exists there — decision 13 keeps the tab hidden from agents, the parity ships regardless); the read-only callout + `qa-avg-big`; the shared score-tone helper `qaScoreTone_` (≤2 red, 3 neutral, ≥4 green) tones every chip on both surfaces
- Q4 | Coverage first — `getQaQueue(period)` returns `coverage[]` (pure `qaCoverageRows_` join of recordings × latest scorecards × roster, case-insensitive, every roster rep listed), `period`/`periodOptions`/`target`/`todayYmd`/`periodEnd`, `coverageUnavailable` on a failed join (never an empty-looking list); period = this month / this quarter / last quarter from `DriveCreatedMs` (`qaPeriodKeysForYmd_`/`qaPeriodMatches_`/`qaPrevPeriod_`/`qaPeriodBounds_`/`qaPeriodOptions_`); client `.toolbar-tabs` period strip persisted to `umsQaPeriod` (18th localStorage key), summary strip (`qaCoverageSummary_`: covered / gaps / days left), coverage `mtRenderTable_` toned by `qaCoverageTier_`; `CONFIG.QA_AUDIT_TARGET_PER_PERIOD` = 3 (Script Property `QA_AUDIT_TARGET_PER_PERIOD` 1..50 overrides via `qaAuditTarget_`); `QaExemptions` tab + `qaSetExemption` (MANAGER-gated, locked, audit `QaExemption` period+flag only) with the pure `qaExemptEligible_` (two COVERED periods at ≥4.5 avg and no criterion <4); "Sample the gaps for me (N)" — `qaSamplePick_` target-aware (`targets` map, exempt → 0, a covered agent never pulled ahead of a short one), `qaSampleRecordings(count, period)` refuses an empty pick by name
- Q5 | Recordings list → sortable table — `mtRenderTable_` Recording · Agent · Length · Status · Reviewer · Comments · Actions (`qaSortQueue_`, `QA_STATE.sort`); Length from the new `DurationSec` column (`qaSetRecordingDuration`, QA-gated, write-once from the detail's `loadedmetadata`; em dash until known — INV-187); "Unattributed" warn-toned; `.qa-row-actions` nowrap so Open · Claim/Release stay on one line
- Q6 | Detail two panes — `.qa-det-grid` ≥1000px: LEFT agent attribution card (`qaRenderAgentRow_` + roster match line) + player + transport; RIGHT scorecard (1/3/5 anchors, running average `qaCardsAvg_`), comments, coach panel; header actions Skip… / Start review / Mark done / Reopen (the Phase-1 dead-handler class is covered by the QA-7 onclick scan)
- Q7 | Coaching hand-off — `#qa-coach-panel` (manager-only, attributed recordings only): `qaCoachOnThis_` parks `window.COACH_PREFILL = { empId: r.agentEmpId, qaFileId, what }` (the timestamped comments as the narrative) and `enterTool('develop','coaching')` (C8 park-and-consume); `qaSetRecordingAgent` returns `agentEmpId` (`qaRosterIdByName_`) and queue items carry it
- Q8 | `.qa-kbd-hint` → `--muted-2` (shipped in PR 1's X5 sweep; verified still in place)
- X2 | Two-level breadcrumbs `QA › Recordings` / `QA › Stats` / `QA › My Reviews` via `qaAppBarHtml_` (decision 10)

Files modified: web-app/Code.js, web-app/qa/script_qa.html, web-app/Tests.js, test/client/run.js, test/client/dom/runDom.js, test/visual/mock.js, test/visual/shoot.mjs, CLAUDE.md, .cycle/STATE.md, .cycle/blocks/19pre-design-pr5-broad-implement.md

CHANGES:
Q4/Q2/Q5/Q7 | web-app/Code.js | `CONFIG.QA_AUDIT_TARGET_PER_PERIOD`; `QA_RECORDINGS_HEADERS` +`DurationSec`+`SkipReason` (+ `QAR.DURATION_SEC/SKIP_REASON`), `getOrCreateQaSheet_` self-heals a short header; `QA_EXEMPTIONS_*` consts + `getOrCreateQaExemptionsSheet_`; sync rows widened; `qaSetRecordingStatus(fileId, status, reason)`; `getQaQueue(period)` (+`rosterIdByName`, `periodOptions`, `coverage` via `qaCoverageRows_`, `coverageUnavailable`); `qaSetRecordingAgent` returns `agentEmpId`; `qaRosterIdByName_`; pure `qaAuditTarget_`, `qaPeriodValid_`, `qaMsToYmd_`, `qaPeriodKeysForYmd_`, `qaPeriodMatches_`, `qaPrevPeriod_`, `qaPeriodLabel_`, `qaPeriodBounds_`, `qaPeriodOptions_`, `qaCardStats_`, `qaExemptEligible_`, `qaCoverageRows_`; `qaReadExemptions_`, `qaSetExemption`, `qaSetRecordingDuration`; `qaSamplePick_(…, targets)` + `qaSampleRecordings(count, period)`
Q1–Q8/X2 | web-app/qa/script_qa.html | rewritten (~1870 lines): app-bar chrome, period strip + `umsQaPeriod`, coverage section (summary strip, gaps sampler, coverage table, Grant/Revoke), recordings table + chips, two-pane detail, pause-and-pin composer, shared transport, toned scorecards, coach panel, My Reviews callout; polish after the shoot: nowrap row actions, "Write a comment…" placeholder, `showKbd` hint gate, empty-state subline
tests | web-app/Tests.js | omnibus `qaSetExemption` (manager tier); QA gate case +`qaSetRecordingDuration` (15 endpoints)
Q4 | test/visual/mock.js, test/visual/shoot.mjs | DO-NOT-EDIT region gains `QA_EXEMPT_AVG_MIN/CRIT_MIN` + eleven verbatim server fns; `getQaQueue` fixture is an IIFE calling the real `qaCoverageRows_` (eligible / low / in_review / exempt / skipped-with-reason / unattributed rows) + the `EMPTY_FIXTURES` twin; scenarios `qa-queue-dark-wide`, `qa-queue-empty-light-wide`, `qa-detail-light-mobile`
pins | test/client/run.js, test/client/dom/runDom.js | QA-10/11/14 rewritten in place; QA-19 coverage join, QA-21 periods/eligibility/exemption/duration/skip/getQaQueue contracts, QA-22 client pure helpers, QA-23 wiring (showKbd, nowrap, scenarios, fixture keys derived from `items.push({`); DOM QA-20 pause-and-pin

TEST RESULTS: pure 738/0 (was 734), DOM 103/0 (was 102), `node --check` Code.js + Tests.js OK, every JS partial parse-guarded. 12 mutations / 12 bites. Visual: 8 QA scenarios (queue light/dark/mobile/empty, detail wide/mobile, stats, my reviews) — `missing: []`, `overflowPx: 0`, eyeballed; the shoot surfaced four polish defects (row actions wrapping to three lines, a duplicated composer placeholder, a keyboard hint on a surface with no key handler, an "everyone at target" subline on an empty roster) — all fixed and re-shot. Editor suite: NOT run (no runtime off-editor) — `runAllTests()` still expects 307 (the QA gate case + omnibus grew IN PLACE). ONE pre-existing DOM flake observed during close-out: `the resume request states the unpaid gap before it is filed` failed twice at 18:31 UTC = 00:01 IST (the fixture rep is `Asia/Kolkata`; its `'00:01:00'` clock-out is not strictly in the past for the one minute after IST midnight) and passed at 18:32 — time-of-day dependent, not this PR's; follow-on below. Scenario S100 (new) walked against the matrix: PASS on the rendered halves; the server halves rest on QA-19/21 + the two editor cases.

REGRESSION RISKS:
- `getQaQueue` gained an optional `period` arg and ADDITIVE fields; an older client ignores them. `qaSetRecordingStatus` gained an optional 3rd arg (a 2-arg call skips with no reason). `qaSampleRecordings(count, period)` — the client now passes the shortfall N instead of a fixed 3, so the sampled count CHANGES (deliberate).
- `qaSamplePick_`'s pick ORDER changed (target-aware: an uncapped `(unassigned)` bucket at load 0 legitimately sorts first — QA-11 corrected twice before the pin matched the property).
- `qaSetRecordingAgent` returns `agentEmpId` additively. QA-14 stays gated (decision 13) — nothing agent-facing widened.
- The queue's per-row inline `onclick`s are gone in favour of table actions; the `qaQueue`/`qaStats`/`qaMyReviews` tab keys and every gate are unchanged.
- Coverage on a very old period (before recordings carried `DriveCreatedMs`) reads 0 sampled — correct, not a regression.

INVARIANTS AT RISK: INV-196 (amended — (g) coverage/period contract, (h) `QaExemptions` + manager gate, (i) duration write-once, (j) skip reason in the QA store only; audit rows stay id/count-only), INV-183 (roster join through `empRosterEmail_`), INV-185 (fixture calls the verbatim `qaCoverageRows_`; keys derived from the server push literal), INV-187/175 (Length em dash until known; `coverageUnavailable` on a failed join; empty vs error scenarios on camera), INV-169 (exemption scan bounded), INV-173/174 (period strip aria-pressed; real buttons; sortable headers), INV-156 (detail seq guards unchanged), INV-01/02 (`qaSetExemption` locked + manager-gated; GATE-TIER tripwire places it on the manager tier), A2 (`.qa-det-grid` is a min-width query; mobile shot 0 overflow), A14 (pin input + comment textarea named), C8 (`COACH_PREFILL` parked-then-consumed). None violated.
NET SCORE: 2 − 0 = 2 (production fixes: a comment landed at the DRIFTED playhead rather than the moment the reviewer started typing — every timestamped comment on a playing recording was late by the typing time; and "Sample 3 for me" could pull a fourth call for an agent already at target while another agent had none. The rest is capability/interface work per R18.)

OPERATOR ACTIONS / DEPLOY:
- Optionally set Script Property `QA_AUDIT_TARGET_PER_PERIOD` (1..50) to override the CONFIG seed of 3 | BLOCKS DEPLOY: N
Deploy: Server + Client (QA views): `cd web-app && clasp push -f`, then Apps Script editor → Deploy → Manage deployments → Edit → Version: New version → Deploy. Test Suite: same push; `runAllTests()` expects 307. No trigger changes.

FOLLOW-ON ITEMS:
- The handoff's Export button on the QA surface was not built (outside Q1–Q8 + X2).
- My Reviews carries the transport controls but NO keyboard shortcuts (space/←/→ are bound in the detail only) — the hint is deliberately hidden there; wiring keys is a small follow-on if agents ever get the tab back.
- Period arithmetic lives server-side only; the client never mirrors it (a period label is text). If a client-side period ever becomes necessary it owes a MIRROR_INDEX entry.
- DOM test `the resume request states the unpaid gap before it is filed` is time-of-day dependent: its `'00:01:00'` fixture clock-out fails for the minute after IST midnight (18:30–18:31 UTC). Derive the clock-out from `timeHmTz(tz)` minus a few minutes instead (the editor-test hazard (a) applied to the DOM harness).

DOCUMENTATION UPDATES NEEDED:
- Done in this session: CLAUDE.md QA tool description (PR 5 paragraph), localStorage 17→18 (`umsQaPeriod`), storage map QA row, manager-gated list (`qaSetExemption`), INV-196 amendment (g)–(j), operator-state entry for PR 5, count chain (738 / DOM 103 / matrix 90 / editor 307), S100.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
